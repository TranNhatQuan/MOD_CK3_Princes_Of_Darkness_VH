
heal_chulorviosis_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_personal_religious.dds"
	}
	decision_group_type = pod_splat
	ai_check_interval = 0

	is_shown = {
		has_trait = chulorviosis
	}

	is_valid = {
		OR = {
			has_trait = beatific
			is_a_thrall = yes
			POD_is_possessed_trigger = yes
			custom_tooltip = {
				text = heal_chulorviosis_decision_custom
				any_relation = {
					type = friend
					OR = {
						has_trait = beatific
						has_trait = demon
					}
				}
			}
			custom_tooltip = {
				text = heal_chulorviosis_decision_custom2
				any_relation = {
					type = lover
					OR = {
						has_trait = beatific
						has_trait = demon
					}
				}
			}
		}
	}

	is_valid_showing_failures_only = {
		is_travelling = no
		is_in_army = no
		is_imprisoned = no
		is_incapable = no
	}

	effect = {
		recover_from_disease_notify_effect = { DISEASE = chulorviosis }
	}
}

stop_flaying_plague_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_personal_religious.dds"
	}
		
	decision_group_type = major

	ai_check_interval = 0

	is_shown = {
		has_trait = fera
		POD_is_wyrm_servant_trigger = no
		POD_bookmark_has_flaying_plague = yes
		NOT = { has_global_variable = flaying_plague_stopped }
	}

	is_valid = {
		custom_tooltip = {
			text = stop_flaying_plague_decision_custom
			
			coterie_diplomacy_gui >= 10
			coterie_martial_gui >= 10
			coterie_stewardship_gui >= 10
			coterie_intrigue_gui >= 10
			coterie_learning_gui >= 10
			coterie_prowess_gui >= 40
		}
	}

	is_valid_showing_failures_only = {
		is_travelling = no
		is_in_army = no
		is_imprisoned = no
		is_incapable = no
	}

	effect = {
		show_as_tooltip = {
			
			add_prestige = massive_prestige_value
			dynasty ?= { add_dynasty_prestige = medium_dynasty_prestige_value }
		}
		
		custom_tooltip = stop_flaying_plague_decision_effect_tooltip
		trigger_event = pod1230objectives_fera.27
	}
	
	#ai_potential = {
	#	years_from_game_start >= 10
	#	NOT = { any_player = { has_trait = fera } }
	#}
	#
	#ai_will_do = {
	#	base = 100
	#}
}

weaponize_flaying_plague_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_personal_religious.dds"
	}
		
	decision_group_type = major

	ai_check_interval = 0

	is_shown = {
		#I want it to be rare, almost hidden. When we add the circle of red, we can open it a little more
		#POD_is_wyrm_servant_trigger = yes
		trigger_if = {
			limit = { POD_splat_mixing_haight_mode = no }

			OR = {
				this = character:791778 #Viorica is the closest canon wise to achieve it
				POD_is_infernalist_trigger = yes #Circle of red
				#For players wanting to chase this, ask Viorica
				has_relation_friend = character:791778
				has_relation_best_friend = character:791778
				has_relation_soulmate = character:791778
				has_relation_coterie_member = character:791778
			}		
		}
		
		POD_bookmark_has_flaying_plague = yes
		NOT = { has_global_variable = flaying_plague_stopped }
	}

	is_valid = {
		#Be a learned person...
		learning >= 20
		
		#...and have some werewolves ready to be experimented upon
		
		custom_tooltip = {
			text = weaponize_flaying_plague_decision_custom
			any_prisoner = {
				count >= 3
				has_trait = werewolf
			}
		}

		#The AI will accomplish the stop the FP on its own, so there is a time limit of sorts to inform the player of
		custom_tooltip = weaponize_flaying_plague_decision_customb
	}

	is_valid_showing_failures_only = {
		is_travelling = no
		is_in_army = no
		is_imprisoned = no
		is_incapable = no
	}

	effect = {
		custom_tooltip = stop_flaying_plague_decision_effect_tooltip
		custom_tooltip = weaponize_flaying_plague_decision_effect_tooltip
		if = {
			limit = { has_POD_vamp_blood_trigger = yes }
			custom_tooltip = weaponize_flaying_plague_decision_effect_tooltip_2
		}
		trigger_event = pod1230objectives_fera.29
	}
}
