survey_capital_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_misc.dds"
	}

	ai_check_interval = 0
	decision_group_type = pod_splat
	cost = { gold = standard_activity_cost }

	cooldown = { years = 10 }

	desc = survey_capital_decision_opening_desc
	selection_tooltip = survey_capital_decision_tooltip

	is_shown = {
		is_landed = yes
		is_vampire_trigger = yes
		highest_held_title_tier > tier_barony
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
		location = capital_province
	}

	effect = {

		#Stress loss
		if = {
			limit = {
				has_trait = lazy
			}
			custom_tooltip = start_hunt_decision_stress_lazy
		}
		else = {
			stress_impact = {
				base = hunt_stress_loss_value
				craven = activity_stress_gain_impact
				diligent = activity_stress_loss_impact
				athletic = activity_stress_loss_impact
				sadistic = activity_stress_loss_impact
				brave = activity_stress_loss_impact
				lifestyle_hunter = activity_stress_loss_impact
				generous = activity_stress_loss_impact
			}
		}
		
		custom_tooltip = survey_decision_effect_tooltip
		
		custom_tooltip = start_hunt_decision_prestige

		trigger_event = POD_survey_events.1000

	    add_legitimacy = minor_legitimacy_gain			
	}
	
	ai_potential = {
		short_term_gold > standard_activity_cost
		NOT = {
			has_trait = reclusive
		}
	}

	ai_will_do = {
		base = 0
	}
}

