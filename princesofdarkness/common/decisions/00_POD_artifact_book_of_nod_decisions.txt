POD_book_of_nod_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_POD_book_of_nod.dds"
	}

	decision_group_type = pod_artifact
	ai_check_interval = 0
	desc = POD_book_of_nod_decision_opening_desc
	selection_tooltip = POD_book_of_nod_decision_tooltip
	
	cooldown = { days = 365 }
	
	is_shown = {
		POD_has_any_book_of_nod_tale_trigger = yes
		is_playable_character = yes
		highest_held_title_tier >= tier_county
		exists = capital_province
	}
	
	is_valid = {
		OR = {
			custom_tooltip = {
				text = library_victory_objective_decision_custom
				any_character_artifact = { has_variable = book_of_nod }
			}
			custom_tooltip = {
				text = POD_book_of_nod_decision_custom_cain
				any_character_artifact = { has_variable = chronicle_of_caine }
			}
			POD_character_owns_linked_artifact = { LINK = chronicle_of_shadows1 }
			POD_character_owns_linked_artifact = { LINK = chronicle_of_shadows2 }
			POD_character_owns_linked_artifact = { LINK = chronicle_of_secrets1 }
			POD_character_owns_linked_artifact = { LINK = chronicle_of_secrets2 }
			POD_character_owns_linked_artifact = { LINK = first_of_times1 }
			POD_character_owns_linked_artifact = { LINK = first_of_times2 }
			POD_character_owns_linked_artifact = { LINK = coming_of_lilith1 }
			POD_character_owns_linked_artifact = { LINK = coming_of_lilith2 }
			POD_character_owns_linked_artifact = { LINK = liliths_magick1 }
			POD_character_owns_linked_artifact = { LINK = liliths_magick2 }
			POD_character_owns_linked_artifact = { LINK = temptation_of_caine1 }
			POD_character_owns_linked_artifact = { LINK = temptation_of_caine2 }
			POD_character_owns_linked_artifact = { LINK = zillahs_tale1 }
			POD_character_owns_linked_artifact = { LINK = zillahs_tale2 }
			POD_character_owns_linked_artifact = { LINK = tale_of_the_crone1 }
			POD_character_owns_linked_artifact = { LINK = tale_of_the_crone2 }
			POD_character_owns_linked_artifact = { LINK = tale_of_the_first_city1 }
			POD_character_owns_linked_artifact = { LINK = tale_of_the_first_city2 }
		}
	}

	effect = {
		trigger_event = POD_artifacts_book_of_nod.100 
		custom_tooltip = POD_book_of_nod_decision_effect_tooltip
	}
}
