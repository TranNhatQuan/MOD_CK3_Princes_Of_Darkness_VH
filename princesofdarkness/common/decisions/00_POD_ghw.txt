PODghw_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_POD_ghw_unlock.dds"
	}

	desc = PODghw_decision_desc
	selection_tooltip = PODghw_decision_tooltip
	decision_group_type = pod_splat
	cost = {		
		piety = 1000
	}

	ai_check_interval = 0
	
	ai_potential = {
		always = no
	}

	ai_will_do = {
		base = 0
	}	

	is_shown = {
		always = no
		#faith = { 
		#	exists = religious_head
		#}		
		#faith = { 
		#	NOT = { this = faith:shadowinquisition }
		#	OR = {
		#		has_doctrine = tenet_armed_pilgrimages #Catholics
		#		has_doctrine = tenet_struggle_submission #Islam (though, the default Faiths have Temporal Heads, so still do not qualify)
		#		has_doctrine = tenet_warmonger #Germanic (though, starting as Unreformed, it still doesn't qualify by default)
		#		has_doctrine = divine_destiny_doctrine #SPECIAL: Has become the Chakravarti
		#	}
		#	OR = {
		#		has_doctrine = doctrine_temporal_head
		#		has_doctrine = doctrine_spiritual_head
		#	}
		#	NOR = {
		#		has_doctrine_parameter = unreformed
		#		AND = {
		#			has_doctrine = tenet_dharmic_pacifism
		#			NOT = { has_doctrine = divine_destiny_doctrine }
		#		}
		#		has_doctrine = tenet_pacifism
		#	}
		#}
		#NOT = { has_global_variable = POD_ghw_unlocked }
	}

	is_valid = {
		piety_level >= 5 
		faith = {
			custom_tooltip = {
				text = hof_exists_tt
				exists = religious_head
			}
			num_county_followers >= 35 # Faith must have at least SOME legitimacy.
			fervor >= 65
		}
	}

	is_valid_showing_failures_only = {
		is_available = yes
		faith = {
			custom_tooltip = {
				text = hof_exists_tt
				exists = religious_head
			}
			religious_head = { is_imprisoned = no }
		}
	}

	effect = {
		set_global_variable = {
			name = POD_ghw_unlocked
			value = yes
		}		
		custom_tooltip = PODghw_decision_effect_tooltip
		faith = { trigger_event = great_holy_war.0010 }
	}	
}

PODshadowcrusade_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_POD_shadowcrusade.dds"
	}

	desc = PODshadowcrusade_decision_desc
	selection_tooltip = PODshadowcrusade_decision_tooltip

	ai_check_interval = 0
	
	ai_potential = {
		always = no
	}

	ai_will_do = {
		base = 0
	}	

	cost = {		
		piety = 500
	}

	is_shown = {
		always = no
		#faith = faith:shadowinquisition
		#NOT = { has_global_variable = POD_ghw_unlocked }
	}

	is_valid = {
		piety_level >= 5 
		faith = {
			custom_tooltip = {
				text = hof_exists_tt
				exists = religious_head
			}
			num_county_followers >= 35 # Faith must have at least SOME legitimacy.
			fervor >= 65
		}
	}

	is_valid_showing_failures_only = {
		is_available = yes
		faith = {
			custom_tooltip = {
				text = hof_exists_tt
				exists = religious_head
			}
			religious_head = { is_imprisoned = no }
		}
	}

	effect = {
		set_global_variable = {
			name = POD_ghw_unlocked
			value = yes
		}		
		custom_tooltip = PODshadowcrusade_decision_effect_tooltip
		faith = { trigger_event = great_holy_war.0010 }
	}
}