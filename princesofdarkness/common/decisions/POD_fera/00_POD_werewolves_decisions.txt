judge_land_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_dynasty_house.dds"
	}
	desc = judge_land_decision_desc
	decision_group_type = pod_splat
	ai_check_interval = 0

	is_shown = {
		has_perk = aura_leadership_perk
	}

	is_valid = {
		is_landed = yes
	}
	
	cooldown = { years = 10 }
	
	cost = { prestige = 150 }
	
	effect = {
		capital_county = {
			add_county_modifier = {
				modifier = judge_land_modifier
				years = 10
			}
		}
		
		if = {
			limit = {
				has_perk = command_gathering_perk
			}
			capital_county = {
				add_county_modifier = {
					modifier = prospering_populace_modifier
					years = 10
				}
			}
		}

		if = {
			limit = {
				has_perk = roll_over_perk
			}
			scripted_effect_survey_increase_opportunities_effect = yes
		}
	}

	ai_potential = {
		always = no
	}

	ai_will_do = {
		base = 0
	}
}

#WDA rev p101
auspice_renunciation_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_dynasty_house.dds"
	}

	decision_group_type = pod_splat
	
	cost = { prestige = { value = major_prestige_value } }

	is_shown = {
		POD_is_werewolf_trigger = yes
		NOT = { has_character_flag = has_auspice_renunciation }
	}
	is_valid = {
	}
	effect = {
		custom_tooltip = auspice_renunciation_decision_tooltip
		add_character_flag = has_auspice_renunciation
		
		remove_trait = ahroun
		remove_trait = galliard
		remove_trait = philodox
		remove_trait = theurge
		remove_trait = ragabash

		add_trait_xp = {
			trait = werewolf
			value = -100
		}
		
		trigger_event = POD_werewolves.6
	
		add_legitimacy = massive_legitimacy_loss
	}

	ai_check_interval = 0
	ai_potential = { always = no }

	ai_will_do = { base = 0 }
}