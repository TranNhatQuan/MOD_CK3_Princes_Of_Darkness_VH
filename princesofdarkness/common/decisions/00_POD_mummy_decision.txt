POD_show_true_name_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_personal_religious.dds"
	}

    ai_check_interval = 0
	decision_group_type = pod_splat	
	
    is_shown = {
		POD_can_study_true_names_trigger = yes
	}

	#cooldown = { years = 1 } not needed as this is informational only

	effect = {
		POD_display_known_ren = yes
		trigger_event = POD_show_known_true_names_events.100
	}

	ai_will_do = {
		base = 0
	}
}

POD_renaming_the_hekau_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_personal_religious.dds"
	}

	ai_check_interval = 0
	decision_group_type = major

	is_shown = {
		has_perk = renaming_the_hekau_perk
		NOT = { has_character_flag = used_renaming_the_hekau }
	}

	widget = {
		gui = "decision_view_widget_renaming_the_hekau"
		controller = decision_option_list_controller
		decision_to_second_step_button = "NEXT_LABEL"
		show_from_start = yes

		# dummy option (gets ignored by the GUI)
		item = {
			value = blank_line
			localization = blank_line
			ai_chance = { value = 1 }
		}
	}

	effect = {
		save_scope_as = actor
		custom_tooltip = {
			text = POD_renaming_the_hekau_decision_tt
			POD_start_journey_effect = {
				TYPE    = scope:actor.var:renaming_the_hekau_selection
				MONTHLY = flag:seek_knowledge
			}
		}
		POD_journey_interaction_pause_hint = yes
		add_character_flag = used_renaming_the_hekau
		POD_renaming_the_hekau_clear_journey_list = yes
	}

	ai_will_do = {
		base = 0
	}
}

POD_shih_summon_decision = {
	picture = {
		reference = "gfx/interface/illustrations/decisions/decision_personal_religious.dds"
	}

	ai_check_interval = 0
	decision_group_type = pod_splat

	is_shown = {
		has_trait = wutian
	}

	cooldown = { years = 5 }

	cost = { 
		gold = 250
		prestige = 500 
	}

	effect = {
		custom_tooltip = {
			text = POD_shih_summon_decision_effect
			create_character = {
				location = root.capital_province
				age = { 16 45 }
				random_traits = yes
				culture = ROOT.culture
				faith = faith:shih_faith
				trait = supehunter
				gender_female_chance = 50
				after_creation = {
					POD_set_resonance_effect = yes
					POD_shih_initialization_effect = yes
					POD_shih_random_perk_initialization_for_noble_effect = yes
				}
				save_scope_as = shih
			}
			send_interface_toast = {
				title =  POD_shih_summon_decision
				right_icon = scope:shih
				add_courtier = scope:shih
				hidden_effect = { return_to_court = yes }
			}
		}
	}

	ai_will_do = {
		base = 0
	}
}