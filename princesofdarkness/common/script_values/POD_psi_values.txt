psychic_add_success_chance = {
    value = 0
    #Synergy
    if = { limit = { scope:recipient = { has_trait = psychic } scope:actor = { has_perk = synergy_1_perk } } add = 10 }
    scope:actor = {
        if = { limit = { has_variable = coterie } 
            var:coterie = { 
                every_in_list = { 
                    variable = members 
                    limit = { has_trait = psychic NOT = { this = scope:actor } }
                    add = 10
                }
            }
        }
        if = {
            limit = { has_trait = psychic }
            if = { limit = { has_trait_xp = { trait = psychic value >= 20 } } add = 10 }
            if = { limit = { has_trait_xp = { trait = psychic value >= 40 } } add = 10 }
            if = { limit = { has_trait_xp = { trait = psychic value >= 60 } } add = 10 }
            if = { limit = { has_trait_xp = { trait = psychic value >= 80 } } add = 10 }
            if = { limit = { has_trait_xp = { trait = psychic value >= 100 } } add = 10 }
        }
    }
}
psychic_willpower_cost_small = {
    value = 15
    if = { limit = { scope:recipient = { has_perk = anti_psi_1_perk } NOT = { scope:recipient = scope:actor } } add = 5 }
    if = { limit = { scope:recipient = { has_perk = anti_psi_2_perk } NOT = { scope:recipient = scope:actor } } add = 5 }
    if = { limit = { scope:recipient = { has_perk = anti_psi_3_perk } NOT = { scope:recipient = scope:actor } } add = 5 }
    if = { limit = { scope:recipient = { has_perk = anti_psi_4_perk } NOT = { scope:recipient = scope:actor } } add = 300 }
}
psychic_willpower_cost_medium = {
    value = psychic_willpower_cost_small
    multiply = 2
}
psychic_willpower_cost_huge = {
    value = psychic_willpower_cost_small
    multiply = 3
}
psychic_willpower_cost_massive = {
    value = psychic_willpower_cost_small
    multiply = 4
}
necro_psi_possession_limit = {
    value = 1
    if = { limit = { scope:actor = { has_perk = necro_psi_5_perk } } add = 1  }
}
POD_psychic_vampirism_interaction_cooldown = {
    value = 5
    if = { limit = {has_perk = psychic_vampirism_4_perk } add = -3 }
}

intellect_psi_lifestyle_xp_mult = {
	switch = {
		trigger = has_trait
		intellect_good_1 = { value = 0.25 }
		intellect_good_2 = { value = 0.5 }
		intellect_good_3 = { value = 1 }
	}
}

medium_courtiers_count = {
	value = 0
}

POD_psi_synergy_strength = {
    if = {
        limit = {
            has_perk = synergy_2_perk
            has_variable = coterie
        }
        var:coterie = {
            every_in_list = {
                variable = members
                limit = { has_trait = psychic }
                add = 1
            }
        }
    }
}