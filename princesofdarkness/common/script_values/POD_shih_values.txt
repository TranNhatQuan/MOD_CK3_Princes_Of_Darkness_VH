shih_skill_limit = {
        value = 15
        if = { limit = { POD_increased_shih_training_skill_limit_trigger = yes } add = 5 }
}
shih_prowess_skill_limit = {
        value = 25
        if = { limit = { POD_increased_shih_training_skill_limit_trigger = yes } add = 5 }
}
shih_children_above_60_skills_total = {
        value = 0
        every_child = {
                limit = { sum_of_all_skills_and_prowess_value >= 60 }
                add = 1
        }
}