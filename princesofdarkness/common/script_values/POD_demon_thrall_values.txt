POD_num_of_cultists = {
	value = num_of_relation_worshipping_cultist
}

cultist_faith_power = {
	value = 1
	if = { limit = { is_a_thrall = yes } add = 1 }
}

no_resurrecting_prisoner_count = {
	value = 0
	every_prisoner = {
		limit = { POD_has_guaranteed_resurrection = no }
		add = 1
	}
}

target_combined_building_level_count = {
	value = 0
	scope:target = {
		title_province = {
			add = combined_building_level
		}
	}
}

target_combined_development_level_count = {
	value = 0
	scope:target = {
		every_de_jure_county = {
			add = development_level
		}
	}
}

recipient_combined_development_level_count = {
	value = 0
	scope:recipient.primary_title = {
		every_de_jure_county = {
			add = development_level
		}
	}
}

POD_sacrifice_prisoner_investiture_xp_gain = {
	value = no_resurrecting_prisoner_count
	multiply = 25
	max = 1000
}

POD_sacrifice_holding_investiture_xp_gain = {
	value = target_combined_building_level_count
	multiply = 75
	max = 3000
}

POD_sacrifice_region_investiture_xp_gain = {
	value = target_combined_development_level_count
	multiply = 100
	max = 5000
}
mass_command_gold_potential_target_value = {
	value = num_of_relation_worshipping_cultist
	subtract = num_of_relation_possessed_target
}

mass_command_gold_value = {
	ordered_relation = {
		type = worshipping_cultist
		limit = { 
			gold > 0
			NOT = { has_relation_possessed_target = root }
		}
		order_by = gold
		max = mass_command_gold_thrall_list_max_size
		if = {
			limit = { gold >= root.major_gold_value }
			add = root.major_gold_value
		}
		else = {
			add = gold
		}
	}
}

mass_command_gold_thrall_list_max_size = {
	if = {
		limit = { root.num_of_relation_worshipping_cultist >= root.arcana_lifestyle_perk_points }
		value = root.arcana_lifestyle_perk_points
	}
	else = {
		value = root.num_of_relation_worshipping_cultist
	}
}
