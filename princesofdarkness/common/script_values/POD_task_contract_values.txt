# necessary for setting variable expiration dates
# since they don't take inline script math
POD_days_remaining_for_task_contract = {
	value = var:end_date
	subtract = current_date
}

# same as above, but with scope:contract saved on the TopScope
POD_days_remaining_for_scoped_task_contract = {
	value = scope:contract.var:end_date
	subtract = current_date
}

# Weights
POD_contracts_weight_up_slayers_value = {
	if = {
		limit = {
			POD_splat_can_inquihunt_trigger = yes
			POD_valid_target_for_inquihunt_trigger = {
				HUNTER = root
				HUNTED = scope:employer
			}
		}
		add = task_contract_weight_standard_bonus_value
	}
}


POD_natural_disaster_delay_years = 3

POD_disaster_flag_duration = {
	value = scope:task_contract.POD_days_remaining_for_task_contract
	add = 1
}