

wyrm_taint_value = {
	value = 0
    if = {
        limit = { exists = var:wyrm_taint }
        add = var:wyrm_taint
    }
}

wyrm_taint_yearly_change_value = {
    value = { value = -2 desc = BASE_VALUE format = "POD_FORMAT_BASE_POSITIVE" }

###General
    add = {
        value = num_sinful_traits
        desc = wyrm_taint_value_sins
        format = "POD_FORMAT_PLUS_NEGATIVE"
    }

    if = {
		limit = { piety_level > 1 }
		subtract = {
            value = piety_level
            desc = wyrm_taint_value_piety
            format = "POD_FORMAT_BASE_POSITIVE"
        }
	}


###Traits
##Negative
#Stress Traits
    if = {
        limit = { has_trait = drunkard }
        add = {
            value = 1
            desc = trait_drunkard
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = hashishiyah }
        add = {
            value = 1
            desc = trait_hashishiyah
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = rakish }
        add = {
            value = 1
            desc = trait_rakish
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = reclusive }
        add = {
            value = 2
            desc = trait_reclusive
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = irritable }
        add = {
            value = 1
            desc = trait_irritable
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = flagellant }
        add = {
            value = 1
            desc = trait_flagellant
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = profligate }
        add = {
            value = 1
            desc = trait_profligate
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = improvident } #kinda weird but might as well
        add = {
            value = 1
            desc = trait_improvident
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = contrite }
        add = {
            value = 1
            desc = trait_contrite
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = comfort_eater }
        add = {
            value = 1
            desc = trait_comfort_eater
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = inappetetic }
        add = {
            value = 1
            desc = trait_inappetetic
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    
#Lifestyle Traits
    if = {
        limit = { has_trait = schemer }
        add = {
            value = 1
            desc = trait_schemer
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = torturer }
        add = {
            value = 1
            desc = trait_torturer
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
#Other Traits
    if = {
        limit = {
            OR = {
                has_trait = lunatic_1
                has_trait = lunatic_genetic
            }
        }
        add = {
            value = 2
            desc = trait_lunatic_1
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = {
            OR = {
                has_trait = possessed_1
                has_trait = possessed_genetic
            }
        }
        add = {
            value = 2
            desc = trait_possessed_1
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = berserker }
        add = {
            value = 1
            desc = trait_berserker
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = kinslayer_1 }
        add = {
            value = 1
            desc = trait_kinslayer_1
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = kinslayer_2 }
        add = {
            value = 2
            desc = trait_kinslayer_2
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = kinslayer_3 }
        add = {
            value = 3
            desc = trait_kinslayer_3
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = cannibal }
        add = {
            value = 2
            desc = trait_cannibal
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
    if = {
        limit = { has_trait = disloyal }
        add = {
            value = 1
            desc = trait_disloyal
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
    }
#POD
    if = {
        limit = { has_trait = demonology }
        if = {
            limit = { has_trait_xp = { trait = demonology value >= 100 } }
            add = {
                value = 6
                desc = trait_demonology_3
                format = "POD_FORMAT_PLUS_NEGATIVE"
            }
        }
        else_if = {
            limit = { has_trait_xp = { trait = demonology value >= 50 } }
            add = {
                value = 4
                desc = trait_demonology_2
                format = "POD_FORMAT_PLUS_NEGATIVE"
            }
        }
        else = {
            add = {
                value = 2
                desc = trait_demonology_1
                format = "POD_FORMAT_PLUS_NEGATIVE"
            }
        }
    }

##Positive
#Lifestyle Traits
    if = {
        limit = { has_trait = family_first }
        subtract = {
            value = 1
            desc = trait_family_first
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = gallant }
        subtract = {
            value = 1
            desc = trait_gallant
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = theologian }
        subtract = {
            value = 2
            desc = trait_theologian
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
#Other Traits
    if = {
        limit = { has_trait = pilgrim }
        subtract = {
            value = 1
            desc = trait_pilgrim
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = devoted }
        subtract = {
            value = 2
            desc = trait_devoted
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = faith_warrior }
        subtract = {
            value = 2
            desc = trait_faith_warrior
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = loyal }
        subtract = {
            value = 2
            desc = trait_loyal
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
#POD
    if = {
        limit = { has_trait = truefaith }
        subtract = {
            value = 2
            desc = trait_truefaith
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = beatific }
        subtract = {
            value = 5
            desc = trait_beatific
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = drone }
        subtract = {
            value = 1 #Weaver is "closest" to the wyrm
            desc = trait_drone
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = gorgon }
        subtract = {
            value = 3 #Wyld is coolest because it shares 2 letters with Wyrm
            desc = trait_gorgon
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_trait = kami }
        subtract = {
            value = 2 #Gaia fails a lot
            desc = trait_kami
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }


###Temporary Modifiers
#Negative
    if = {
		limit = { has_variable = balefire_health_modifier_stacks }
		add = {
            value = var:balefire_health_modifier_stacks
            multiply = 2
            desc = wyrm_taint_value_balefire
            format = "POD_FORMAT_PLUS_NEGATIVE"
        }
	}

#Positive

###Perks and Permanent Modifiers
#Positive
    #Faiths
    if = {
        limit = { faith = { has_doctrine = doctrine_fera_triat_alignment_balanced } }
        subtract = {
            value = 1
            desc = doctrine_fera_triat_alignment_balanced_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    #Werewolf
    if = {
        limit = { has_perk = strength_will_perk }
        subtract = {
            value = 1
            desc = strength_will_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = truth_gaia_perk }
        subtract = {
            value = 1
            desc = truth_gaia_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = sense_wyrm_perk }
        subtract = {
            value = 1
            desc = sense_wyrm_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = swallow_rage_perk }
        subtract = {
            value = 1
            desc = swallow_rage_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = blessing_of_the_first_pack_perk }
        subtract = {
            value = 1
            desc = blessing_of_the_first_pack_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = word_of_honor_perk }
        subtract = {
            value = 1
            desc = word_of_honor_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    
    #Bastet
    if = {
        limit = { has_perk = alms_to_the_poor_perk }
        subtract = {
            value = 1
            desc = alms_to_the_poor_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = call_to_battle_perk }
        subtract = {
            value = 1
            desc = call_to_battle_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }

    #Mokole
    if = {
        limit = { has_perk = strength_of_purpose_perk }
        subtract = {
            value = 1
            desc = strength_of_purpose_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = bloody_mindedness_perk }
        subtract = {
            value = 1
            desc = bloody_mindedness_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = grondr_purification_perk }
        subtract = {
            value = 1
            desc = grondr_purification_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }

    #Hengeyokai
    if = {
        limit = { has_perk = sense_imbalance_perk }
        subtract = {
            value = 1
            desc = sense_imbalance_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }

    #Planetary
    if = {
        limit = { has_perk = earth_heal_perk }
        subtract = {
            value = 1
            desc = earth_heal_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
    if = {
        limit = { has_perk = cleansing_flame_perk }
        subtract = {
            value = 1
            desc = cleansing_flame_perk_name
            format = "POD_FORMAT_BASE_POSITIVE"
        }
    }
}

wyrm_taint_miniscule_value = 2
wyrm_taint_minor_value = 5
wyrm_taint_medium_value = 10
wyrm_taint_massive_value = 15
wyrm_taint_monumental_value = 25


wyrm_taint_torture_value = {
    value = wyrm_taint_minor_value

    if = {
        limit = {
            POD_is_mokole_trigger = yes
            scope:actor.faith ?= { has_doctrine_parameter = mokole_wyrm_taint_torture }
        }
        add = 10
    }
}

###############################
#### Urge Taints Lifestyle ####
###############################
physical_urgetaints_damage_value = {
    value = 1

    if = {
        limit = { physical_urgetaints_perks_value >= 3 }
        add = {
            value = physical_urgetaints_perks_value
            divide = 3
            floor = yes
        }
    }

    if = {
        limit = { is_ai = yes }
        max = 2
    }
}

physical_urgetaints_perks_value = {
    value = 0
    if = {
        limit = { has_variable = physical_urgetaints_perks }
        add = var:physical_urgetaints_perks
    }
}

mental_urgetaints_stress_value = {
    value = 10

    add = {
        value = mental_urgetaints_perks_value
        multiply = 10
    }

    if = {
        limit = { is_ai = yes }
        max = 50
    }
}

mental_urgetaints_perks_value = {
    value = 0
    if = {
        limit = { has_variable = mental_urgetaints_perks }
        add = var:mental_urgetaints_perks
    }
}

urgetaints_xp_gain_value = {
    value = 100
	
	add = {
		value = learning
		multiply = 20
    }
	if = {
		limit = { piety_level >= 2 }
		add = {
			value = piety_level
			multiply = 100
		}
	}
	add = {
		value = POD_fera_rank_value
		multiply = 100
	}

    if = {
		limit = { faith = { has_doctrine_parameter = reduced_urgetaints_xp } }
		multiply = 0.75
	}
    else_if = {
		limit = { faith = { has_doctrine_parameter = increased_urgetaints_xp } }
		multiply = 1.25
	}

	max = 1000
}

##############
#### Misc ####
##############

wyrm_cannibalism_tenet_lifestyle_xp = { #Needs to scope to the victim
    value = 0

    add = {
        value = age
        multiply = 2
    }
    if = {
        limit = { age >= 100 }
        add = {
            value = age
            divide = 100
            floor = yes
            multiply = 50
        }
    }
}