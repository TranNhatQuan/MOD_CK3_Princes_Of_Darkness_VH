##############################################
##########   VAMPIRE INTERACTIONS   ##########
##############################################

POD_dementation_interaction_cost = {
	if = { limit = { scope:prison_of_the_mind    ?= yes } add = 5 }
	if = { limit = { scope:childmind             ?= yes } add = 4 }
	if = { limit = { scope:cobble_the_road       ?= yes } add = 4 }
	if = { limit = { scope:stain_the_soul        ?= yes } add = 3 }
	if = { limit = { scope:total_insanity        ?= yes } add = 2 }
	if = { limit = { scope:incubus_passion       ?= yes } add = 1 }
	if = { limit = { scope:passion               ?= yes } add = 1 }
	if = { limit = { scope:the_haunting          ?= yes } add = 1 }
	if = { limit = { scope:silence_the_sane_mind ?= yes } add = 1 }
	if = { limit = { scope:fire_voice            ?= yes } add = 1 }

	min = 1
	#max = 5 # overflow possible with blood-storing artifacts
}

POD_touch_of_the_saints_interaction_cost = {
	if = { limit = { scope:drunkard          ?= yes } add = 1 }
	if = { limit = { scope:flagellant        ?= yes } add = 1 }
	if = { limit = { scope:comfort_eater     ?= yes } add = 1 }
	if = { limit = { scope:contrite          ?= yes } add = 1 }
	if = { limit = { scope:improvident       ?= yes } add = 1 }
	if = { limit = { scope:inappetetic       ?= yes } add = 1 }
	if = { limit = { scope:reclusive         ?= yes } add = 1 }
	if = { limit = { scope:irritable         ?= yes } add = 1 }
	if = { limit = { scope:rakish            ?= yes } add = 1 }
	if = { limit = { scope:hashishiyah       ?= yes } add = 1 }
	if = { limit = { scope:profligate        ?= yes } add = 1 }
	if = { limit = { scope:depressed         ?= yes } add = 1 }
	if = { limit = { scope:lunatic           ?= yes } add = 1 }
	if = { limit = { scope:possessed         ?= yes } add = 1 }
	if = { limit = { scope:lunatic_genetic   ?= yes } add = 1 }
	if = { limit = { scope:depressed_genetic ?= yes } add = 1 }

	min = 1
	#max = 5 # overflow possible with blood-storing artifacts
}

POD_melpominee_interaction_cost = {
	if = { limit = { scope:haunting_melody  ?= yes } add = 3 }
	if = { limit = { scope:toreadors_bane   ?= yes } add = 1 }
	if = { limit = { scope:sirens_beckoning ?= yes } add = 1 }
	if = { limit = { scope:mosh_pit         ?= yes } add = 1 }
	if = { limit = { scope:primal_scream    ?= yes } add = 1 }

	min = 1
	#max = 5 # overflow possible with blood-storing artifacts
}

POD_steal_the_mind_perk_interaction_cost = {
	if = { limit = { scope:diplomacy   ?= yes } add = 1 }
	if = { limit = { scope:martial     ?= yes } add = 1 }
	if = { limit = { scope:stewardship ?= yes } add = 1 }
	if = { limit = { scope:intrigue    ?= yes } add = 1 }
	if = { limit = { scope:learning    ?= yes } add = 1 }

	min = 1
}

POD_unburden_the_bestial_soul_perk_interaction_cost = {
	if = { limit = { scope:conscience   ?= yes } add = 1 }
	if = { limit = { scope:courage      ?= yes } add = 1 }
	if = { limit = { scope:self_control ?= yes } add = 1 }
	if = { limit = { scope:derangements ?= yes } add = 1 }

	min = 1
}

POD_king_davids_blessing_perk_interaction_cost = {
	if = { limit = { scope:drunkard      ?= yes } add = 1 }
	if = { limit = { scope:flagellant    ?= yes } add = 1 }
	if = { limit = { scope:comfort_eater ?= yes } add = 1 }
	if = { limit = { scope:contrite      ?= yes } add = 1 }
	if = { limit = { scope:improvident   ?= yes } add = 1 }
	if = { limit = { scope:inappetetic   ?= yes } add = 1 }
	if = { limit = { scope:reclusive     ?= yes } add = 1 }
	if = { limit = { scope:irritable     ?= yes } add = 1 }
	if = { limit = { scope:rakish        ?= yes } add = 1 }
	if = { limit = { scope:hashishiyah   ?= yes } add = 1 }
	if = { limit = { scope:profligate    ?= yes } add = 1 }

	min = 1
	#max = 5 # overflow possible with blood-storing artifacts
}

POD_offer_draught_interaction_cost = {
	if = { limit = { scope:POD_offer_draught_might     ?= yes } add = 1 }
	if = { limit = { scope:POD_offer_draught_elegance  ?= yes } add = 1 }
	if = { limit = { scope:POD_offer_draught_endurance ?= yes } add = 1 }

	min = 1
}


#############################################
##########   HUNTER INTERACTIONS   ##########
#############################################

POD_qiao_feng_heal_interaction_cost = {
	if = { limit = { scope:wound		?= yes } add = 2 }
	if = { limit = { scope:scarred		?= yes } add = 1 }
	if = { limit = { scope:disfigured	?= yes } add = 3 }
	if = { limit = { scope:blind		?= yes } add = 4 }
	if = { limit = { scope:one_eyed		?= yes } add = 3 }
	if = { limit = { scope:maimed		?= yes } add = 4 }
	if = { limit = { scope:one_legged	?= yes } add = 4 }
	if = { limit = { scope:eunuch_1		?= yes } add = 5 }
	if = { limit = { scope:disease		?= yes } add = 7 }
	if = { limit = { scope:superdisease	?= yes } add = 10 }

	min = 1
}

POD_qiao_meng_balance_of_the_spirit_interaction_cost = {
	if = { limit = { scope:balance_of_the_spirit	?= yes } add = 2 }
	if = { limit = { scope:mental_dominion			?= yes } add = 5 }
	# all other options cost 1

	min = 1
}



############################################
##########   DEMON INTERACTIONS   ##########
############################################

POD_healing_touch_interaction_cost = {
	if = { limit = { scope:wounded     ?= yes } add = 1 }
	if = { limit = { scope:ill         ?= yes } add = 1 }
	if = { limit = { scope:disease     ?= yes } add = 5 }
	if = { limit = { scope:scarred     ?= yes } add = 1 }
	if = { limit = { scope:disfigured  ?= yes } add = 2 }
	if = { limit = { scope:blind       ?= yes } add = 3 }
	if = { limit = { scope:maimed      ?= yes } add = 4 }
	if = { limit = { scope:one_legged  ?= yes } add = 4 }
	if = { limit = { scope:one_eyed    ?= yes } add = 2 }
	if = { limit = { scope:eunuch      ?= yes } add = 3 }
	if = { limit = { scope:scaly       ?= yes } add = 4 }
	if = { limit = { scope:clubfooted  ?= yes } add = 4 }
	if = { limit = { scope:infertile   ?= yes } add = 3 }
	if = { limit = { scope:wheezing    ?= yes } add = 4 }
	if = { limit = { scope:bleeder     ?= yes } add = 4 }
	if = { limit = { scope:weak        ?= yes } add = 7 }
	if = { limit = { scope:lisping     ?= yes } add = 5 }
	if = { limit = { scope:stuttering  ?= yes } add = 5 }
	if = { limit = { scope:hunchbacked ?= yes } add = 5 }
	if = { limit = { scope:impotent    ?= yes } add = 5 }
	if = { limit = { scope:infirm      ?= yes } add = 6 }
	if = { limit = { scope:incapable   ?= yes } add = 8 }
	if = { limit = { scope:inbred      ?= yes } add = 8 }

	min = 1
}


############################################
##########   MUMMY INTERACTIONS   ##########
############################################

POD_healing_bandages_interaction_cost = {
	if = { limit = { scope:scarred		?= yes } add = 1 }
	if = { limit = { scope:disfigured	?= yes } add = 2 }
	if = { limit = { scope:wounded		?= yes } add = 1 }
	if = { limit = { scope:very_wounded	?= yes } add = 1 }

	min = 1
}

POD_hekau_weather_magic_interaction_cost = {
	if = { limit = { scope:county		?= yes } add = 2 }
	if = { limit = { scope:duchy		?= yes } add = 4 }
	if = { limit = { scope:kingdom		?= yes } add = 6 }
	if = { limit = { scope:empire		?= yes } add = 8 }
	if = { limit = { scope:continent	?= yes } add = 10 }

	if = { 
		limit = { scope:flood ?= yes scope:actor ?= { has_perk = inundation_of_the_nile_perk } }
		max = 0
	}
	# no min cost to make the interaction tooltip properly
}

POD_hekau_plague_of_maat_interaction_cost = {
	if = { limit = { scope:consumption		?= yes } add = 4 }
	if = { limit = { scope:typhus			?= yes } add = 4 }
	if = { limit = { scope:smallpox			?= yes } add = 5 }
	if = { limit = { scope:bubonic_plague	?= yes } add = 8 }
	if = { limit = { scope:measles			?= yes } add = 4 }
	if = { limit = { scope:dysentery		?= yes } add = 4 }
	
	min = 4
}

POD_copy_artifact_interaction_cost = {
	if = {
		limit = { exists = scope:target }
		scope:target = {
			switch = {
				trigger = rarity
				common		= { add = 1 }
				masterwork	= { add = 2 }
				famed		= { add = 3 }
				illustrious	= { add = 4 }
			}
		}
	}
	min = 1
}


############################################
########   KUEI-JIN INTERACTIONS   #########
############################################

POD_shintai_arms_of_the_sea_interaction_cost = {
	if = { limit = { scope:duchy		?= yes } add = 4 }
	if = { limit = { scope:kingdom		?= yes } add = 6 }
	if = { limit = { scope:empire		?= yes } add = 8 }
	# no min cost to make the interaction tooltip properly
}


##########################################
##########   FAE INTERACTIONS   ##########
##########################################

# also reused for holly strike and healing waters
POD_heathe_balm_character_interaction_cost = {
	if = { limit = { scope:scarred		?= yes } add = 1 }
	if = { limit = { scope:disfigured	?= yes } add = 2 }
	if = { limit = { scope:wounded		?= yes } add = 1 }
	if = { limit = { scope:one_eyed		?= yes } add = 2 }
	if = { limit = { scope:blind		?= yes } add = 2 }
	if = { limit = { scope:one_legged	?= yes } add = 3 }
	if = { limit = { scope:maimed		?= yes } add = 3 }
	if = { limit = { scope:eunuch		?= yes } add = 3 }

	min = 1
}

POD_area_cantrip_interaction_glamour_cost = {
	if = {
		limit = { exists = scope:target }
		if = {
			limit = { scope:target = { tier = tier_hegemony } }
			add = 5
		}
		else_if = {
			limit = { scope:target = { tier = tier_empire } }
			add = 4
		}
		else_if = {
			limit = { scope:target = { tier = tier_kingdom } }
			add = 3
		}
		else_if = {
			limit = { scope:target = { tier = tier_duchy } }
			add = 2
		}
		else = {
			add = 1
		}
	}
	
	min = 1
}

POD_area_cantrip_interaction_piety_cost = {
	if = {
		limit = { exists = scope:target }
		if = {
			limit = { scope:target = { tier = tier_hegemony } }
			add = 2000
		}
		else_if = {
			limit = { scope:target = { tier = tier_empire } }
			add = 1000
		}
		else_if = {
			limit = { scope:target = { tier = tier_kingdom } }
			add = 500
		}
		else_if = {
			limit = { scope:target = { tier = tier_duchy } }
			add = 250
		}
		else = {
			add = 0
		}
	}
}

POD_eldritch_prime_interaction_realm_nature_level = {
	if = {
		limit = {
			OR = {
				scope:meteor_shower ?= yes
				scope:storm ?= yes
				scope:drought ?= yes
				scope:flood ?= yes
			}
		}
		add = 5
	}
	else = {
		add = 4
	}
}

POD_grandeur_interaction_cost = {
	if = { limit = { scope:imprison_option	?= yes } add = 3 }
	if = { limit = { scope:recruit_option	?= yes } add = 2 }

	min = 2
}