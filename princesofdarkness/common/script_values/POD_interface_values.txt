POD_debug_squared_distance = {
	if = {
		limit = { scope:recipient = { exists = capital_province } }
		value = "scope:actor.capital_province.squared_distance(scope:recipient.capital_province)"
	}
	else = {
		value = "scope:actor.capital_province.squared_distance(scope:recipient.location)"
	}
}

POD_hud_portrait_scale = {
	if = {
		limit = { has_game_rule = POD_hud_portrait_size_vanilla }
		value = 1.3
	}
	else_if = {
		limit = { has_game_rule = POD_hud_portrait_size_larger }
		value = 1.7
	}
	else = {
		value = 1.5
	}
}

POD_hud_portrait_x = {
	if = {
		limit = { has_game_rule = POD_hud_portrait_size_vanilla }
		value = -10
	}
	else_if = {
		limit = { has_game_rule = POD_hud_portrait_size_larger }
		value = 30
	}
	else = {
		value = 20
	}
}

POD_malkavian_shader_intensity = {
	value = 0

	if = {
		limit = { has_global_variable = POD_marauder_mode }
		add = 1
	}
	else_if = {
		limit = { is_malkavian_trigger = yes }

		add = 0.2

		add = {
			value = 0.3
			multiply = stress_level
		}
	}

	max = 1
}

##### Achievement values

POD_very_easy_achievements_unlocked = {
	value = 0
	
	if = { # True Blood
		limit = { has_global_variable = pod_achiev_vamptutorial }
		add = 1
	}
	
	if = { # Head in the Clouds
		limit = { has_global_variable = pod_achiev_kjtutorial }
		add = 1
	}
	
	if = { # Respect My Authority
		limit = { has_global_variable = pod_achiev_feratutorial }
		add = 1
	}
	
	if = { # DIY
		limit = { has_global_variable = pod_achiev_mummytutorial }
		add = 1
	}
	
	if = { # Master of Puppets
		limit = { has_global_variable = pod_achiev_demontutorial }
		add = 1
	}
	
	if = { # By Our Powers Combined...
		limit = { has_global_variable = pod_achiev_coterietutorial }
		add = 1
	}
	
	if = { # What's Yours Is Mine
		limit = { has_global_variable = pod_achiev_heisttutorial }
		add = 1
	}
}

POD_easy_achievements_unlocked = {
	value = 0
	
	if = { # A Killer and a Saint
		limit = { has_global_variable = pod_achiev_lucita }
		add = 1
	}
	
	if = { # Snake Charmer
		limit = { has_global_variable = pod_achiev_mithras }
		add = 1
	}
	
	if = { # A Fitting End for this Story
		limit = { has_global_variable = pod_achiev_helena1 }
		add = 1
	}
	
	if = { # A Fitting Start for this Fanfiction
		limit = { has_global_variable = pod_achiev_helena2 }
		add = 1
	}
	
	if = { # All Roads Lead To Me
		limit = { has_global_variable = pod_achiev_camilla }
		add = 1
	}
	
	if = { # The Blood of Women
		limit = { has_global_variable = pod_achiev_piyau }
		add = 1
	}
}

POD_medium_achievements_unlocked = {
	value = 0
	
	if = { # Legendary Hero
		limit = { has_global_variable = pod_achiev_4thgenhunt }
		add = 1
	}
	
	if = { # Stone Cold Stunner
		limit = { has_global_variable = pod_achiev_visceratika }
		add = 1
	}
	
	if = { # Wait, Wrong Demon!
		limit = { has_global_variable = pod_achiev_rusenko }
		add = 1
	}
	
	if = { # No Canon Ending for Me
		limit = { has_global_variable = pod_achiev_schrekt }
		add = 1
	}
	
	if = { # The Truth about Saulot?
		limit = { has_global_variable = pod_achiev_javaniel }
		add = 1
	}
	
	if = { # My Precious
		limit = { has_global_variable = pod_achiev_myprecious }
		add = 1
	}
	
	if = { # Ambition Erudition
		limit = { has_global_variable = pod_achiev_aristotle }
		add = 1
	}
	
	if = { # Am I Early?
		limit = { has_global_variable = pod_achiev_macao }
		add = 1
	}
	
	if = { # Dharmic Explorer
		limit = { has_global_variable = pod_achiev_unprovendharma }
		add = 1
	}
}

POD_hard_achievements_unlocked = {
	value = 0
	
	# if = { # The Latest Crusade (disabled for now)
	# 	limit = { has_global_variable = pod_achiev_grail }
	# 	add = 1
	# }
	
	if = { # Somehow, he has returned
		limit = { has_global_variable = pod_achiev_alexander }
		add = 1
	}
	
	if = { # Ate My Way Up The Ladder
		limit = { has_global_variable = pod_achiev_nomnom }
		add = 1
	}
	
	if = { # I Dream Of The First Times
		limit = { has_global_variable = pod_achiev_noddist }
		add = 1
	}
	
	if = { # Stairway to Heaven
		limit = { has_global_variable = pod_achiev_elohim }
		add = 1
	}
	
	if = { # Nah, I'd Win
		limit = { has_global_variable = pod_achiev_lamb }
		add = 1
	}
	
	if = { # Make Peace, not War
		limit = { has_global_variable = pod_achiev_qawiyya }
		add = 1
	}
	
	if = { # Gods of Egypt II
		limit = { has_global_variable = pod_achiev_horus }
		add = 1
	}
	
	if = { # The True Lion King
		limit = { has_global_variable = pod_achiev_simba }
		add = 1
	}
	
	if = { # Snag in the Wheel
		limit = { has_global_variable = pod_achiev_wheelofages }
		add = 1
	}
	
	if = { # Racing the Clock
		limit = { has_global_variable = pod_achiev_anda }
		add = 1
	}
}

POD_very_hard_achievements_unlocked = {
	value = 0
	
	if = { # Isis' Revenge
		limit = { has_global_variable = pod_achiev_kemintiri }
		add = 1
	}
	
	if = { # Very Alternate History
		limit = { has_global_variable = pod_achiev_baybars }
		add = 1
	}
	
	if = { # Clausewitz Engine
		limit = { has_global_variable = pod_achiev_santa }
		add = 1
	}
	
	if = { # Revenge of the Doll
		limit = { has_global_variable = pod_achiev_yukio }
		add = 1
	}
}

POD_very_easy_achievements_total	= 7
POD_easy_achievements_total			= 6
POD_medium_achievements_total		= 9
POD_hard_achievements_total			= 10
POD_very_hard_achievements_total	= 4

POD_total_achievements = {
	add = POD_very_easy_achievements_total
	add = POD_easy_achievements_total
	add = POD_medium_achievements_total
	add = POD_hard_achievements_total
	add = POD_very_hard_achievements_total
}

POD_total_achievements_unlocked = {
	add = POD_very_easy_achievements_unlocked
	add = POD_easy_achievements_unlocked
	add = POD_medium_achievements_unlocked
	add = POD_hard_achievements_unlocked
	add = POD_very_hard_achievements_unlocked
}

POD_diplomacy_cap = {
	value = define:NSkills|MAX_DIPLOMACY
}
POD_martial_cap = {
	value = define:NSkills|MAX_MARTIAL
}
POD_stewardship_cap = {
	value = define:NSkills|MAX_STEWARDSHIP
}
POD_intrigue_cap = {
	value = define:NSkills|MAX_INTRIGUE
}
POD_learning_cap = {
	value = define:NSkills|MAX_LEARNING
}
POD_prowess_cap = {
	value = define:NSkills|MAX_PROWESS
}