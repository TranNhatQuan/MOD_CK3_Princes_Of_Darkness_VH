POD_blood_hunt_war = {
	group = invasion
	icon = excommunication
	interface_priority = 50
	
	war_name = "POD_BLOOD_HUNT_WAR_WAR_NAME"
	war_name_base = "POD_BLOOD_HUNT_WAR_WAR_NAME"
	cb_name = "POD_BLOOD_HUNT_WAR_CB_NAME"

	combine_into_one = yes
	should_show_war_goal_subview = yes
	
	target_titles = none
	ai_only_against_neighbors = yes
	
	attacker_score_from_occupation_scale = 50
	defender_score_from_occupation_scale = 50
	attacker_score_from_battles_scale = 200
	defender_score_from_battles_scale = 200
	
	on_primary_attacker_death = invalidate
	on_primary_defender_death = invalidate
	attacker_allies_inherit = yes
	defender_allies_inherit = yes

	allow_hostages = no
	allowed_for_character = {
		scope:attacker = { religion = religion:camarilla_religion }
	}
	
	allowed_for_character_display_regardless = {
		scope:attacker = { POD_is_anathema_trigger = no }
	}
	
	allowed_against_character = {
		scope:defender.camarilla_status < 7
	}
	
	allowed_against_character_display_regardless = {
		scope:defender = { is_imprisoned = no }
		scope:attacker = { scope:defender = { POD_is_anathema_trigger = yes } }
	}
	
	cost = {
		prestige = {
			add = {
				value = 100
				desc = CB_BASE_COST
			}
			multiply = common_cb_piety_cost_multiplier
		}
	}

	valid_to_start = {
		always = yes
	}

	should_invalidate = {
		scope:defender = { POD_is_anathema_trigger = no }
	}

	on_invalidated_desc = {
		first_valid = {
			triggered_desc = {
				trigger = { scope:defender = { is_alive = no } }
				desc = msg_POD_blood_hunt_war_invalidated_hunted_unavailable_dead_message
			}
			triggered_desc = {
				trigger = { scope:defender = { POD_is_anathema_trigger = no } }
				desc = msg_POD_blood_hunt_war_invalidated_hunted_no_anathema_message
			}
			desc = msg_invalidate_war_title
		}
	}
	
	on_invalidated = {
		scope:attacker = {
			#Used for the piety_change scripted effect.
			save_scope_as = winner
		}

		#Handle all piety gains/losses in scripted effects.
		excommunication_cb_piety_change = {
			WINNER = scope:attacker
			LOSER = scope:defender
		}

		scope:defender = {
			#Used for the piety_change scripted effect.
			save_scope_as = loser
		}
	}
	
	on_declaration = {
		on_declared_war = yes
		scope:attacker = { trigger_event = POD_camarilla.110 }
	}	

	on_victory_desc = {
		first_valid = {
			triggered_desc = {
				trigger = { scope:defender = { is_local_player = yes } }
				desc = POD_blood_hunt_war_victory_desc_defender
			}
			desc = POD_blood_hunt_war_victory_desc
		}
	}
	
	on_victory = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain from winning against higher ranked enemy
		scope:attacker = { accolade_attacker_war_end_glory_gain_med_effect = yes }
		
		excommunication_cb_piety_change = {
			WINNER = scope:attacker
			LOSER = scope:defender
		}
		
		scope:defender = {
			if = { # Imprison them if they aren't imprisoned.
				limit = { is_imprisoned = no }
				add_character_flag = { flag = lost_duel days = 3 }
				hard_imprison_character_effect = {
					TARGET = this
					IMPRISONER = scope:attacker
				}
			}
			
			every_vassal_or_below = {
				limit = { is_imprisoned = no }
				add_character_flag = { flag = lost_duel days = 3 }
				hard_imprison_character_effect = {
					TARGET = this
					IMPRISONER = scope:attacker
				}
			}
		}

		# Truce
		add_truce_attacker_victory_effect = yes
	}

	on_white_peace_desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					scope:defender = { is_local_player = yes }
				}
				desc = POD_blood_hunt_war_defeat_desc_defender
			}
			desc = POD_blood_hunt_war_defeat_desc
		}
	}

	on_white_peace = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain for defender for being ok
		scope:defender = { accolade_defender_war_end_glory_gain_low_effect = yes }
		
		scope:attacker = {
			add_piety = excommunication_cb_piety_white_peace
			stress_impact = {
				ambitious = medium_stress_impact_gain
				arrogant = medium_stress_impact_gain
			}
		}

		scope:defender = {
			stress_impact = {
				arrogant = medium_stress_impact_gain
			}
		}

		# Truce
		add_truce_white_peace_effect = yes
	}

	on_defeat_desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					scope:attacker = { is_local_player = yes }
				}
				desc = POD_blood_hunt_war_defeat_desc_attacker
			}
			triggered_desc = {
				trigger = {
					scope:defender = { is_local_player = yes }
				}
				desc = POD_blood_hunt_war_defeat_desc_defender
			}
			desc = POD_blood_hunt_war_defeat_desc
		}
	}
	
	on_defeat = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain for winning against higher ranked enemy
		scope:defender = { accolade_defender_war_end_glory_gain_med_effect = yes }
		
		#Handle all piety gains/losses in scripted effects.
		excommunication_cb_piety_change = {
			LOSER = scope:attacker
			WINNER = scope:defender
		}

		# Truce
		add_truce_attacker_defeat_effect = yes
	}
	
	ai_score = {
		value = raid_for_captives_ai_score_value
	}
		
	ai_score_mult = {
		value = 1
	}
}

POD_wild_hunt_war = {
	group = invasion
	icon = excommunication
	interface_priority = 50
	
	war_name = "POD_WILD_HUNT_WAR_WAR_NAME"
	war_name_base = "POD_WILD_HUNT_WAR_WAR_NAME"
	cb_name = "POD_WILD_HUNT_WAR_CB_NAME"

	combine_into_one = yes
	should_show_war_goal_subview = yes
	
	target_titles = none
	ai_only_against_neighbors = yes
	
	attacker_score_from_occupation_scale = 50
	defender_score_from_occupation_scale = 50
	attacker_score_from_battles_scale = 200
	defender_score_from_battles_scale = 200
	
	on_primary_attacker_death = invalidate
	on_primary_defender_death = invalidate
	attacker_allies_inherit = yes
	defender_allies_inherit = yes

	allow_hostages = no
	allowed_for_character = {
		scope:attacker.sabbat_status >= 7
	}
	
	allowed_for_character_display_regardless = {
	}
	
	allowed_against_character = {
		scope:defender.sabbat_status < 0
	}
	
	allowed_against_character_display_regardless = {
		scope:defender = { is_imprisoned = no }
		scope:attacker = { 
			trigger_if = { 
				limit = { scope:defender = { has_trait = excommunicated } }
				scope:defender = { NOT = { religion = religion:sabbat_religion } }
			}
		}
	}
	
	cost = {
		prestige = {
			add = {
				value = 100
				desc = CB_BASE_COST
			}
			multiply = common_cb_piety_cost_multiplier
		}
	}

	valid_to_start = {
		always = yes
	}

	should_invalidate = {
		scope:defender = { 
			AND = { 
				religion = religion:sabbat_religion 
				NOT = { has_trait = excommunicated }
			}
		}
	}

	on_invalidated_desc = {
		first_valid = {
			triggered_desc = {
				trigger = { scope:defender = { is_alive = no } }
				desc = msg_POD_wild_hunt_war_invalidated_hunted_unavailable_dead_message
			}
			triggered_desc = {
				trigger = { scope:defender = { NOT = { has_trait = excommunicated } } }
				desc = msg_POD_wild_hunt_war_invalidated_hunted_true_sabbat_message
			}
			desc = msg_invalidate_war_title
		}
	}
	
	on_invalidated = {
		scope:attacker = {
			#Used for the piety_change scripted effect.
			save_scope_as = winner
		}

		#Handle all piety gains/losses in scripted effects.
		excommunication_cb_piety_change = {
			WINNER = scope:attacker
			LOSER = scope:defender
		}

		scope:defender = {
			#Used for the piety_change scripted effect.
			save_scope_as = loser
		}
	}
	
	on_declaration = {
		on_declared_war = yes
		scope:attacker = { trigger_event = POD_sabbat.110 }
	}

	on_victory_desc = {
		first_valid = {
			triggered_desc = {
				trigger = { scope:defender = { is_local_player = yes } }
				desc = POD_wild_hunt_war_victory_desc_defender
			}
			desc = POD_wild_hunt_war_victory_desc
		}
	}
	
	on_victory = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain from winning against higher ranked enemy
		scope:attacker = { accolade_attacker_war_end_glory_gain_med_effect = yes }
		
		excommunication_cb_piety_change = {
			WINNER = scope:attacker
			LOSER = scope:defender
		}
		
		create_title_and_vassal_change = {
			type = conquest
			save_scope_as = change
			add_claim_on_loss = yes
		}
		scope:defender = {
			every_held_title = {
				limit = { NOT = { tier = tier_barony } }
				change_title_holder = {
					holder = scope:attacker
					change = scope:change
					take_baronies = yes
				}
			}
		}
		resolve_title_and_vassal_change = scope:change
		
		scope:defender = { 
			pod_death = {
				death_reason = death_execution
				killer = scope:attacker
			}
		}

		# FP1: note the victory for future memorialisation via stele (if applicable).
		scope:attacker = { fp1_remember_recent_conquest_victory_effect = yes }
	}

	on_white_peace_desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					scope:defender = { is_local_player = yes }
				}
				desc = POD_wild_hunt_war_defeat_desc_defender
			}
			desc = POD_wild_hunt_war_defeat_desc
		}
	}

	on_white_peace = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain for defender for being ok
		scope:defender = { accolade_defender_war_end_glory_gain_low_effect = yes }
		
		scope:attacker = {
			add_piety = excommunication_cb_piety_white_peace
			stress_impact = {
				ambitious = medium_stress_impact_gain
				arrogant = medium_stress_impact_gain
			}
		}

		scope:defender = {
			stress_impact = {
				arrogant = medium_stress_impact_gain
			}
		}

		# Truce
		add_truce_white_peace_effect = yes
	}

	on_defeat_desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					scope:attacker = { is_local_player = yes }
				}
				desc = POD_wild_hunt_war_defeat_desc_attacker
			}
			triggered_desc = {
				trigger = {
					scope:defender = { is_local_player = yes }
				}
				desc = POD_wild_hunt_war_defeat_desc_defender
			}
			desc = POD_wild_hunt_war_defeat_desc
		}
	}
	
	on_defeat = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain for winning against higher ranked enemy
		scope:defender = { accolade_defender_war_end_glory_gain_med_effect = yes }
		
		#Handle all piety gains/losses in scripted effects.
		excommunication_cb_piety_change = {
			LOSER = scope:attacker
			WINNER = scope:defender
		}

		# Truce
		add_truce_attacker_defeat_effect = yes
	}
	
	ai_score = {
		value = raid_for_captives_ai_score_value
	}
		
	ai_score_mult = {
		value = 1
	}
}

roadofmetamorphosis_war_cb = {
	icon = ducal_conquest_cb
	group = religious_disorganised
	ai_only_against_neighbors = yes
	allow_hostages = no

	# Root is the title
	# scope:attacker is the attacker
	# scope:defender is the defender
	allowed_for_character_display_regardless = {
		custom_tooltip = {
			text = roadofmetamorphosis_war_cooldown
			NOT = {	scope:attacker = { has_character_flag = roadofmetamorphosis_war_cooldown } }
		}
	}
	
	allowed_against_character = {
		scope:attacker = {
			faith = { has_doctrine_parameter = roadofmetamorphosis_war_cb_active }
		}

		scope:defender = {
			is_independent_ruler = yes
			trigger_if = {
				limit = { scope:attacker = { is_ai = no } } # Already pre-filtered by the AI due to ai_only_against_neighbors = yes. any_neighboring_top_liege_realm_owner is expensive
				scope:attacker = {
					any_neighboring_top_liege_realm_owner = {
						this = scope:defender
					}
				}
			}
		}
	}

	cost = {
		piety = {
			add = {
				value = 100
				desc = CB_BASE_COST
			}
			multiply = common_cb_piety_cost_multiplier
	
			#Dynasty Salubri Perk
			if = {
				limit = {
					scope:attacker = {
						has_dynasty = yes
						dynasty = {
							has_dynasty_perk = salubri_legacy_5
						}
					}
				}
				multiply = {
					value = salubri_legacy_5_discount
					desc = CB_ATTACKER_DYNASTY_SALUBRI
				}
			}
			
			#Dynasty Banu Haqim Perk
			if = {
				limit = {
					scope:attacker = {
						has_dynasty = yes
						dynasty = {
							has_dynasty_perk = banuhaqim_legacy_5
						}
					}
				}
				multiply = {
					value = banuhaqim_legacy_5_discount
					desc = CB_ATTACKER_DYNASTY_BANUHAQIM
				}
			}
		}	
	}

	should_invalidate = {
		OR = {
			AND = {	#A formal flowery war only invalidates if neither side is an interested participant.
				scope:attacker.faith = {
					NOT = { has_doctrine_parameter = roadofmetamorphosis_war_cb_active }
				}
				scope:defender.faith = {
					NOT = { has_doctrine_parameter = roadofmetamorphosis_war_cb_active }
				}
			}
		}
	}

	on_invalidated_desc = {
		desc = flowery_war_cb_ended_invalid.desc_no_sacrificers
	}

	on_invalidated = {
		scope:attacker = {
			remove_character_flag = roadofmetamorphosis_war_cooldown # Reset cooldown as well
		}
	}

	on_declaration = {
		on_declared_war = yes
		scope:attacker = {
			add_character_flag = {
				flag = roadofmetamorphosis_war_cooldown
				years = 5
			}
		}
	}

	on_victory_desc = {
		desc = roadofmetamorphosis_war_cb_victory_desc
	}

	on_victory = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain from winning against higher ranked enemy
		scope:attacker = { accolade_attacker_war_end_glory_gain_low_effect = yes }

		scope:attacker = {	#If the attacker has the appropriate tenet, grab some suitable courtiers from the defender.
			custom_tooltip = roadofmetamorphosis_war_cb_prisoners.tt_attacker
			hidden_effect = {
				scope:defender = {
					every_courtier_or_guest = { add_to_list = potential_captives_list }
					random_in_list = {	#Nab someone related to the defender, preferring tangentials.
						list = potential_captives_list
						limit = {
							OR = {
								AND = {
									has_dynasty = yes
									dynasty = scope:defender.dynasty
								}
								is_consort_of = scope:defender
							}
						}
						alternative_limit = { always = yes }
						weight = {
							base = 1
							modifier = {
								add = 25
								NOT = { is_close_family_of = scope:defender }
							}
							modifier = {
								add = 15
								NOT = { is_extended_family_of = scope:defender }
							}
						}
						add_to_list = definite_captives_list
						save_temporary_scope_as = future_captive
							scope:attacker = {
								imprison = {
									target = scope:future_captive
									type = dungeon
								}
							}
					}
					random_in_list = {	#Always try to grab a second person.
						list = potential_captives_list
						limit = {
							NOT = { is_in_list = definite_captives_list }
						}
						weight = {
							base = 1
							modifier = {
								add = 40
								NOT = {
									any_claim = {
										useful_courtier_or_guest_claim_trigger = { RULER = scope:defender }
									}
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_chancellor_trigger = { COURT_OWNER = scope:defender }
									diplomacy >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_marshal_trigger = { COURT_OWNER = scope:defender }
									martial >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_steward_trigger = { COURT_OWNER = scope:defender }
									stewardship >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_spymaster_trigger = { COURT_OWNER = scope:defender }
									intrigue >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NOT = { learning >= high_skill_rating }
							}
							modifier = {
								add = 10
								NOT = {	prowess >= high_skill_rating }
							}
							modifier = {
								add = 50
								NOT = { is_close_or_extended_family_of = scope:defender }
							}
						}
						add_to_list = definite_captives_list
						save_temporary_scope_as = future_captive
							scope:attacker = {
								imprison = {
									target = scope:future_captive
									type = dungeon
								}
							}
					}
					random_in_list = {	#Always try to grab a third person.
						list = potential_captives_list
						limit = {
							NOT = { is_in_list = definite_captives_list }
						}
						weight = {
							base = 1
							modifier = {
								add = 40
								NOT = {
									any_claim = {
										useful_courtier_or_guest_claim_trigger = { RULER = scope:defender }
									}
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_chancellor_trigger = { COURT_OWNER = scope:defender }
									diplomacy >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_marshal_trigger = { COURT_OWNER = scope:defender }
									martial >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_steward_trigger = { COURT_OWNER = scope:defender }
									stewardship >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NAND = {
									can_be_spymaster_trigger = { COURT_OWNER = scope:defender }
									intrigue >= high_skill_rating
								}
							}
							modifier = {
								add = 10
								NOT = { learning >= high_skill_rating }
							}
							modifier = {
								add = 10
								NOT = {	prowess >= high_skill_rating }
							}
							modifier = {
								add = 50
								NOT = { is_close_or_extended_family_of = scope:defender }
							}
						}
						add_to_list = definite_captives_list
						save_temporary_scope_as = future_captive
							scope:attacker = {
								imprison = {
									target = scope:future_captive
									type = dungeon
								}
							}
					}
					if = {	#If at least duchy-tier, try to grab a fourth person.
						limit = { primary_title.tier >= tier_duchy }
						random_in_list = {
							list = potential_captives_list
							limit = {
								NOT = { is_in_list = definite_captives_list }
							}
							weight = {
								base = 1
								modifier = {
									add = 40
									NOT = {
										any_claim = {
											useful_courtier_or_guest_claim_trigger = { RULER = scope:defender }
										}
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_chancellor_trigger = { COURT_OWNER = scope:defender }
										diplomacy >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_marshal_trigger = { COURT_OWNER = scope:defender }
										martial >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_steward_trigger = { COURT_OWNER = scope:defender }
										stewardship >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_spymaster_trigger = { COURT_OWNER = scope:defender }
										intrigue >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NOT = { learning >= high_skill_rating }
								}
								modifier = {
									add = 10
									NOT = { prowess >= high_skill_rating }
								}
							}
							add_to_list = definite_captives_list
							save_temporary_scope_as = future_captive
							scope:attacker = {
								imprison = {
									target = scope:future_captive
									type = dungeon
								}
							}
						}
					}
					if = {	#If at least kingdom-tier, try to grab a fifth person.
						limit = { primary_title.tier >= tier_kingdom }
						random_in_list = {
							list = potential_captives_list
							limit = {
								NOT = { is_in_list = definite_captives_list }
							}
							weight = {
								base = 1
								modifier = {
									add = 40
									NOT = {
										any_claim = {
											useful_courtier_or_guest_claim_trigger = { RULER = scope:defender }
										}
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_chancellor_trigger = { COURT_OWNER = scope:defender }
										diplomacy >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_marshal_trigger = { COURT_OWNER = scope:defender }
										martial >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_steward_trigger = { COURT_OWNER = scope:defender }
										stewardship >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NAND = {
										can_be_spymaster_trigger = { COURT_OWNER = scope:defender }
										intrigue >= high_skill_rating
									}
								}
								modifier = {
									add = 10
									NOT = { learning >= high_skill_rating }
								}
								modifier = {
									add = 10
									NOT = { prowess >= high_skill_rating }
								}
							}
							add_to_list = definite_captives_list
							save_temporary_scope_as = future_captive
							scope:attacker = {
								imprison = {
									target = scope:future_captive
									type = dungeon
								}
							}
						}
					}
				}
			}
		}

		# Allies on both sides get full prestige value for helping in the war, based on their war participation.
		modify_allies_of_participants_fame_values = {
			WINNER = scope:attacker
			FAME_BASE = major_prestige_value
			IS_RELIGIOUS_WAR = no
			WINNER_ALLY_FAME_SCALE = 1
			LOSER_ALLY_FAME_SCALE = 1
		}

		# Truce
		add_truce_attacker_victory_effect = yes

	}

	on_white_peace_desc = {
		desc = roadofmetamorphosis_war_cb_white_peace_desc
	}

	on_white_peace = {
		scope:attacker = { 
			show_pow_release_message_effect = yes
			stress_impact = {
				ambitious = medium_stress_impact_gain
				arrogant = medium_stress_impact_gain
			}
		}
		scope:attacker = {
			add_piety = religious_cb_piety_white_peace
		}

		# Allies on both sides get full prestige value for helping in the war, based on their war participation.
		modify_allies_of_participants_fame_values = {
			WINNER = scope:attacker # not important as the scales are identical
			FAME_BASE = major_prestige_value
			IS_RELIGIOUS_WAR = no
			WINNER_ALLY_FAME_SCALE = 1
			LOSER_ALLY_FAME_SCALE = 1
		}

		scope:defender = {
			stress_impact = {
				arrogant = medium_stress_impact_gain
			}
		}

		# Truce
		add_truce_attacker_victory_effect = yes

	}

	on_defeat_desc = {
		desc = roadofmetamorphosis_war_cb_defeat_desc
	}

	on_defeat = {
		scope:attacker = { show_pow_release_message_effect = yes }

		#EP2 Accolade glory gain from winning against higher ranked enemy
		scope:defender = { accolade_defender_war_end_glory_gain_low_effect = yes }

		scope:attacker = {
			if = {
				limit = { scope:attacker.primary_title.tier = tier_county }
				add_prestige = { value = minor_prestige_loss }
			}
			if = {
				limit = { scope:attacker.primary_title.tier = tier_duchy }
				add_prestige = { value = medium_prestige_loss }
			}
			if = {
				limit = { scope:attacker.primary_title.tier = tier_kingdom }
				add_prestige = { value = major_prestige_loss }
			}
			if = {
				limit = { scope:attacker.primary_title.tier >= tier_empire }
				add_prestige = { value = massive_prestige_loss }
			}
		}

		# Allies on both sides get full prestige value for helping in the war, based on their war participation.
		modify_allies_of_participants_fame_values = {
			WINNER = scope:defender
			FAME_BASE = major_prestige_value
			IS_RELIGIOUS_WAR = no
			WINNER_ALLY_FAME_SCALE = 1
			LOSER_ALLY_FAME_SCALE = 1
		}

		# Truce
		add_truce_attacker_defeat_effect = yes

		scope:attacker = { save_temporary_scope_as = loser }
		on_lost_aggression_war_discontent_loss = yes
	}

	on_primary_attacker_death = inherit
	on_primary_defender_death = inherit

	attacker_allies_inherit = yes
	defender_allies_inherit = yes

	war_name = "ROADOFMETAMORPHOSIS_WAR_CB_NAME"
	war_name_base = "ROADOFMETAMORPHOSIS_WAR_WAR_NAME_BASE"
	cb_name = "ROADOFMETAMORPHOSIS_WAR_CB_NAME"
	interface_priority = 80

	ticking_war_score_targets_entire_realm = yes
	attacker_ticking_warscore = 0
	attacker_wargoal_percentage = 0.01

	attacker_score_from_occupation_scale = 150
	defender_score_from_occupation_scale = 150

	attacker_score_from_battles_scale = 150
	defender_score_from_battles_scale = 150

	max_attacker_score_from_battles = 200
	max_defender_score_from_battles = 200
	
	max_defender_score_from_occupation = 150
	max_attacker_score_from_occupation = 150

	max_ai_diplo_distance_to_title = 500
	
	ai_score = {
		value = raid_for_captives_ai_score_value
	}

	ai_score_mult = {
		value = 1
		# Increase odds if the character is a vassal with Ghazi Contract
		add = {
			if = {
				limit = {
					scope:attacker = {
						is_independent_ruler = no
						vassal_contract_has_flag = vassal_contract_cheaper_religious_war
					}
				}
				add = 1
			}
		}
		# Holy War are limited to connected neighbour to limit big empire expansions over weak realms.
		add = {
			if = {
				limit = {  religious_war_vassals_constraints = yes }
				add = -1000
			}
		}
	}
}