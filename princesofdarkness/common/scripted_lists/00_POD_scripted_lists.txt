landed_ruler = {
	base = ruler
	conditions = { is_landed = yes }
}

august_ruler = {
	base = ruler
	conditions = { has_government = august_court_government }
}

patron_council_ruler = {
	base = ruler
	conditions = { has_trait = patron_council }
}