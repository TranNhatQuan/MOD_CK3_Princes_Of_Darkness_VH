POD_character_vampire_lifestyle_initialization_effect = {

	# Vanilla
	
    if = {
        limit = {
            has_trait = diplomat
        }
        assign_foreign_affairs_effect = yes
    }
	
	if = {
        limit = {
            has_trait = august
        }
        assign_majesty_effect = yes
    }
	
	if = {
        limit = {
            has_trait = family_first
        }
        assign_family_effect = yes
    }	
	
	if = {
        limit = {
            has_trait = schemer
        }
        assign_skulduggery_effect = yes
    }		

	if = {
        limit = {
            has_trait = lifestyle_wayfarer
        }
        assign_wayfarer_effect = yes
    }		

	if = {
        limit = {
            has_trait = lifestyle_voyager
        }
        assign_voyager_effect = yes
    }	

	if = {
        limit = {
            has_trait = lifestyle_surveyor
        }
        assign_surveyor_effect = yes
    }	

	if = {
        limit = {
            has_trait = seducer
        }
        assign_temptation_effect = yes
    }		
	if = {
        limit = {
            has_trait = torturer
        }
        assign_intimidation_effect = yes
    }		

	if = {
        limit = {
            has_trait = scholar
        }
        assign_scholarship_effect = yes
    }		

	if = {
        limit = {
            has_trait = theologian
        }
        assign_theology_effect = yes
    }		

	if = {
        limit = {
            has_trait = strategist
        }
        assign_strategy_effect = yes
    }		

	if = {
        limit = {
            has_trait = overseer
        }
        assign_authority_effect = yes
    }				

	if = {
        limit = {
            has_trait = gallant
        }
        assign_chivalry_effect = yes
    }		

	if = {
        limit = {
            has_trait = avaricious
        }
        assign_wealth_effect = yes
    }		

	if = {
        limit = {
            has_trait = architect
        }
        assign_domain_effect = yes
    }		

	if = {
        limit = {
            has_trait = administrator
        }
        assign_duty_effect = yes
    }			
	
	# Vamp/Ghoul
	
    if = {
        limit = {
            has_trait = abombweadvanced
        }
        assign_abombwe_effect = yes
    }           

	if = {
        limit = {
            has_trait = animalismadvanced
        }
        assign_animalism_effect = yes
    }			

	if = {
        limit = {
            has_trait = proteanadvanced
        }
        assign_protean_effect = yes
    }			

	if = {
        limit = {
            has_trait = celerityadvanced
        }
        assign_celerity_effect = yes
    }			

	if = {
        limit = {
            has_trait = potenceadvanced
        }
        assign_potence_effect = yes
    }			

	if = {
        limit = {
            has_trait = fortitudeadvanced
        }
        assign_fortitude_effect = yes
    }			

	if = {
        limit = {
            has_trait = vicissitudeadvanced
        }
        assign_vicissitude_effect = yes
    }			

	if = {
        limit = {
            has_trait = auspexadvanced
        }
        assign_auspex_effect = yes
    }			

	if = {
        limit = {
            has_trait = obfuscateadvanced
        }
        assign_obfuscate_effect = yes
    }			

	if = {
        limit = {
            has_trait = dominateadvanced
        }
        assign_dominate_effect = yes
    }	
	
	if = {
        limit = {
            has_trait = daimonionadvanced
        }
        assign_daimonion_effect = yes
    }	

	if = {
        limit = {
            has_trait = bloodsorceryadvanced
        }
        assign_bloodsorcery_effect = yes
    }			

	if = {
        limit = {
            has_trait = presenceadvanced
        }
        assign_presence_effect = yes
    }	
    if = {
        limit = {
            has_trait = kaiadvanced
        }
        assign_kai_effect = yes
    }
    if = {
        limit = {
            has_trait = riftadvanced
        }
        assign_rift_effect = yes
    }
	if = {
	    limit = {
		    has_trait = obtenebrationadvanced
		}
		assign_obtenebration_effect = yes
	}
	if = {
	    limit = {
		    has_trait = serpentisadvanced
		}
		assign_serpentis_effect = yes
	}
	if = {
        limit = {
            has_trait = necromancyadvanced
        }
        assign_melancholy_necromancy_effect = yes
    }		
	if = {
        limit = {
            has_trait = visceratikaadvanced
        }
        assign_visceratika_effect = yes
    }		
	if = {
        limit = {
            has_trait = temporisadvanced
        }
        assign_temporis_effect = yes
    }	
	if = {
        limit = {
            has_trait = dementationadvanced
        }
        assign_dementation_effect = yes
    }
	if = {
        limit = {
            has_trait = melpomineeadvanced
        }
        assign_melpominee_effect = yes
    }
	if = {
        limit = {
            has_trait = spiritusadvanced
        }
        assign_animal_spiritus_effect = yes
    }
    if = {
        limit = {
            has_trait = flightadvanced
        }
        assign_animal_flight_effect = yes
    }
	if = {
        limit = {
            has_trait = valerenadvanced
        }
        assign_valeren_effect = yes
    }
	if = {
        limit = {
            has_trait = quietusadvanced
        }
        assign_quietus_effect = yes
    }
	if = {
        limit = {
            has_trait = mytherceriaadvanced
        }
        assign_mytherceria_effect = yes
    }
	if = {
        limit = {
            has_trait = chimerstryadvanced
        }
        assign_chimerstry_effect = yes
    }
	if = {
        limit = {
            has_trait = thinbloodalchemyadvanced
        }
        assign_thinbloodalchemy_effect = yes
    }
	if = {
        limit = {
            has_trait = bardoadvanced
        }
        assign_bardo_effect = yes
    }
	
	# Mage
	
	# if = {
        # limit = {
            # has_trait = mind_master
        # }
        # assign_mind_effect = yes
    # }
	# if = {
        # limit = {
            # has_trait = entropy_master
        # }
        # assign_entropy_effect = yes
    # }
	# if = {
        # limit = {
            # has_trait = life_master
        # }
        # assign_life_effect = yes
    # }
	# if = {
        # limit = {
            # has_trait = forces_master
        # }
        # assign_forces_effect = yes
    # }
	# if = {
        # limit = {
            # has_trait = matter_master
        # }
        # assign_matter_effect = yes
    # }
	# if = {
        # limit = {
            # has_trait = prime_master
        # }
        # assign_prime_effect = yes
    # }
	# if = {
        # limit = {
            # has_trait = time_master
        # }
        # assign_time_effect = yes
    # }
	# if = {
        # limit = {
            # has_trait = correspondence_master
        # }
        # assign_correspondence_effect = yes
    # }
	
	# Hunter

	if = {
        limit = {
            has_trait = whole_of_body
        }
        assign_medicine_effect = yes
    }

	if = {
        limit = {
            has_trait = beatific
        }
        assign_truefaith_effect = yes
    }	
	
	if = {
        limit = {
            has_trait = ha_patris_mastery
        }
        assign_patris_effect = yes
    }
	
	if = {
        limit = {
            has_trait = ha_filius_mastery
        }
        assign_filius_effect = yes
    }
	
	if = {
        limit = {
            has_trait = ha_spiritus_mastery
        }
        assign_spiritus_effect = yes
    }
	
	if = {
        limit = {
            has_trait = red_forge
			dynasty = dynasty:13000001
        }
        assign_ritae_effect = yes
    }
	
	if = {
        limit = {
            has_trait = acre_auto
			OR = {
				dynasty = dynasty:13000002
				dynasty = dynasty:13000006
				dynasty = dynasty:13000008
				}
        }
        assign_investitures_effect = yes
    }
	
	if = {
        limit = {
            has_trait = sisters_oratorio
			dynasty = dynasty:13000003
        }
        assign_psalms_effect = yes
    }	
	
	if = {
        limit = {
            has_trait = murnau_arcana
			dynasty = dynasty:13000004
        }
        assign_legacies_effect = yes
    }
	
	if = {
        limit = {
            has_trait = oculi_wine
			dynasty = dynasty:13000005
        }
        assign_blessings_effect = yes
    }
	
	# Fae
	POD_character_fae_lifestyle_initialization_effect = yes
	
	add_character_flag =  had_POD_character_lifestyle
}

POD_character_fae_lifestyle_initialization_effect = {
	if = {
        limit = {
            has_trait = realm_actor
        }
        assign_realm_actor_effect = yes
    }
	if = {
        limit = {
            has_trait = realm_fae
        }
        assign_realm_fae_effect = yes
    }
	if = {
        limit = {
            has_trait = realm_nature
        }
        assign_realm_nature_effect = yes
    }
	if = {
        limit = {
            has_trait = realm_prop
        }
        assign_realm_prop_effect = yes
    }
	if = {
        limit = {
            has_trait = realm_scene
        }
        assign_realm_scene_effect = yes
    }
	if = {
        limit = {
            has_trait = realm_time
        }
        assign_realm_time_effect = yes
    }
	if = {
        limit = {
            has_trait = art_chicanery_advanced
        }
        assign_chicanery_effect = yes
    }
	if = {
        limit = {
            has_trait = art_legerdemain_advanced
        }
        assign_legerdemain_effect = yes
    }
	if = {
        limit = {
            has_trait = art_primal_advanced
        }
        assign_primal_effect = yes
    }
	if = {
        limit = {
            has_trait = art_soothsay_advanced
        }
        assign_soothsay_effect = yes
    }
	if = {
        limit = {
            has_trait = art_wayfare_advanced
        }
        assign_wayfare_effect = yes
    }
	if = {
        limit = {
            has_trait = art_aphrodesia_advanced
        }
        assign_aphrodesia_effect = yes
    }
	if = {
        limit = {
            has_trait = art_kryos_advanced
        }
        assign_kryos_effect = yes
    }
	if = {
        limit = {
            has_trait = art_metamorphosis_advanced
        }
        assign_metamorphosis_effect = yes
    }
	if = {
        limit = {
            has_trait = art_pyretics_advanced
        }
        assign_pyretics_effect = yes
    }
	if = {
        limit = {
            has_trait = art_chronos_advanced
        }
        assign_chronos_effect = yes
    }
	if = {
        limit = {
            has_trait = art_dream_craft_advanced
        }
        assign_dream_craft_effect = yes
    }
	if = {
        limit = {
            has_trait = art_naming_advanced
        }
        assign_naming_effect = yes
    }
	if = {
        limit = {
            has_trait = art_sovereign_advanced
        }
        assign_sovereign_effect = yes
    }
	if = {
        limit = {
            has_trait = art_infusion_advanced
        }
        assign_infusion_effect = yes
    }
	if = {
        limit = {
            has_trait = art_tale_craft_advanced
        }
        assign_tale_craft_effect = yes
    }
	if = {
        limit = {
            has_trait = art_petros_advanced
        }
        assign_petros_effect = yes
    }
	if = {
        limit = {
            has_trait = art_aquis_advanced
        }
        assign_aquis_effect = yes
    }
	if = {
        limit = {
            has_trait = art_pyros_advanced
        }
        assign_pyros_effect = yes
    }
	if = {
        limit = {
            has_trait = art_verdage_advanced
        }
        assign_verdage_effect = yes
    }
	if = {
        limit = {
            has_trait = art_tieh_tan_advanced
        }
        assign_tieh_tan_effect = yes
    }
	if = {
        limit = {
            has_trait = art_lin_tan_advanced
        }
        assign_lin_tan_effect = yes
    }
	if = {
        limit = {
            has_trait = art_shui_tan_advanced
        }
        assign_shui_tan_effect = yes
    }
	if = {
        limit = {
            has_trait = art_hou_tan_advanced
        }
        assign_hou_tan_effect = yes
    }
	if = {
        limit = {
            has_trait = art_di_tan_advanced
        }
        assign_di_tan_effect = yes
    }
	if = {
        limit = {
            has_trait = art_nei_tan_advanced
        }
        assign_nei_tan_effect = yes
    }
}