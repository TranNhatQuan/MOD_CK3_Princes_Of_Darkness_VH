POD_entrance_attacker_tt_effect = {
	if = {
		limit = { scope:attacker = { has_perk = majesty_perk }}
		custom_tooltip = POD_entrance.tt.pc.majesty
	}
	if = {
		limit = { scope:attacker = { has_trait = presenceadvanced } }
		custom_tooltip = POD_entrance.tt.pc.presenceadvanced
	}
	if = {
		limit = { scope:attacker = { has_perk = ironclad_command_perk } }
		custom_tooltip = POD_entrance.tt.pc.ironclad_command_perk
	}
}
POD_entrance_defender_tt_effect = {
	if = {
		limit = { scope:defender = { has_character_flag = willpower_spent } }
		custom_tooltip = POD_entrance.tt.willpower_spent
	}
	if = {
		limit = { scope:defender = { has_perk = unswayable_mind_perk } }
		custom_tooltip = POD_entrance.tt.unswayable_mind_perk
	}
	if = {
		limit = { scope:defender = { has_perk = fortify_the_inner_facade_perk } }
		custom_tooltip = POD_entrance.tt.fortify_the_inner_facade_perk
	}
	##### old bloodsorcery
	# if = {
	# 	limit = { scope:defender = { has_character_flag = song_of_jupiter } }
	# 	custom_tooltip = POD_entrance.tt.song_of_jupiter
	# }
}
POD_mesmerize_defender_tt_effect = {
	if = {
		limit = { scope:defender = { has_character_flag = willpower_spent } }
		custom_tooltip = POD_entrance.tt.willpower_spent
	}
	if = {
		limit = { scope:defender = { has_perk = unswayable_mind_perk } }
		custom_tooltip = POD_entrance.tt.unswayable_mind_perk
	}
	if = {
		limit = { scope:defender = { has_perk = fortify_the_inner_facade_perk } }
		custom_tooltip = POD_entrance.tt.fortify_the_inner_facade_perk
	}
	##### old bloodsorcery
	# if = {
	# 	limit = { scope:defender = { has_character_flag = song_of_jupiter } }
	# 	custom_tooltip = POD_entrance.tt.song_of_jupiter
	# }
	if = {
		limit = {
			scope:defender = { is_vampire_trigger = yes }
			scope:attacker.generation_value < scope:defender.generation_value
		}
		custom_tooltip = POD_mesmerize_tt_defender_generation_adv
	}
	if = {
		limit = { scope:defender = { has_trait = dominateadvanced } }
		custom_tooltip = POD_mesmerize.tt.dominateadvanced
	}
	if = {
		limit = { scope:defender = { has_trait = presenceadvanced } }
		custom_tooltip = POD_mesmerize.tt.presenceadvanced
	}
	if = {
		limit = { scope:defender = { has_trait = auspexadvanced } }
		custom_tooltip = POD_mesmerize.tt.auspexadvanced
	}
	if = {
		limit = { scope:defender = { has_trait = fortitudeadvanced } }
		custom_tooltip = POD_mesmerize.tt.fortitudeadvanced
	}
	#kuei jin
	
	#mummy
	#shih
	if = {
		limit = { scope:defender = { has_perk = mountain_meditation_perk } }
		custom_tooltip = POD_mesmerize.tt.mountain_meditation_perk
	}
	#psi
	if = {
		limit = { scope:defender = { has_perk = anti_psi_1_perk } }
		custom_tooltip = POD_mesmerize.tt.anti_psi_1_perk
	}
	if = {
		limit = { scope:defender = { has_perk = anti_psi_2_perk } }
		custom_tooltip = POD_mesmerize.tt.anti_psi_2_perk
	}
	if = {
		limit = { scope:defender = { has_perk = anti_psi_3_perk } }
		custom_tooltip = POD_mesmerize.tt.anti_psi_3_perk
	}
	if = {
		limit = { scope:defender = { has_perk = anti_psi_4_perk } }
		custom_tooltip = POD_mesmerize.tt.anti_psi_4_perk
	}
	if = {
		limit = { scope:defender = { has_perk = anti_psi_5_perk } }
		custom_tooltip = POD_mesmerize.tt.anti_psi_5_perk
	}
	#tf
	if = {
		limit = { scope:defender = { has_perk = holy_bulwark_perk } }
		custom_tooltip = POD_mesmerize.tt.holy_bulwark_perk
	}
	#werewolves
	if = {
		limit = { scope:defender = { has_perk = release_from_bondage_perk } }
		custom_tooltip = POD_mesmerize.tt.release_from_bondage_perk
	}
	#fae
	if = {
		limit = { scope:defender = { has_perk = fair_is_foul_foul_is_fair_perk } }
		custom_tooltip = POD_mesmerize.tt.fair_is_foul_foul_is_fair_perk
	}
	if = {
		limit = { scope:defender = { has_perk = captive_heart_perk } }
		custom_tooltip = POD_mesmerize.tt.captive_heart_perk
	}
	if = {
		limit = { scope:defender = { has_perk = geas_ban_perk } }
		custom_tooltip = POD_mesmerize.tt.geas_ban_perk
	}
	#artifacts
	if = {
		limit = { scope:defender = { 
			any_equipped_character_artifact = {	has_variable = ancestor_talisman }
			} 
		}
		custom_tooltip = POD_mesmerize.tt.ancestor_talisman
	}
	#personality bonus or malus
	if = {
		limit = { scope:defender = { has_personality_dominant_trigger = yes } }
		custom_tooltip = POD_mesmerize.tt.dominate_personality
	}
}
POD_mesmerize_attacker_tt_effect = {
	if = {
		limit = { 
			scope:attacker = {
				OR = {
					has_perk = rationalize_perk
				} 
			} 
		}
		custom_tooltip = POD_mesmerize.tt.pc.rationalize_perk
	}
	if = {
		limit = { scope:attacker = { has_trait = dominateadvanced } }
		custom_tooltip = POD_mesmerize.tt.pc.dominateadvanced
	}
	if = {
		limit = { scope:attacker = { has_perk = will_conquers_perk } }
		custom_tooltip = POD_mesmerize.tt.pc.will_conquers_perk
	}
	if = {
		limit = {
			scope:defender = { is_vampire_trigger = yes }
			scope:attacker.generation_value > scope:defender.generation_value
		}
		custom_tooltip = POD_mesmerize_tt_attacker_generation_adv
	}
	#mortals have lowered defenses
	if = {
		limit = { scope:defender = { has_trait = mortal } }
		custom_tooltip = POD_mesmerize.tt.mortal
	}
	#personality bonus or malus
	if = {
		limit = { scope:defender = { has_personality_submissive_trigger = yes } }
		custom_tooltip = POD_mesmerize.tt.submissive_personality
	}
}