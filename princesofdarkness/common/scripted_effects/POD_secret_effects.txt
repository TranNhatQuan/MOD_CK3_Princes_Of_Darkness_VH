
POD_give_massregnant_secret_effect = {
	save_temporary_scope_as = massregnant_character
	if = {
		limit = { #Not already a massregnant
			NOR = {
				has_trait = massregnant
				any_secret = { secret_type = secret_massregnant }
			}	
		}
		if = {
			limit = {
				trait_is_shunned_or_criminal_in_my_or_lieges_faith_trigger = { TRAIT = trait:massregnant GENDER_CHARACTER = scope:massregnant_character }
			}
			add_secret = {
				type = secret_massregnant
			}
		}
		else = {
			add_trait = massregnant
		}
	}	
}


POD_give_masssire_secret_effect  = {
	save_temporary_scope_as = masssire_character
	if = {
		limit = { #Not already a masssire
			NOR = {
				has_trait = masssire
				any_secret = { secret_type = secret_masssire }
			}	
		}
		if = {
			limit = {
				trait_is_shunned_or_criminal_in_my_or_lieges_faith_trigger = { TRAIT = trait:masssire GENDER_CHARACTER = scope:masssire_character }
			}
			add_secret = {
				type = secret_masssire
			}
		}
		else = {
			add_trait = masssire
		}
	}	
}

POD_give_regnant_secret_effect = {
	# save_temporary_scope_as = regnant_character
	# if = {
		# limit = { #Not already a regnant
			# NOR = {
				# has_trait = regnant
				# any_secret = { secret_type = secret_regnant }
			# }	
		# }
		# if = {
			# limit = {
				# trait_is_shunned_or_criminal_in_my_or_lieges_faith_trigger = { TRAIT = trait:regnant GENDER_CHARACTER = scope:regnant_character }
			# }
			# add_secret = {
				# type = secret_regnant
			# }
		# }
		# else = {
			# add_trait = regnant
		# }
	# }
}

give_sorcerer_secret_or_trait_effect = {
	save_temporary_scope_as = sorcerer
	if = {
		limit = {
			is_sorcerer_trigger = no
		}
		if = {
			limit = {
				trait_is_shunned_or_criminal_in_my_or_lieges_faith_trigger = { TRAIT = trait:sorcerer GENDER_CHARACTER = scope:sorcerer }
			}
			add_secret = {
				type = secret_sorcerer
			}
		}
		else = {
			add_trait = sorcerer
		}
	}
}