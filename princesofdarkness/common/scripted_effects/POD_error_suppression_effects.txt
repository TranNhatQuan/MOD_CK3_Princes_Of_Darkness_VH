POD_error_suppression_unused_flag = {
	if = {
		limit = { always = no }
		if = {
			limit = { this = flag:$FLAG$ }
		}
	}
}

POD_error_suppression_unset_flag = {
	if = {
		limit = { always = no }
		save_scope_value_as = {
			name  = target
			value = flag:$FLAG$
		}
	}
}