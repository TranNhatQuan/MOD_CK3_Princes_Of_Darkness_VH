POD_static_stress_gain = {
	if = {
		limit = { NOT = { $COUNT$ = 0 } } 
		custom_description = {
			text = "POD_static_stress_gain"
			value = $COUNT$
			add_character_flag = static_stress_gain_flag
			save_scope_value_as = { name = saved_stress  value = stress }
			set_variable = { name = saved_stress  value = stress days = 1 }
			set_variable = { name = saved_stress_level  value = stress_level days = 1 }
			
			add_stress = 100
			
			while = {
				limit = { stress = scope:saved_stress }
				add_character_modifier = POD_static_stress_gain_1_modifier
				force_character_skill_recalculation = yes
				add_stress = 100
			}
			add_stress = -600
			while = {
				limit = { stress > 0 }
				add_character_modifier = POD_static_stress_gain_2_modifier
				force_character_skill_recalculation = yes
				add_stress = -600
			}      
			add_stress = 100
			while = {
				limit = { NOT = { stress = 100 } }
				if = { limit = { stress > 100 } add_character_modifier = POD_static_stress_gain_3_modifier }
				else = { add_character_modifier = POD_static_stress_gain_35_modifier }

				force_character_skill_recalculation = yes
				add_stress = -600
				add_stress = 100
			}
			add_stress = -600
			add_stress = 100
			add_stress = -50
			while = {
				limit = { NOT = { stress = 50 } }
				if = { limit = { stress < 50 } add_character_modifier = POD_static_stress_gain_4_modifier }
				else = { add_character_modifier = POD_static_stress_gain_45_modifier }
				force_character_skill_recalculation = yes

				add_stress = -600
				add_stress = 100
				add_stress = -50
			}
			add_stress = -600
			add_stress = scope:saved_stress
			remove_character_flag = static_stress_gain_flag
			remove_variable = saved_stress
			remove_variable = saved_stress_level
			add_stress = $COUNT$
			remove_all_character_modifier_instances = POD_static_stress_gain_1_modifier
			remove_all_character_modifier_instances = POD_static_stress_gain_2_modifier
			remove_all_character_modifier_instances = POD_static_stress_gain_3_modifier
			remove_all_character_modifier_instances = POD_static_stress_gain_35_modifier
			remove_all_character_modifier_instances = POD_static_stress_gain_4_modifier
			remove_all_character_modifier_instances = POD_static_stress_gain_45_modifier
			force_character_skill_recalculation = yes
		}
	}
}
POD_static_dread_gain = {
	if = {
		limit = { NOT = { $COUNT$ = 0 } } 
		custom_description = {
			text = add_dread # reuse vanilla loc
			value = $COUNT$
			save_scope_value_as = { name = saved_dread  value = dread }
			set_variable = { name = saved_dread  value = dread days = 1 }
			
			add_dread = -50
			while = {
				limit = { dread > 0 }
				add_character_modifier = POD_static_dread_gain_2_modifier
				force_character_skill_recalculation = yes
				add_dread = -50
			}
			add_dread = 50
			
			while = {
				limit = { dread = scope:saved_dread }
				add_character_modifier = POD_static_dread_gain_1_modifier
				force_character_skill_recalculation = yes
				add_dread = 50
			}
			add_dread = -50
			while = {
				limit = { dread > 0 }
				add_character_modifier = POD_static_dread_gain_2_modifier
				force_character_skill_recalculation = yes
				add_dread = -50
			}      
			add_dread = 25
			while = {
				limit = { NOT = { dread = 25 } }
				if = { limit = { dread > 25 } add_character_modifier = POD_static_dread_gain_3_modifier }
				else = { add_character_modifier = POD_static_dread_gain_35_modifier }

				force_character_skill_recalculation = yes
				add_dread = -100
				add_dread = 25
			}
			add_dread = -600
			add_dread = 50
			add_dread = -25
			while = {
				limit = { NOT = { dread = 25 } }
				if = { limit = { dread < 25 } add_character_modifier = POD_static_dread_gain_4_modifier }
				else = { add_character_modifier = POD_static_dread_gain_45_modifier }
				force_character_skill_recalculation = yes

				add_dread = -600
				add_dread = 50
				add_dread = -25
			}
			add_dread = -600
			add_dread = scope:saved_dread
			remove_variable = saved_dread
			add_dread = $COUNT$
			remove_all_character_modifier_instances = POD_static_dread_gain_1_modifier
			remove_all_character_modifier_instances = POD_static_dread_gain_2_modifier
			remove_all_character_modifier_instances = POD_static_dread_gain_3_modifier
			remove_all_character_modifier_instances = POD_static_dread_gain_35_modifier
			remove_all_character_modifier_instances = POD_static_dread_gain_4_modifier
			remove_all_character_modifier_instances = POD_static_dread_gain_45_modifier
			force_character_skill_recalculation = yes
		}
	}
}