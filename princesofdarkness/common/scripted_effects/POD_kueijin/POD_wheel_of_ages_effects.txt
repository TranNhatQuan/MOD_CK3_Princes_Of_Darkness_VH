#POD_change_wheel_of_ages_progress = {
#	if = { 
#		limit = { NOT = { exists = global_var:wheel_of_ages } } 
#		set_global_variable = { name = wheel_of_ages value = 4 } 
#	}
#	custom_description = {
#		text = POD_change_wheel_of_ages_progress
#		value = $VALUE$
#		change_global_variable = {
#			name = wheel_of_ages_progress
#			add = $VALUE$
#		}
#		if = { limit = { wheel_of_ages_progress >= 100 } POD_change_wheel_of_ages = { VALUE = 1 } }
#		if = { limit = { wheel_of_ages_progress <= 0 } POD_change_wheel_of_ages = { VALUE = -1 } }
#	}
#}
#POD_change_wheel_of_ages = {
#	set_global_variable = {
#		name = wheel_of_ages_progress
#		value = 50
#	}
#	change_global_variable = {
#		name = wheel_of_ages
#		add = $VALUE$
#	}
#	every_living_character = {
#		limit = { POD_middle_kingdom_splat_trigger = yes }
#		switch = {
#			trigger = wheel_of_ages
#			2 = { trigger_event = POD_age.1 }
#			3 = { trigger_event = POD_age.2 }
#			4 = { trigger_event = POD_age.3 }
#			5 = { trigger_event = POD_age.4 }
#			6 = { trigger_event = POD_age.5 }
#		}
#	}
#}

POD_remove_wheel_of_ages_modifiers = {
	#POD_remove_character_modifier = { MODIFIER = joyous_age_modifier }
	POD_remove_character_modifier = { MODIFIER = godbodies_modifier }
	# POD_remove_character_modifier = { MODIFIER = akuma_debuff_modifier }
	# POD_remove_character_modifier = { MODIFIER = akuma_buff_modifier }
	POD_remove_character_modifier = { MODIFIER = akuma_huge_buff_modifier }
	# POD_remove_character_modifier = { MODIFIER = yama_debuff_modifier }
	# POD_remove_character_modifier = { MODIFIER = yama_buff_modifier }
	POD_remove_character_modifier = { MODIFIER = yama_huge_buff_modifier }
	POD_remove_character_modifier = { MODIFIER = kj_harder_dharma_modifier }
	POD_remove_character_modifier = { MODIFIER = kj_imposible_dharma_modifier }
}

POD_assing_age_modifier_effect = {
	hidden_effect = {
		POD_remove_wheel_of_ages_modifiers = yes
	}
	#is this the 5th age?
	if = {
		limit = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:fifth_age_started
			}
		}
		#has x been prevented?
		if = {
			limit = {
				NOT = {
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:reckoning_war_of_shame_objective_decision
					}
				}
				is_akuma_trigger = no
				has_trait = dharma
			}				
			add_character_modifier = {
				modifier = kj_harder_dharma_modifier
				years = 50
			}	
		}
		#has x been prevented?
		if = {
			limit = {
				NOT = {
					is_target_in_global_variable_list = {
						name = unavailable_unique_decisions
						target = flag:unhell_objective_decision
					}
				}
				is_akuma_trigger = yes 				
			}				
			add_character_modifier = {
				modifier = akuma_huge_buff_modifier
				years = 50
			}	
		}
		#Godbodies are just not added
		#Political instability only happens once
	}
	#If it's not the fifth age, it's the fourth, so add godbodies
	else_if = {
		limit = { has_trait = dharma }
		add_character_modifier = godbodies_modifier
	}
	
	#switch = {
	#	trigger = wheel_of_ages
	#	3 = {
	#		if = { limit = { is_akuma_trigger = yes }
	#			add_character_modifier = akuma_debuff_modifier
	#		}
	#		if = { limit = { has_character_modifier = yama_king_modifier }
	#			add_character_modifier = yama_debuff_modifier
	#		}
	#		if = { limit = { has_trait = dharma }
	#			add_character_modifier = godbodies_modifier
	#		}
	#		add_character_modifier = joyous_age_modifier
	#	}
	#	4 = {
	#		if = { limit = { has_trait = dharma }
	#			add_character_modifier = godbodies_modifier
	#		}
	#	}
	#	5 = {
	#		if = { limit = { is_akuma_trigger = yes }
	#			add_character_modifier = akuma_buff_modifier
	#		}
	#		else = {
	#			add_character_modifier = kj_harder_dharma_modifier
	#		}
	#		if = { limit = { has_character_modifier = yama_king_modifier }
	#			add_character_modifier = yama_buff_modifier
	#		}
	#	}
	#	6 = {
	#		if = { limit = { is_akuma_trigger = yes }
	#			add_character_modifier = akuma_huge_buff_modifier
	#			add_character_modifier = kj_harder_dharma_modifier
	#		}
	#		else = {
	#			add_character_modifier = kj_imposible_dharma_modifier
	#		}
	#		if = { limit = { has_character_modifier = yama_king_modifier }
	#			add_character_modifier = yama_huge_buff_modifier
	#		}
	#	}
	#}

}