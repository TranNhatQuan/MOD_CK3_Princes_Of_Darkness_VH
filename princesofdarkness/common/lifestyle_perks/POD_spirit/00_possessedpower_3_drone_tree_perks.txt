drone_aura_of_tranquility_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 0.75 4 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 8
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { 
				possessedpower_lifestyle_perks <= 8
			}
			custom_description_no_bullet = { text = has_8_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = aura_of_tranquility_perk_effect
		}
	}
}

drone_blending_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 2.25 0 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		hostile_scheme_phase_duration_add = -5
		owned_scheme_secrecy_add = 5
	}

	effect = {
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = blending_perk_effect
		}
	}
}

drone_stasis_engine_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 0.75 5 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 16
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 16 }
			custom_description_no_bullet = { text = has_16_possessed_perks_unlocked }
		}
		if = {
			limit = { 
				NOT = { has_trait = drone }
			}
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = stasis_engine_perk_effect
		}
	}
}

drone_computer_link_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 3.75 0 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		POD_construct_maa_damage_mult = 0.15
		POD_construct_maa_pursuit_mult = 0.15
		siege_weapon_siege_value_mult = 0.2
	}

	effect = {
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = computer_link_perk_effect
		}
	}
}

drone_cybersenses_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 0 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		cultural_head_fascination_mult = 0.2
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = cybersenses_perk_effect
		}
	}
}

drone_darksight_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 5.25 0 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		scheme_discovery_chance_mult = 0.10
	}

	effect = {
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = darksight_perk_stats_effect
		}
	}
}

drone_dispersion_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 3.75 5 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 16
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 16 }
			custom_description_no_bullet = { text = has_16_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = dispersion_perk_effect
		}
	}
}

drone_ectoplasmic_extrusion_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 0.75 2 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 2
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		prowess_no_portrait = 2
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 2 }
			custom_description_no_bullet = { text = has_2_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = ectoplasmic_extrusion_perk_effect
		}
	}
}

drone_electrical_field_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 2.25 2 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 2
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		prowess_no_portrait = 2
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 2 }
			custom_description_no_bullet = { text = has_2_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = electrical_field_perk_effect
		}
	}
}

drone_elemental_resistance_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 1.5 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		negate_diplomacy_penalty_add = 2
		negate_martial_penalty_add = 2
		negate_stewardship_penalty_add = 2
		negate_intrigue_penalty_add = 2
		negate_learning_penalty_add = 2
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = elemental_resistance_perk_effect
		}
	}
}

drone_enchanting_voice_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 3.75 2 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 2
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 2 }
			custom_description_no_bullet = { text = has_2_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = enchanting_voice_perk_effect
		}
	}
}

drone_enhanced_attribute_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 3 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		diplomacy = 1
		martial = 1
		stewardship = 1
		intrigue = 1
		learning = 1
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = enhanced_attribute_perk_effect
		}
	}
}

drone_homogeneity_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 0.75 3 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 4
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 4 }
			custom_description_no_bullet = { text = has_4_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = homogeneity_perk_effect
		}
	}
}

drone_invisibility_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 2.25 4 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 8
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { 
				possessedpower_lifestyle_perks <= 8
			}
			custom_description_no_bullet = { text = has_8_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = invisibility_perk_stats_effect
		}
	}
}

drone_invulnerability_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 5.25 6 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 32
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		POD_terrain_health_cancel_negative_supply = yes
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 32 }
			custom_description_no_bullet = { text = has_32_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = invulnerability_perk_stats_effect
		}
	}
}

drone_lord_of_the_land_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 3.75 4 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 8
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		controlled_province_advantage = 10
		monthly_county_control_growth_add = 0.10
		domain_tax_mult = 0.05
	}

	effect = {
		if = {
			limit = { 
				possessedpower_lifestyle_perks <= 8
			}
			custom_description_no_bullet = { text = has_8_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = lord_of_the_land_perk_effect
		}
	}
}

drone_magnetokinesis_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 6.75 5 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 16
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		monthly_income_mult = 0.1
		monthly_income = 3
		max_loot_mult = 0.25
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 16 }
			custom_description_no_bullet = { text = has_16_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = magnetokinesis_perk_effect
		}
	}
}

drone_mask_presence_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 2.25 3 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 4
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 4 }
			custom_description_no_bullet = { text = has_4_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = mask_presence_perk_effect
		}
	}
}

drone_matter_weave_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 3.75 3 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 4
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		build_speed = -0.2
		holding_build_speed = -0.2
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 4 }
			custom_description_no_bullet = { text = has_4_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = matter_weave_perk_effect
		}
	}
}

drone_memory_caress_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 6.75 0 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		tyranny_loss_mult = 0.1
	}

	effect = {
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = memory_caress_perk_effect
		}
	}
}

drone_mind_blast_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 5.25 4 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 8
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { 
				possessedpower_lifestyle_perks <= 8
			}
			custom_description_no_bullet = { text = has_8_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = mind_blast_perk_effect
		}
	}
}

drone_peaceful_fighting_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 8.25 0 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = peaceful_fighting_perk_effect
		}
	}
}

drone_reassuring_presence_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 5.25 2 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 2
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		legitimacy_loss_mult = -0.3
	}

	effect = {
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = reassuring_presence_perk_effect
		}
	}
}

drone_silent_speech_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 4.5 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}
	
	character_modifier = {
		owned_scheme_secrecy_add = 10
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = silent_speech_perk_effect
		}
	}
}

drone_spirit_gift_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 6 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		monthly_lifestyle_xp_gain_mult = 0.15
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = spirit_gift_perk_effect
		}
	}
}

drone_spirit_kinship_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 6.75 2 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 2
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		POD_spirit_maa_damage_mult = 0.05
		POD_spirit_maa_toughness_mult = 0.05
		POD_spirit_maa_max_size_add = 2
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 2 }
			custom_description_no_bullet = { text = has_2_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = spirit_kinship_perk_effect
		}
	}
}

drone_spirit_sense_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 7.5 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = spirit_sense_perk_effect
		}
	}
}

drone_spirit_static_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 8.25 2 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 2
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 2 }
			custom_description_no_bullet = { text = has_2_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = spirit_static_perk_stats_effect
		}
	}
}

drone_spirit_ties_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 5.25 3 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 4
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		POD_spirit_maa_max_size_add = 2
		POD_spirit_maa_maintenance_mult = -0.1
		POD_terrain_gnosis_regen_supply_limit_mult = 0.05
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 4 }
			custom_description_no_bullet = { text = has_4_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
	}
}

drone_stasis_cocoon_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 6.75 4 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 8
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	effect = {
		if = {
			limit = { 
				possessedpower_lifestyle_perks <= 8
			}
			custom_description_no_bullet = { text = has_8_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = stasis_cocoon_perk_effect
		}
	}
}

drone_stasis_touch_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 6.75 3 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 4
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		counter_efficiency = 0.15
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 4 }
			custom_description_no_bullet = { text = has_4_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = stasis_touch_perk_effect
		}
	}
}

drone_step_sideways_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 8.25 4 }
	icon = trait_drone
	
	can_be_picked = {
		has_perk = drone_spirit_ties_perk
		possessedpower_lifestyle_perks > 8
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		POD_terrain_umbra_aptitude_advantage = 0.25
		POD_terrain_umbra_sense_advantage = 0.05
		POD_terrain_umbra_spirit_wilds_access_cancel_negative_supply = yes
	}

	effect = {
		if = {
			limit = { 
				possessedpower_lifestyle_perks <= 8
			}
			custom_description_no_bullet = { text = has_8_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_perk = drone_spirit_ties_perk } }
			custom_description_no_bullet = { text = NOT_has_spirit_ties_perk }
		}
		custom_description_no_bullet = {
			text = spirit_ties_perk_effect
		}
	}
}

drone_thick_skinned_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 8.25 3 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 4
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		prowess_no_portrait = 2
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 4 }
			custom_description_no_bullet = { text = has_4_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = thick_skinned_perk_effect
		}
	}
}

drone_triatic_scent_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 9 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		retreat_losses = -0.1
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = triatic_scent_perk_effect
		}
	}
}

drone_triatic_sense_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 10.5 1 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 1
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		pursue_efficiency = 0.1
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 1 }
			custom_description_no_bullet = { text = has_1_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = triatic_sense_perk_effect
		}
	}
}

drone_unnatural_strength_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 9.75 5 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 16
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {
		prowess_no_portrait = 5
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 16 }
			custom_description_no_bullet = { text = has_16_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = unnatural_strength_perk_effect
		}
	}
}

drone_voice_of_reason_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 9.75 2 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 2
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {	
		general_opinion = 5
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 2 }
			custom_description_no_bullet = { text = has_2_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = voice_of_reason_perk_effect
		}
	}
}

drone_wall_walking_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 9.75 3 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 4
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {	
		character_travel_speed_mult = 0.20
		movement_speed = 0.1
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 4 }
			custom_description_no_bullet = { text = has_4_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = wall_walking_perk_effect
		}
	}
}

drone_webbing_perk = {
	lifestyle = possessedpower_lifestyle
	tree = drone
	position = { 9.75 4 }
	icon = trait_drone
	
	can_be_picked = {
		has_trait = drone
		possessedpower_lifestyle_perks > 8
	}

	auto_selection_weight = {
		value = 2000
		if = {
			limit = { has_focus = drone_focus }
			multiply = 5
		}
	}

	character_modifier = {	
		controlled_province_advantage = 10
	}

	effect = {
		if = {
			limit = { possessedpower_lifestyle_perks <= 8 }
			custom_description_no_bullet = { text = has_8_possessed_perks_unlocked }
		}
		if = {
			limit = { NOT = { has_trait = drone } }
			custom_description_no_bullet = { text = NOT_has_drone_trait }
		}
		custom_description_no_bullet = {
			text = webbing_perk_effect
		}
	}
}