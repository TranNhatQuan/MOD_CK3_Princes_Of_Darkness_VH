PODenslave = {
	skill = learning
	icon = icon_scheme_hostile
	desc = PODenslave_desc_general
	success_desc = "PODenslave_SUCCESS_DESC"
	discovery_desc = "GENERIC_DISCOVERY_DESC"
	illustration = "gfx/interface/illustrations/event_scenes/corridor.dds"
	category = hostile
	target_type = character
	is_secret = no
	cooldown = { years = 2 }
	is_basic = yes
	
	# Parameters
	speed_per_skill_point = t1_spsp_owner_value
	speed_per_target_skill_point = t3_spsp_target_value
	success_chance_growth_per_skill_point = 0
	tier_speed = 0
	#uses_resistance = no
	base_progress_goal = 365
	base_maximum_success = 100
	minimum_success = 100
	#minimum_secrecy = 100
	
	allow = {
	}
	
	valid = {
		
	}

	odds_prediction = {
		add = 100
	}

	base_success_chance = {
		base = 100
	}

	# base_secrecy = {
	# 	add = 100
	# }

	on_start = {
		# if = {
		# 	limit = {
		# 		scheme_owner = {
		# 			NOT = { has_variable = agents_added }
		# 		}
		# 	}
		# 	add_agent_slot = agent_drillmaster
		# 	add_agent_slot = agent_proponent
		# 	add_agent_slot = agent_theologian
		# 	add_agent_slot = agent_wrangler
		# 	add_agent_slot = agent_diplomat
		# }
		# else = {
		# 	scheme_owner = { remove_variable = agents_added }
		# }
	}

	on_phase_completed = {
		scope:owner = {
			send_interface_toast = {
				title = POD_shared_loca_success
				left_icon = scope:target
				right_icon = scope:owner
					
				add_hook = {
					type = enslave_hook
					target = scope:target
				}
			}
			trigger_event = POD_mummy_scheme.102
		}
	}

	on_invalidated = {
	}

	on_monthly = {
		save_scope_as = scheme
		scheme_owner = { save_scope_as = owner }
		scope:target = { save_scope_as = target }
	}
	
}
