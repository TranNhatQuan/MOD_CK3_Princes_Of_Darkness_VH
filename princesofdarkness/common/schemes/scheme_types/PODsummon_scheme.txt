PODsummon = {

	skill = diplomacy
	desc = PODsummon_desc_general
	success_desc = "PODSUMMON_SUCCESS_DESC"
	icon = icon_scheme_personal
	illustration = "gfx/interface/illustrations/event_scenes/pod_abstract1.dds"
	category = personal
	target_type = character
	is_secret = no
	is_basic = yes
	uses_resistance = no
	cooldown = { years = 5 }
	
	# Parameters
	speed_per_skill_point = t2_spsp_owner_value
	speed_per_target_skill_point = t3_spsp_target_value
	success_chance_growth_per_skill_point = 0
	tier_speed = 0
	#uses_resistance = no
	base_progress_goal = 365
	base_maximum_success = 100
	minimum_success = 100
	#minimum_secrecy = 100
	
	
	allow = {
		 #has_POD_vamp_blood_trigger = yes # complex trigger... and why?
		is_adult = yes
		scope:target = { 
			is_adult = yes 
		}
	}
	
	valid = {
		
	}

	odds_prediction = {
		add = 100
	}

	base_success_chance = {
		base = 100
	}
	
	on_phase_completed = {
		scheme_owner = {
			trigger_event = POD_summon.300 #summon sucess
		}
	}

	on_invalidated = {
	}

	on_monthly = {
		save_scope_as = scheme
		scheme_owner = { save_scope_as = owner }
		scope:target = { save_scope_as = target }

		random = {
			chance = 50
			scope:owner = {
				send_interface_toast = {
					left_icon = scope:owner
					right_icon = scope:target
					title = PODsummon_monthly_notification_t
					POD_hunger_rouse_check_effect = yes
				}
			}
		}
		
		#TODO_CD: Lifestyle gain?
	}
}
