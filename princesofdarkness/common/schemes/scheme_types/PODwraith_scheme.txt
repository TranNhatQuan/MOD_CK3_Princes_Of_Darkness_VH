PODenthrall = {

	skill = diplomacy
	desc = PODenthrall_desc_general
	success_desc = "PODenthrall_SUCCESS_DESC"
	icon = icon_scheme_personal
	illustration = "gfx/interface/illustrations/event_scenes/corridor.dds"
	category = personal
	target_type = character
	is_secret = no
	is_basic = yes
	uses_resistance = no
	cooldown = { years = 5 }
	
	# Parameters
	speed_per_skill_point = t2_spsp_owner_value
	speed_per_target_skill_point = t3_spsp_target_value
	success_chance_growth_per_skill_point = 0
	tier_speed = 0
	#uses_resistance = no
	base_progress_goal = 365
	base_maximum_success = 100
	minimum_success = 100
	#minimum_secrecy = 100
	
	allow = {
	}
	
	valid = {
		
	}

	odds_prediction = {
		add = 100
	}

	base_success_chance = {
		base = 100
	}

	# base_secrecy = {
	# 	add = 100
	# }
	
	on_phase_completed = {
		scope:owner = {
			send_interface_toast = {
				title = POD_shared_loca_success
				left_icon = scope:target
				right_icon = scope:owner
					
				add_hook = {
					type = enthrall_hook
					target = scope:target
				}
				scope:target = {
					add_opinion = {
						modifier = love_opinion
						target = scope:owner
						opinion = 25
					}
				}
			}
		}
	}


	on_invalidated = {
	}

	on_monthly = {
		save_scope_as = scheme
		scheme_owner = { save_scope_as = owner }
		scope:target = { save_scope_as = target }
	}
	
}
