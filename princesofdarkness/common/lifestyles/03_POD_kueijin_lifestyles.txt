demon_arts_lifestyle = {

	is_valid = {
		OR = {
			has_trait = kueijin
			has_trait = dhampyr
			has_trait = hun_and_po
			has_character_modifier = higher_shih_modifier
		}
	}

	xp_per_level = 1000
	base_xp_gain = 15
}

soul_arts_lifestyle = {

	is_valid = {
		OR = {
			has_trait = kueijin
			has_trait = dhampyr
			has_trait = hun_and_po
			has_character_modifier = higher_shih_modifier
		}
	}

	is_highlighted = {
		has_trait = dharma
	}

	xp_per_level = 1000
	base_xp_gain = 25
}

chi_arts_lifestyle = {

	is_valid = {
		OR = {
			has_trait = kueijin
			has_character_modifier = shade_walker_modifier
			has_trait = hun_and_po
			
			AND = {
				has_perk = renaming_the_hekau_perk
				has_trait_with_flag = chi_art
			}
		}
	}

	xp_per_level = 1000
	base_xp_gain = 10
}


shintai_lifestyle = {

	is_valid = {
		OR = {
			has_trait = kueijin
			has_trait = dhampyr
			has_trait = hun_and_po
		}
	}

	is_highlighted = {
		has_trait = dharma
	}

	xp_per_level = 2000
	base_xp_gain = 10
}