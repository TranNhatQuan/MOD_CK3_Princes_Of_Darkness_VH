POD_story_line = {}

POD_story_node = {}

POD_story_node_map = {
	on_end = {
		if = {
			limit = { has_variable_list = nodes }
			every_in_list = {
				variable = nodes
				end_story = yes
			}
		}
		if = {
			limit = { has_variable_list = lines }
			every_in_list = {
				variable = lines
				end_story = yes
			}
		}
		if = {
			limit = { has_variable_list = grid_lines }
			every_in_list = {
				variable = grid_lines
				end_story = yes
			}
		}
	}
}

# for delaunay triangulation
POD_story_triangle = {}