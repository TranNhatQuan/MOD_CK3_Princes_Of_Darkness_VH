#NOTE!
#When you add new secrets, make sure they're properly included in these scritped triggers:
# trait_is_shunned_in_faith_trigger
# trait_is_criminal_in_faith_trigger
# secret_is_shunned_in_faith_trigger
# secret_is_criminal_in_faith_trigger

#Also, all secrets should be added through their associated scripted effect/effects, not by the add_secret effect

# Predator Types

#Note! Should only be added with "give_predatortype_consensualist_secret_or_trait_effect"

secret_predatortype_consensualist = {
	category = vampiric

	is_valid = {
		secret_predatortype_consensualist_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_shunned = {
		secret_predatortype_consensualist_is_shunned_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_criminal = {
		secret_predatortype_consensualist_is_criminal_trigger = {
			OWNER = scope:secret_owner
		}
	}

	
	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_familias_secret_or_trait_effect"
secret_predatortype_familias = {
	category = vampiric

	is_valid = {
		secret_predatortype_familias_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_shunned = {
		secret_predatortype_familias_is_shunned_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_criminal = {
		secret_predatortype_familias_is_criminal_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_cultist_secret_or_trait_effect"
secret_predatortype_cultist = {
	category = vampiric

	is_valid = {
		secret_predatortype_cultist_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_shunned = {
		secret_predatortype_cultist_is_shunned_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_criminal = {
		secret_predatortype_cultist_is_criminal_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_bloodleech_secret_or_trait_effect"
secret_predatortype_bloodleech = {
	category = vampiric

	is_valid = {
		secret_predatortype_bloodleech_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_shunned = {
		secret_predatortype_bloodleech_is_shunned_trigger = {
			OWNER = scope:secret_owner
		}
	}

	is_criminal = {
		secret_predatortype_bloodleech_is_criminal_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret

		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}


#Note! Should only be added with "give_predatortype_alleycat_secret_or_trait_effect"
secret_predatortype_alleycat = {
	category = vampiric

	is_valid = {
		secret_predatortype_alleycat_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret

		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_bedlamite_secret_or_trait_effect"
secret_predatortype_bedlamite = {
	category = vampiric

	is_valid = {
		secret_predatortype_bedlamite_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_cauchemar_secret_or_trait_effect"
secret_predatortype_cauchemar = {
	category = vampiric

	is_valid = {
		secret_predatortype_cauchemar_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_cenobite_secret_or_trait_effect"
secret_predatortype_cenobite = {
	category = vampiric

	is_valid = {
		secret_predatortype_cenobite_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_footpad_secret_or_trait_effect"
secret_predatortype_footpad = {
	category = vampiric

	is_valid = {
		secret_predatortype_footpad_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_gentry_secret_or_trait_effect"
secret_predatortype_gentry = {
	category = vampiric

	is_valid = {
		secret_predatortype_gentry_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_graverobber_secret_or_trait_effect"
secret_predatortype_graverobber = {
	category = vampiric

	is_valid = {
		secret_predatortype_graverobber_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret

		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_phlebotomist_secret_or_trait_effect"
secret_predatortype_phlebotomist = {
	category = vampiric

	is_valid = {
		secret_predatortype_phlebotomist_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_siren_secret_or_trait_effect"
secret_predatortype_siren = {
	category = vampiric

	is_valid = {
		secret_predatortype_siren_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_farmer_secret_or_trait_effect"
secret_predatortype_farmer = {
	category = vampiric

	is_valid = {
		secret_predatortype_farmer_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}

#Note! Should only be added with "give_predatortype_extortionist_secret_or_trait_effect"
secret_predatortype_extortionist = {
	category = vampiric

	is_valid = {
		secret_predatortype_extortionist_is_valid_trigger = {
			OWNER = scope:secret_owner
		}
	}

	on_expose = {
		save_scope_as = secret
		
		secret_exposed_notification_effect = yes

		# Actual effects of exposing the secret
		scope:secret_owner = {
			secret_exposed_owner_effects_effect = { SECRET = scope:secret POV = scope:secret_exposer }
		}
	}
}
