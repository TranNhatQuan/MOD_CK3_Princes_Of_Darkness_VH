# #NOTE!
# #When you add new secrets, make sure they're properly included in these scritped triggers:
# # trait_is_shunned_in_faith_trigger
# # trait_is_criminal_in_faith_trigger
# # secret_is_shunned_in_faith_trigger
# # secret_is_criminal_in_faith_trigger

# #Also, all secrets should be added through their associated scripted effect/effects, not by the add_secret effect

# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration1 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration1_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	
		
		# secret_exposed_notification_effect = yes
	# }
# }

# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration2 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration2_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	
		
		# secret_exposed_notification_effect = yes
	# }
# }

# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration3 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration3_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	
		
		# secret_exposed_notification_effect = yes
	# }
# }

# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration4 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration4_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	
		
		# secret_exposed_notification_effect = yes
	# }
# }

# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration5 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration5_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	
		
		# secret_exposed_notification_effect = yes
	# }
# }

# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration6 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration6_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }

# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration7 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration7_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes

	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration8 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration8_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes

	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration9 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration9_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration10 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration10_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration11 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration11_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration12 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration13_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }



# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration13 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration13_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration14 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration14_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# is_shunned = {
		# secret_vampgeneration14_is_shunned_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# is_criminal = {
		# secret_vampgeneration14_is_criminal_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration15 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration15_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# is_shunned = {
		# secret_vampgeneration15_is_shunned_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# is_criminal = {
		# secret_vampgeneration15_is_criminal_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }


# #Note! Should only be added with "give_vampgeneration_secret_or_trait_effect"
# secret_vampgeneration16 = {
	# category = vampiric

	# is_valid = {
		# secret_vampgeneration16_is_valid_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# is_shunned = {
		# secret_vampgeneration16_is_shunned_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# is_criminal = {
		# secret_vampgeneration16_is_criminal_trigger = {
			# OWNER = scope:secret_owner
		# }
	# }

	# on_expose = {
		# save_scope_as = secret
		
		# scope:secret_owner = {
			# if = {
				# limit = {
					# NOT = {
						# this = scope:secret_exposer
					# }
				# }
				# trigger_event = POD_secrets.0002
			# }
		# }	

		# secret_exposed_notification_effect = yes
	# }
# }


