POD_hekau_scouring_oblivion_interaction = {
	category = interaction_category_mummy
	icon = POD_hekau_interaction
	desc = POD_hekau_scouring_oblivion_interaction_desc

	cooldown = { years = 3 }

	is_shown = {
		scope:actor = { has_perk = scouring_oblivion_perk }
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_sekhem_for_hekau = { AMOUNT = 3 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_mummy_memory_check_effect = { LEVEL = 5 }
			POD_hekau_cost_effect = { COUNT = 3 }
		}
		scope:recipient = {
			add_character_modifier = {
				modifier = POD_hekau_scouring_oblivion_modifier
				years = 5
			}
			if = {
				limit = { scope:actor = { has_perk = lighten_the_shadow_perk } }
				remove_trait = wight
				if = {
					limit = { POD_is_not_a_mindless_clone = yes }
					POD_end_shadow = yes
				}
				if = {
					limit = { has_trait = spectre } #So we won't remove infernalist for non-spectre
					POD_save_energy_level_before_splat_change = yes
					remove_trait = spectre
					hidden_effect = { POD_change_to_appropriate_energy_type = yes }
					if = {
						limit = { religion = religion:spectre_religion }
						POD_character_wraith_faith_initiation_effect = yes
					}
				}
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_hekau_casting_away_interaction = {
	category = interaction_category_mummy
	icon = POD_hekau_interaction
	desc = POD_hekau_casting_away_interaction_desc

	cooldown = { years = 3 }

	is_shown = {
		NOT = { scope:recipient = scope:actor }
		scope:actor = { has_perk = casting_away_perk }
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_sekhem_for_hekau = { AMOUNT = 6 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_hekau_cost_effect = { COUNT = 6 }
			POD_mummy_memory_check_effect = { LEVEL = 7 }
			duel = {
				skill = prowess
				target = scope:recipient
				1 = {
					compare_modifier = { value = scope:duel_value multiplier = high_positive_duel_skill_multiplier }
					scope:recipient = {
						add_character_modifier = {
							modifier = trapped_in_tempest_modifier
							years = 1
						}
						POD_umbra_send_effect = { LOCATION = flag:tempest }
						if = { limit = { has_access_to_underworld_trigger = no } pod_death = { death_reason = death_tempest killer = scope:actor } }
					}
				}
				250 = {
					compare_modifier = { value = scope:duel_value multiplier = low_negative_duel_skill_multiplier }
				}
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_hekau_separate_khaibit_interaction = {
	category = interaction_category_mummy
	desc = POD_hekau_separate_khaibit_interaction_desc
	icon = POD_hekau_interaction

	cooldown = { years = 20 }

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_perk = separate_khaibit_perk }
		scope:actor = { POD_is_clone_trigger = no }
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_sekhem_for_hekau = { AMOUNT = 6 }
			POD_has_no_mindless_clone_of_type = { TYPE = flag:separated_khaibit }
		}
		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		custom_tooltip = POD_hekau_separate_khaibit_interaction_desc
		scope:actor = {
			POD_mummy_memory_check_effect = { LEVEL = 7 }
			POD_hekau_cost_effect = { COUNT = 6 }
		}
		scope:actor = {
			trigger_event = POD_mummy_hekau.201
		}
	}

	auto_accept = yes

	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes

		piety > 800
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 360
	ai_will_do = {
		base = 5

		modifier = {
			add = 10
			is_at_war = yes
		}
	}
}

POD_hekau_barring_the_gates_interaction = {
	category = interaction_category_mummy
	desc = POD_hekau_barring_the_gates_interaction_desc
	icon = POD_hekau_interaction

	cooldown = { months = 12 }

	target_type = title
	target_filter = recipient_domain_titles

	can_be_picked = {
		scope:target = {
			POD_title_can_receive_county_modifiers = yes
		}
	}

	is_shown = {
		scope:actor = { has_perk = barring_the_gates_perk }
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_sekhem_for_hekau = { AMOUNT = 6 }
		}
		scope:recipient = {
			is_landed = yes
			highest_held_title_tier > tier_barony
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	send_option = {
		flag = strengthening_the_shroud
		localization = POD_shroud.101.a
	}

	send_option = {
		flag = weakening_the_shroud
		localization = POD_shroud.101.b
	}

	on_accept = {
		scope:actor = {
			POD_hekau_cost_effect = { COUNT = 6 }
			POD_mummy_memory_check_effect = { LEVEL = 8 }
		}
		scope:target = {
			custom_tooltip = {
				text = POD_hekau_barring_the_gates_interaction_desc
				every_de_jure_county = {
					every_county_province = {
						if = { limit = { scope:strengthening_the_shroud = yes } POD_increase_shroud_modifier_effect = yes }
						if = { limit = { scope:weakening_the_shroud = yes } POD_decrease_shroud_modifier_effect = yes }
					}
				}
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_hekau_promise_made_before_ammuts_scales_interaction = {
	category = interaction_category_mummy
	desc = POD_hekau_promise_made_before_ammuts_scales_interaction_desc
	icon = POD_hekau_interaction
	
	cooldown = { years = 5 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_perk = promise_made_before_ammuts_scales_perk }
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_sekhem_for_hekau = { AMOUNT = 8 }
		}
		scope:recipient = { NOT = { has_character_modifier = extra_life_1_modifier } }
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_hekau_cost_effect = { COUNT = 8 }
			POD_mummy_memory_check_effect = { LEVEL = 8 }
			add_trait_xp = { trait = mummy value = 2 }
		}
		scope:recipient = { add_character_modifier = extra_life_1_modifier }
		scope:recipient = {
			add_opinion = {
				target = scope:actor
				modifier = gift_opinion
				opinion = 50
			}
		}
	}

	auto_accept = yes
}

POD_hekau_call_of_oblivion_interaction = {
	category = interaction_category_mummy
	icon = POD_hekau_interaction
	desc = POD_hekau_casting_away_interaction_desc

	cooldown = { years = 3 }

	is_shown = {
		NOT = { scope:recipient = scope:actor }
		scope:actor = { has_perk = call_of_oblivion_perk }
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_sekhem_for_hekau = { AMOUNT = 8 }
		}
		scope:recipient = {
			custom_tooltip = {
				text = POD_hekau_call_of_oblivion_interaction_req
				is_immortal = no
			}
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_hekau_cost_effect = { COUNT = 8 }
			POD_mummy_memory_check_effect = { LEVEL = 8 }
			duel = {
				skill = prowess
				target = scope:recipient
				1 = {
					compare_modifier = { value = scope:duel_value multiplier = high_positive_duel_skill_multiplier }
					scope:recipient = {
						custom_tooltip = {
							text = POD_hekau_call_of_oblivion_interaction_effect
							POD_erase_from_existence = yes
						}
					}
				}
				500 = {
					compare_modifier = { value = scope:duel_value multiplier = low_negative_duel_skill_multiplier }
				}
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}