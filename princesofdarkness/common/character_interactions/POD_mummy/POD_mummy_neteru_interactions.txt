POD_set_neteru_interaction = {
	category = interaction_category_mummy
	common_interaction = yes
	interface_priority = 100
	desc = POD_set_neteru_interaction_desc
	icon = POD_hekau_interaction

	ai_min_reply_days = 4
	ai_max_reply_days = 9
	
	cost = { prestige = minor_prestige_value }
	
	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			is_playable_character = yes
			is_amenti_trigger = yes
		}
		scope:recipient = {
			NOT = { has_relation_worshipped_reborn = scope:actor }
			NOT = { has_trait = mummy }
		}
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			NOT =  { is_at_war_with = scope:recipient }
		}
		scope:recipient = {
			custom_description = {
				object = scope:actor
				text = is_courtier_or_vassal_of
				OR = {
					is_courtier_of = scope:actor
					is_vassal_of = scope:actor
				}
			}
			NOT = { has_strong_hook = scope:actor }
			POD_is_spirit_trigger = no
		}
		trigger_if = {
			limit = { scope:actor = { faith = { has_doctrine  = special_doctrine_against_to_apophis_hostility } } }
			NOT = { scope:recipient = { faith = { has_doctrine  = special_doctrine_allied_to_apophis_hostility } } }
		}
		trigger_if = {
			limit = { scope:actor = { faith = { has_doctrine  = special_doctrine_allied_to_apophis_hostility } } }
			NOT = { scope:recipient = { faith = { has_doctrine  = special_doctrine_against_to_apophis_hostility } } }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	#Use hook
	send_option = {
		is_valid = {
			scope:actor = {
				has_usable_hook = scope:recipient
			}
		}
		flag = hook
		localization = SCHEME_HOOK
	}
	should_use_extra_icon = {
		scope:actor = { has_usable_hook = scope:recipient }
	}
	extra_icon = "gfx/interface/icons/character_interactions/hook_icon.dds"

	send_options_exclusive = no

	on_accept = {
		scope:recipient = {
			set_relation_worshipped_reborn = scope:actor
			if = {
				limit = {
					OR = {
						AND = {
							faith = { has_doctrine = special_doctrine_osirian_league }
							scope:actor = { NOT = { has_trait = shemsuheru } }
						}
						faith = {
							NOR = {
								has_doctrine = doctrine_undying_accepted
								has_doctrine = doctrine_undying_ignorant
							}
						}
					}
					
				}
				neteru_faith_initiation_effect = { LIEGE = scope:actor }
			}
		}
	}
	
	ai_targets = {
		ai_recipients = vassals
	}
	
	ai_frequency = 72
	
	ai_potential = {
		primary_title.tier > tier_county
		prestige > minor_prestige_value
	}
	
	ai_accept = {
		base = 0 # Try to make it 0 for most interactions

		modifier = {
			scope:hook = yes
			add = 50
			desc = SCHEME_HOOK_USED
		}
		
		opinion_modifier = { # Opinion Factor
			who = scope:recipient
			opinion_target = scope:actor
			multiplier = 0.5
			desc = AI_SIMPLE_OPINION_REASON
		}
		
		modifier = {
			add = {
				value = ai_zeal
				divide = 2
				multiply = -1
			}
			ai_zeal > 0
			desc = TAKE_THE_VOWS_ZEALOUS
		}

		modifier = {
			add = -50
			has_relation_rival = scope:recipient
		}
		modifier = {
			add = -80
			has_relation_nemesis = scope:recipient
		}
		
		modifier = {
			add = 35
			scope:actor = { is_ai = yes }
		}
	}
	
	ai_will_do = {
		base = 100
		
		modifier = { # The AI will only use a Hook if they couldn't otherwise do this
			scope:hook = yes
			add = -1
		}
	}
}

POD_remove_neteru_interaction = {
	category = interaction_category_mummy
	common_interaction = yes
	interface_priority = 100
	desc = POD_remove_neteru_interaction_desc
	icon = POD_hekau_interaction
	
	is_shown = {
		scope:actor != scope:recipient
		scope:recipient = { has_relation_worshipped_reborn = scope:actor }
	}

	on_accept = {
		scope:recipient = {
			remove_relation_worshipped_reborn = scope:actor
			if = {
				limit = {
					exists = house
					house = scope:actor.house
					can_execute_decision = create_cadet_branch_decision
				}
				create_cadet_branch = {}
			}
			if = {
				limit = {
					has_trait = mortal
					can_hold_title_as_mortal_trigger = no
				}
				POD_depose_effect = yes
			}
		}
	}
	
	auto_accept = yes
	
	ai_will_do = {
		base = 0
	}
}

POD_spell_of_life_interaction = {
	category = interaction_category_mummy
	common_interaction = yes
	interface_priority = 90
	desc = POD_spell_of_life_interaction_desc
	icon = POD_hekau_interaction
	
	cooldown = { years = 10 }
	cost = {
		piety = {
			if = {
				limit = {
					scope:actor = {
						this = character:790512
						has_character_flag = kemintirinoredemption_flag
					}
				}
				value = 2500
			}
			else = {
				value = 10000
			}
		}
	}
	
	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			NOT = { has_trait = banemummy }
			has_character_flag = knows_spell_of_life
		}
		POD_interaction_is_shown = yes
	}
	
	is_valid = {
		scope:recipient = { has_main_supernatural_race_trigger = no }
	}

	is_valid_showing_failures_only = {
		scope:recipient = {
			trigger_if = {
				limit = { scope:actor = { is_amenti_trigger = yes } }
				has_relation_worshipped_reborn = scope:actor
			}
			is_available_adult = yes
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:recipient = {
			POD_become_mummy_effect = yes
			
			remove_relation_worshipped_reborn = scope:actor
			
			add_opinion = {
				modifier = POD_spell_of_life_opinion
				target = scope:actor
			}
			scope:actor = {
				switch = {
					trigger = has_trait
					shemsuheru = { scope:recipient = { POD_mummy_initialization_shemsu_heru_effect = yes } }
					cabiri     = { scope:recipient = { POD_mummy_initialization_cabiri_effect = yes } }
					wutian     = { scope:recipient = { POD_mummy_initialization_wutian_effect = yes } }
					wukuei     = { scope:recipient = { POD_mummy_initialization_wukuei_effect = yes } }
				}
			}
			if = {
				limit = { scope:actor = { POD_is_mummy_trigger = no } }
				set_character_faith = scope:actor.faith
			}
		}
	}
	
	auto_accept = yes
	
	ai_will_do = {
		base = 0
	}
}

POD_create_asekh_sen_interaction = {
	category = interaction_category_mummy
	common_interaction = yes
	interface_priority = 90
	desc = POD_create_asekh_sen_interaction_desc
	icon = POD_hekau_interaction
	
	cooldown = { years = 2 }
	cost = { piety = 750 }
	
	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_trait = banemummy }
		scope:recipient = {
			POD_can_be_ghouled_scheme_trigger = yes
		}
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:recipient = {
			has_relation_worshipped_reborn = scope:actor
			is_available_adult = yes
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:recipient = {
			POD_become_asekh_sen_effect = yes
			set_character_faith = scope:actor.faith
		}
		scope:recipient = {
			add_opinion = {
				target = scope:actor
				modifier = gift_opinion
				opinion = 50
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
	}
	ai_targets = {
		ai_recipients = courtiers
		ai_recipients = peer_vassals
		max = 10
	}
	ai_frequency = 60
	ai_will_do = {
		base = 0
		modifier = {
			add = 100
			scope:recipient = {
				is_knight_of = scope:actor
			}
		}
	}
}

POD_ask_neteru_for_conversion_interaction = {
	category = interaction_category_religion
	common_interaction = yes
	icon = POD_hekau_interaction
	
	desc = {
		triggered_desc = {
			trigger = {
				scope:actor = { faith = { has_doctrine = special_doctrine_against_to_apophis_hostility } }
			}
			desc = POD_ask_neteru_for_osiris_conversion_interaction_desc
		}
		triggered_desc = {
			trigger = {
				scope:actor = { faith = { has_doctrine = special_doctrine_allied_to_apophis_hostility } }
			}
			desc = POD_ask_neteru_for_apophis_conversion_interaction_desc
		}
	}
	
	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			has_trait = mummy
			OR = {
				has_character_modifier = horus_first_modifier
			}
		}
		POD_interaction_is_shown = yes
	}

	is_valid = {
		scope:recipient = {
			has_relation_worshipped_reborn = scope:actor
			trigger_if = {
				limit = {
					scope:actor = {
						OR = {
							has_character_modifier = horus_first_modifier
						}
					}
				}
				scope:recipient = {
					NOT = { faith = { has_doctrine = special_doctrine_against_to_apophis_hostility } }
					OR = {
						has_POD_vamp_blood_trigger = yes
						has_trait = werewolf
						has_trait = kinfolk
						has_trait = supehunter
						has_trait = mortal
					}
				}
			}
		}
	}

	is_valid_showing_failures_only = {
		scope:recipient = { NOT = { has_strong_hook = scope:actor } }
		scope:recipient = { NOT = { has_trait = devoted } }
		scope:recipient = { NOT = { has_trait = order_member } }
		scope:recipient = {
			NOT = { is_imprisoned_by = scope:actor }
		}
		scope:recipient = {
			NOT = { is_at_war_with = scope:actor }
		}
		scope:actor = {
			trigger_if = {
				limit = {
					government_has_flag = government_is_landless_adventurer
					scope:recipient = { is_courtier_of = scope:actor }
				}
				custom_tooltip = {
					text = valid_demand_conversion_conditions_trigger.tt.domicile_requires_shrine
					OR = {
						domicile = { has_domicile_parameter = camp_enable_conversion }
						AND = {
							scope:actor = { has_trait = ashen_adventurer }
							var:military_order_precept ?= flag:zealous
							POD_military_order_rank >= 1
						}
					}
				}
			}
		}

		custom_description = {
			text = "is_head_of_religion"
			subject = scope:recipient
			NOT = { scope:recipient.faith.religious_head = scope:recipient }
		}

		custom_description = {
			text = "is_protected_via_contract"
			subject = scope:recipient
			NAND = { # Vassal Contract forbids meddling by liege
				exists = scope:recipient.liege
				scope:recipient.liege = scope:actor
				scope:recipient = {
					is_ruler = yes
					vassal_contract_has_flag = religiously_protected
				}
			}
		}
		
		trigger_if = {
			limit = {
				scope:recipient = {
					AND = {
						has_variable = cannot_be_converted_by_value
						var:cannot_be_converted_by_value = scope:actor
					}
				}
			}
			custom_tooltip = {
				text = promised_to_not_convert_character
				scope:recipient = {
					NOR = {
						has_variable = cannot_be_converted_by_value
						var:cannot_be_converted_by_value = scope:actor
					}
				}
			}
		}

		custom_tooltip = {
			text = cannot_take_overt_hostile_actions_against_diarch.tt
			NOT = { scope:recipient ?= scope:actor.diarch }
		}
		
		POD_interaction_is_valid_showing_failures_only = yes
	}
	
	on_accept = {
		demand_conversion_interaction_effect = yes
		hidden_effect = {
			scope:actor = {
				send_interface_message = {
					title = ask_for_conversion_courtier_interaction.tt
					left_icon = scope:recipient
					scope:recipient = {
						show_as_tooltip = { demand_conversion_interaction_effect = yes }
					}
				}
			}
		}
		scope:recipient = {
			if = {
				limit = { has_trait = mortal }
				set_character_faith = scope:actor.faith
			}
			else = {
				neteru_faith_initiation_effect = { LIEGE = scope:actor }
			}
		}
	}
	
	auto_accept = yes
	
	ai_will_do = {
		base = 0
	}
}