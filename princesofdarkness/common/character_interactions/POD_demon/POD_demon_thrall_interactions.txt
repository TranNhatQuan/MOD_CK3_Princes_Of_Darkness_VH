POD_demonic_submission_interaction = {
	category = interaction_category_demon
	icon = POD_demon_interaction
	desc = POD_demonic_submission_interaction_desc
	
	cost = { prestige = 250 }
	
	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_trait = demon }
		POD_incompatible_splats_trigger = no
		scope:recipient = {
			is_landed = yes
		}
		scope:actor = {
			highest_held_title_tier >= tier_duchy
			is_landed = yes
		}
	}

	is_valid_showing_failures_only = {
		scope:actor = { NOT = { is_vassal_of = scope:recipient } }
	}

	on_accept = {
		create_title_and_vassal_change = {
			type = granted
			save_scope_as = title_change
			add_claim_on_loss = yes
		}
		if = {
			limit = { scope:actor.primary_title.tier = tier_duchy }
			scope:actor = {
				every_held_title = {
					limit = { tier = tier_duchy }
					change_title_holder = {
						holder = scope:recipient
						change = scope:title_change
					}
					scope:recipient = {
						add_opinion = {
							target = scope:actor
							modifier = received_title_duchy
						}
					}
				}
			}
		}
		else_if = {
			limit = { scope:actor.primary_title.tier = tier_kingdom }
			scope:actor = {
				every_held_title = {
					limit = { tier = tier_kingdom }
					change_title_holder = {
						holder = scope:recipient
						change = scope:title_change
					}
					scope:recipient = {
						add_opinion = {
							target = scope:actor
							modifier = received_title_kingdom
						}
					}
				}
			}
		}
		else_if = {
			limit = { scope:actor.primary_title.tier = tier_empire }
			scope:actor = {
				every_held_title = {
					limit = { tier = tier_empire }
					change_title_holder = {
						holder = scope:recipient
						change = scope:title_change
					}
					scope:recipient = {
						add_opinion = {
							target = scope:actor
							modifier = received_title_empire
						}
					}
				}
			}
		}
		else_if = {
			limit = { scope:actor.primary_title.tier = tier_hegemony }
			scope:actor = {
				every_held_title = {
					limit = { tier = tier_hegemony }
					change_title_holder = {
						holder = scope:recipient
						change = scope:title_change
					}
					scope:recipient = {
						add_opinion = {
							target = scope:actor
							# no hegemony opinion in vanilla
							modifier = received_title_empire
						}
					}
				}
			}
		}
		resolve_title_and_vassal_change = scope:title_change
		
		create_title_and_vassal_change = {
			type = swear_fealty
			save_scope_as = change
		}
		scope:actor = {
			change_liege = {
				liege = scope:recipient
				change = scope:change
			}
		}
		resolve_title_and_vassal_change = scope:change
		scope:recipient = { 
			add_opinion = {
				target = scope:actor
				modifier = gift_opinion
				opinion = 20
			}
		}
	}
	
	auto_accept = yes
	
	ai_will_do = {
		base = 0
	}
}

POD_set_thrall_interaction = {
	category = interaction_category_demon
	common_interaction = yes
	interface_priority = 100
	desc = POD_set_thrall_interaction_desc
	icon = POD_arcana_interaction

	ai_min_reply_days = 4
	ai_max_reply_days = 9

	cost = { prestige = minor_prestige_value }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			is_playable_character = yes
			has_perk = arcana_enthrall_perk
			POD_splat_has_access_to_arcana = yes
		}
		scope:recipient = { is_a_thrall = no }
		POD_hostile_mental_interaction_is_shown = yes
	}

	is_valid = { scope:recipient = { faith = scope:actor.faith } }

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_resolve_for_power = { COUNT = 2 }
		}
		scope:actor = { NOT = { is_at_war_with = scope:recipient } }
		custom_tooltip = {
			text = POD_enthrall_interaction_limit
			scope:actor = { num_of_relation_demonic_thrall < max_thrall_cap }
		}
		scope:recipient = {
			can_be_thrall = yes
			NOT = { has_strong_hook = scope:actor }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	#Use hook
	send_option = {
		is_valid = { scope:actor = { has_usable_hook = scope:recipient } }
		flag = hook
		localization = SCHEME_HOOK
	}

	should_use_extra_icon = { scope:actor = { has_usable_hook = scope:recipient } }
	extra_icon = "gfx/interface/icons/character_interactions/hook_icon.dds"

	send_options_exclusive = no

	on_accept = {
		scope:actor = { POD_resolve_cost_effect = { COUNT = 2 } }
		scope:recipient = {
			POD_become_demonic_thrall_of = { DEMON = scope:actor }
		}
	}

	ai_targets = {
		ai_recipients = vassals
	}

	ai_frequency = 60

	ai_potential = {
		primary_title.tier > tier_county
		prestige > minor_prestige_value
	}

	ai_accept = {
		base = 0 # Try to make it 0 for most interactions

		modifier = {
			scope:hook = yes
			add = 50
			desc = SCHEME_WEAK_HOOK_USED
		}

		opinion_modifier = { # Opinion Factor
			who = scope:recipient
			opinion_target = scope:actor
			multiplier = 0.5
			desc = AI_SIMPLE_OPINION_REASON
		}

		modifier = {
			add = -50
			NOT = { scope:actor.faith = scope:recipient.faith }
			desc = "ZEAL_AGAINST_INFIDELS"
		}

		modifier = {
			add = 25
			scope:actor.faith = scope:recipient.faith
			desc = "SAME_FAITH"
		}

		modifier = {
			add = 25
			scope:actor = {
				domicile ?= { has_domicile_parameter = vampire_thrall_parameter }				
			}
			scope:recipient = {
				is_vampire_trigger = yes
			}
			desc = $abbadon_throne_vampire_thrall_domicile_building$
		}
		
		modifier = {
			add = -50
			has_relation_rival = scope:recipient
		}
		modifier = {
			add = -80
			has_relation_nemesis = scope:recipient
		}

		modifier = { # Safegurad for failed infernalist baron initiation
			add = 1000
			scope:actor = { is_ai = yes }
		}
	}

	ai_will_do = {
		base = 100

		modifier = { # The AI will only use a Hook if they couldn't otherwise do this
			scope:hook = yes
			add = -1
		}
	}
}

POD_remove_thrall_interaction = {
	category = interaction_category_demon
	desc = POD_remove_thrall_interaction_desc
	icon = POD_arcana_interaction

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		scope:recipient = { has_relation_demonic_master = scope:actor }
	}

	on_accept = { scope:recipient = { remove_relation_demonic_master = scope:actor } }

	auto_accept = yes

	ai_will_do = { base = 0 }
}

POD_set_cultist_interaction = {
	category = interaction_category_demon
	common_interaction = yes
	interface_priority = 100
	desc = POD_set_cultist_interaction_desc
	icon = POD_demon_interaction

	ai_min_reply_days = 1
	ai_max_reply_days = 2

	cost = { 
		prestige = {
			value = minor_prestige_value
			if = { limit = { scope:actor = { has_perk = arcana_gift_of_faith_perk } } multiply = 0.75 }
			if = { limit = { scope:actor = { has_perk = arcana_theft_of_faith_perk } } multiply = 0.5 }
		} 
	}

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			is_playable_character = yes
			POD_splat_has_access_to_arcana = yes
		}
		scope:recipient = {  NOT = { has_relation_worshipped_demon = scope:actor } }
		POD_hostile_mental_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			NOT =  { is_at_war_with = scope:recipient }
		}
		scope:recipient = {
			NOT = { has_strong_hook = scope:actor }
			custom_tooltip = {
				text = POD_set_cultist_vassal_requirement
				OR = {
					is_courtier_of = scope:actor
					is_vassal_of = scope:actor
					liege ?= { has_relation_demonic_master = scope:actor }
				}
			}
			POD_is_spirit_trigger = no
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	#Use hook
	send_option = {
		is_valid = { scope:actor = { has_usable_hook = scope:recipient } }
		flag = hook
		localization = SCHEME_HOOK
	}

	should_use_extra_icon = { scope:actor = { has_usable_hook = scope:recipient } }
	extra_icon = "gfx/interface/icons/character_interactions/hook_icon.dds"

	send_options_exclusive = no

	on_accept = {
		scope:recipient = {
			set_relation_worshipped_demon = scope:actor
			if = { limit = { has_trait = supehunter } scope:actor = { save_scope_as = vampiresire } POD_hunter_enthrall_effect = yes }
		}
		scope:actor = {
			POD_landless_demon_gain_random_knowledge = yes
		}			
	}

	ai_targets = {
		ai_recipients = vassals
	}

	ai_frequency = 72

	ai_potential = {
		primary_title.tier > tier_county
		prestige > minor_prestige_value
	}

	ai_accept = {
		base = 0 # Try to make it 0 for most interactions

		modifier = {
			scope:hook = yes
			add = 50
			desc = SCHEME_HOOK_USED
		}

		opinion_modifier = { # Opinion Factor
			who = scope:recipient
			opinion_target = scope:actor
			multiplier = 0.5
			desc = AI_SIMPLE_OPINION_REASON
		}

		modifier = {
			add = {
				value = ai_zeal
				divide = 2
				multiply = -1
			}
			ai_zeal > 0
			NOT = { scope:actor.faith = scope:recipient.faith }
			desc = TAKE_THE_VOWS_ZEALOUS
		}

		modifier = {
			add = -50
			has_relation_rival = scope:recipient
		}
		modifier = {
			add = -80
			has_relation_nemesis = scope:recipient
		}

		modifier = {
			add = 35
			scope:actor = { is_ai = yes }
		}
	}

	ai_will_do = {
		base = 100

		modifier = { # The AI will only use a Hook if they couldn't otherwise do this
			scope:hook = yes
			add = -1
		}
	}
}

POD_remove_cultist_interaction = {
	category = interaction_category_demon
	common_interaction = yes
	interface_priority = 100
	desc = POD_remove_cultist_interaction_desc
	icon = POD_demon_interaction

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		scope:recipient = { has_relation_worshipped_demon = scope:actor }
	}

	is_valid = { scope:recipient = { NOT = { has_relation_demonic_master = scope:actor } } }

	on_accept = {
		scope:recipient = {
			remove_relation_worshipped_demon = scope:actor
			if = { limit = { has_trait = mortal can_hold_title_as_mortal_trigger = no } POD_depose_effect = yes }
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_summon_demon_interaction = {
	category = interaction_category_demon
	icon = POD_demon_interaction
	common_interaction = yes
	interface_priority = 120
	desc = POD_summon_demon_interaction_desc

	cooldown = { years = 2 }
	cost = { piety = 1750 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		OR = {
			has_global_variable = abyss_broken
			scope:actor = {
				has_character_flag = landless_demon_summoning_unlocked
			}
		}
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:recipient = {
			is_imprisoned_by = scope:actor
			can_be_demonic_possesed = yes
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:recipient = {
			POD_become_demon_effect = yes
		}
		if = {
			limit = {
				scope:actor = {
					NOT = { has_character_flag = landless_demon_summoning_unlocked }
				}
			}
			scope:recipient = {
				add_trait = possessed_genetic				
			}			
		}		
		if = {
			limit = {
				scope:actor = {
					has_character_flag = landless_demon_summoning_unlocked
				}
			}
			scope:recipient = {
				POD_change_ritual_of_sundered_soul_effect = { COUNT = 2 }
			}			
		}
		scope:recipient = { add_opinion = { target = scope:actor modifier = gift_opinion opinion = 50 } }
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_command_thrall_gold_interaction = {
	category = interaction_category_demon
	icon = POD_demon_interaction
	desc = POD_command_thrall_gold_interaction_desc

	cooldown_against_recipient = { years = 5 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		scope:recipient = {
			has_relation_worshipped_demon = scope:actor
			is_a_thrall = yes
		}
		POD_interaction_is_shown = yes
	}

	is_valid = {
		scope:recipient = { gold >= 1 }
	}

	is_valid_showing_failures_only = {
		scope:actor = { arcana_lifestyle_perk_points >= 1 }
		POD_interaction_is_valid_showing_failures_only = yes
	}

	send_option = {
		is_valid = {
			scope:recipient = { gold >= scope:actor.major_gold_value }
			scope:actor = { arcana_lifestyle_perk_points >= 1 }
		}
		flag = major_gold
		localization = POD_command_thrall_gold_interaction_option_major_gold
	}

	send_option = {
		is_valid = {
			scope:recipient = { gold >= scope:actor.massive_gold_value }
			scope:actor = { arcana_lifestyle_perk_points >= 2 }
		}
		flag = massive_gold
		localization = POD_command_thrall_gold_interaction_option_massive_gold
	}

	send_option = {
		is_valid = {
			scope:recipient = { gold >= scope:actor.monumental_gold_value }
			scope:actor = { arcana_lifestyle_perk_points >= 4 }
		}
		flag = monumental_gold
		localization = POD_command_thrall_gold_interaction_option_monumental_gold
	}

	send_option = {
		is_valid = {
			scope:recipient = { gold >= scope:actor.excessive_gold_value }
			scope:actor = { arcana_lifestyle_perk_points >= 8 }
		}
		flag = excessive_gold
		localization = POD_command_thrall_gold_interaction_option_excessive_gold
	}

	send_option = {
		is_valid = {
			scope:recipient = { gold >= scope:actor.omega_gold_value }
			scope:actor = { arcana_lifestyle_perk_points >= 16 }
		}
		flag = omega_gold
		localization = POD_command_thrall_gold_interaction_option_omega_gold
	}

	on_accept = {
		switch = {
			trigger = yes
			scope:omega_gold = {
				scope:actor = { add_arcana_lifestyle_perk_points = -16 }
				scope:recipient = {
					add_investiture_lifestyle_perk_points = 16
					pay_short_term_gold = { target = scope:actor gold = scope:actor.omega_gold_value }
				}
			}
			scope:excessive_gold = {
				scope:actor = { add_arcana_lifestyle_perk_points = -8 }
				scope:recipient = {
					add_investiture_lifestyle_perk_points = 8
					pay_short_term_gold = { target = scope:actor gold = scope:actor.excessive_gold_value }
				}
			}
			scope:monumental_gold = {
				scope:actor = { add_arcana_lifestyle_perk_points = -4 }
				scope:recipient = {
					add_investiture_lifestyle_perk_points = 4
					pay_short_term_gold = { target = scope:actor gold = scope:actor.monumental_gold_value }
				}
			}
			scope:massive_gold = {
				scope:actor = { add_arcana_lifestyle_perk_points = -2 }
				scope:recipient = {
					add_investiture_lifestyle_perk_points = 2
					pay_short_term_gold = { target = scope:actor gold = scope:actor.massive_gold_value }
				}
			}
			scope:major_gold = {
				scope:actor = { add_arcana_lifestyle_perk_points = -1 }
				scope:recipient = {
					add_investiture_lifestyle_perk_points = 1
					pay_short_term_gold = { target = scope:actor gold = scope:actor.major_gold_value }
				}
			}
			fallback = {
				scope:actor = { add_arcana_lifestyle_perk_points = -1 }
				scope:recipient = {
					add_investiture_lifestyle_perk_points = 1
					pay_short_term_gold = { target = scope:actor gold = scope:recipient.gold }
				}
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_command_thrall_artifact_interaction = {
	category = interaction_category_demon
	icon = POD_demon_interaction
	desc = POD_command_thrall_artifact_interaction_desc

	target_type = artifact
	target_filter = recipient_artifacts

	cooldown_against_recipient = { years = 5 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		scope:recipient = {
			has_relation_worshipped_demon = scope:actor
			is_a_thrall = yes
		}
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:recipient = {
			custom_description = {
				text = "demand_artifact_interaction_valid_tt_1"
				has_any_artifact = yes
			}
		}
		trigger_if = {
			limit = { exists = scope:target }
			trigger_if = {
				limit = { scope:target = { rarity = illustrious } }
				scope:actor = { arcana_lifestyle_perk_points >= 8 }
			}
			trigger_else_if = {
				limit = { scope:target = { rarity = famed } }
				scope:actor = { arcana_lifestyle_perk_points >= 4 }
			}
			trigger_else_if = {
				limit = { scope:target = { rarity = masterwork } }
				scope:actor = {arcana_lifestyle_perk_points >= 2 }
			}
			trigger_else = {
				scope:actor = { arcana_lifestyle_perk_points >= 1 }
			}
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		if = {
			limit = { scope:target = { rarity = illustrious } }
			scope:actor = { add_arcana_lifestyle_perk_points = -8 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 8 }
		}
		else_if = {
			limit = { scope:target = { rarity = famed } }
			scope:actor = { add_arcana_lifestyle_perk_points = -4 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 4 }
		}
		else_if = {
			limit = { scope:target = { rarity = masterwork } }
			scope:actor = { add_arcana_lifestyle_perk_points = -2 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 2 }
		}
		else = {
			scope:actor = { add_arcana_lifestyle_perk_points = -1 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 1 }
		}
		scope:target = { set_owner = scope:actor }
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_command_thrall_title_interaction = {
	category = interaction_category_demon
	icon = POD_demon_interaction
	desc = POD_command_thrall_title_interaction_desc

	target_type = title
	target_filter = recipient_domain_titles

	cooldown_against_recipient = { years = 5 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		scope:recipient = {
			has_relation_worshipped_demon = scope:actor
			is_a_thrall = yes
			is_landed = yes
		}
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:recipient = { is_at_war = no }
		POD_interaction_is_valid_showing_failures_only = yes
	}

	can_be_picked_title = {
		scope:target = { 
			NOT = { tier = tier_hegemony }
			NOT = { tier = tier_empire }
			NOT = { tier = tier_barony }
			POD_title_is_haven_landed_domicile = no
		}
		trigger_if = {
			limit = { scope:target = { tier = tier_kingdom } }
			scope:actor = { arcana_lifestyle_perk_points >= 8 }
		}
		trigger_else_if = {
			limit = { scope:target = { tier = tier_duchy } }
			scope:actor = { arcana_lifestyle_perk_points >= 4 }
		}
		trigger_else_if = {
			limit = { scope:target = { tier = tier_county } }
			scope:actor = { arcana_lifestyle_perk_points >= 2 }
		}
		trigger_else = {
			scope:actor = { arcana_lifestyle_perk_points >= 1 }
		}
	}

	has_valid_target = { exists = scope:target }

	has_valid_target_showing_failures_only = {
		NOT = { scope:target = scope:recipient.primary_title }
	}

	on_accept = {
		if = {
			limit = { scope:target = { tier = tier_kingdom } }
			scope:actor = { add_arcana_lifestyle_perk_points = -8 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 8 }
		}
		else_if = {
			limit = { scope:target = { tier = tier_duchy } }
			scope:actor = { add_arcana_lifestyle_perk_points = -4 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 4 }
		}
		else_if = {
			limit = { scope:target = { tier = tier_county } }
			scope:actor = { add_arcana_lifestyle_perk_points = -2 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 2 }
		}
		else = {
			scope:actor = { add_arcana_lifestyle_perk_points = -1 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 1 }
		}
		create_title_and_vassal_change = {
			type = granted
			save_scope_as = title_change
			add_claim_on_loss = no
		}
		scope:target = {
			change_title_holder = {
				holder = scope:actor
				change = scope:title_change
			}
		}
		resolve_title_and_vassal_change = scope:title_change
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_command_thrall_vassal_interaction = {
	category = interaction_category_demon
	icon = POD_demon_interaction
	desc = POD_command_thrall_vassal_interaction_desc

	cooldown_against_recipient = { years = 5 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		scope:recipient = {
			has_relation_worshipped_demon = scope:actor
			is_a_thrall = yes
			is_landed = yes
		}
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		trigger_if = {
			limit = { exists = scope:secondary_recipient }
			scope:actor.primary_title.tier > scope:secondary_recipient.primary_title.tier
			trigger_if = {
				limit = { scope:secondary_recipient = { primary_title.tier = tier_kingdom } }
				scope:actor = { arcana_lifestyle_perk_points >= 8 }
			}
			trigger_else_if = {
				limit = { scope:secondary_recipient = { primary_title.tier = tier_duchy } }
				scope:actor = { arcana_lifestyle_perk_points >= 4 }
			}
			trigger_else_if = {
				limit = { scope:secondary_recipient = { primary_title.tier = tier_county } }
				scope:actor = { arcana_lifestyle_perk_points >= 2 }
			}
			trigger_else = {
				scope:actor = { arcana_lifestyle_perk_points >= 1 }
			}
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	populate_recipient_list = {
		scope:recipient = {
			every_vassal = {
				limit = {
					NOT = { this = scope:actor }
					primary_title.tier > tier_barony
				}
				add_to_list = characters
			}
		}
	}

	on_accept = {
		if = {
			limit = { scope:secondary_recipient = { primary_title.tier = tier_kingdom } }
			scope:actor = { add_arcana_lifestyle_perk_points = -8 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 8 }
		}
		else_if = {
			limit = { scope:secondary_recipient = { primary_title.tier = tier_duchy } }
			scope:actor = { add_arcana_lifestyle_perk_points = -4 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 4 }
		}
		else_if = {
			limit = { scope:secondary_recipient = { primary_title.tier = tier_county } }
			scope:actor = { add_arcana_lifestyle_perk_points = -2 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 2 }
		}
		else = {
			scope:actor = { add_arcana_lifestyle_perk_points = -1 }
			scope:recipient = { add_investiture_lifestyle_perk_points = 1 }
		}
		create_title_and_vassal_change = {
			type = swear_fealty
			save_scope_as = change
		}
		scope:secondary_recipient = {
			change_liege = {
				liege = scope:actor
				change = scope:change
			}
		}
		resolve_title_and_vassal_change = scope:change
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_command_thrall_war_interaction = {
	category = interaction_category_demon
	icon = POD_demon_interaction
	desc = POD_command_thrall_war_interaction_desc

	cooldown_against_recipient = { years = 5 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { POD_splat_has_access_to_arcana = yes }
		scope:recipient = {
			has_relation_worshipped_demon = scope:actor
			is_a_thrall = yes
			is_landed = yes
			highest_held_title_tier > tier_barony
		}
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = { arcana_lifestyle_perk_points >= 1 }
		scope:recipient = { is_at_war = no }
		POD_interaction_is_valid_showing_failures_only = yes
	}

	populate_recipient_list = {
		scope:actor = {
			every_neighboring_top_liege_realm_owner = {
				limit = { NOT = { this = scope:recipient } }
				add_to_list = characters
			}
		}
		scope:recipient = {
			every_neighboring_top_liege_realm_owner = {
				limit = { NOT = { this = scope:actor } }
				add_to_list = characters
			}
		}
	}

	on_accept = {
		scope:actor = { add_arcana_lifestyle_perk_points = -1 }
		scope:recipient = {
			add_investiture_lifestyle_perk_points = 1
			start_war = {
				cb = religious_war
				target = scope:secondary_recipient
				target_title = scope:secondary_recipient.primary_title
			}
		}
		if = {
			limit = {
				scope:actor = { 
					domicile ?= { has_domicile_parameter = abbadon_wrath_parameter }
				}
			}
			scope:recipient = {
				add_character_modifier = {
					modifier = warfare_wrathful_strategy_modifier
					years = 5
				}
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}