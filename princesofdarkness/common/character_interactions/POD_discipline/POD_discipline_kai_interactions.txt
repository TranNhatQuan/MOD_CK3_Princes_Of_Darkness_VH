POD_kai_elder_4_perk_interaction = { #Raijinto
	category = interaction_category_vampire
	icon = POD_kai_interaction
	desc = POD_kai_elder_4_perk_interaction_desc

	cooldown = { years = 5 }
	ai_frequency = 0

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			has_perk = kai_elder_4_perk
			has_kai_trigger = yes
		}
		POD_hostile_interaction_is_shown = yes
	}

	can_send = {
		exists = scope:secondary_recipient
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_blood_for_discipline = { AMOUNT = 5 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	populate_recipient_list = {
		scope:actor = {
			every_relation = {
				limit = {
					is_imprisoned_by = scope:actor
				}
				type = ghoul_thrall
				add_to_list = characters
			}
			dynasty = {
				every_dynasty_member = {
					limit = {
						is_imprisoned_by = scope:actor
					}
					add_to_list = characters
				}
			}
		}
	}

	on_accept = {
		scope:actor = {
			POD_discipline_cost_effect = { COUNT = 5 }
			POD_commit_heinous_act_effect = yes
			POD_increase_wounds_effect = {
				REASON = lightning
				COUNT = POD_raijinto_base_damage
			}
			if = {
				limit = {
					faith = { has_doctrine_parameter = hurt_mortal_lose_piety }
				}
				add_piety = -200
			}
		}
		scope:recipient = {
			if = {
				limit = {
					POD_has_advanced_defensive_capabilities_trigger = {
						AMOUNT = "amount = 0"
					}
				}
				pod_death = {
					death_reason = death_lightning
					killer = scope:actor
				}
			}
			else = {
				save_scope_value_as = {
					name = raijinto_damage
					value = {
						value = POD_raijinto_base_damage
						if = {
							limit = {
								POD_has_advanced_defensive_capabilities_trigger = {
									AMOUNT = "amount = 1"
								}
							}
							multiply = 3
						}
						else_if = {
							limit = {
								POD_has_advanced_defensive_capabilities_trigger = {
									AMOUNT = "amount <= 3"
								}
							}
							multiply = 2
						}
					}
				}
				POD_increase_wounds_hostile_effect = {
					ATTACKER = scope:actor
					REASON = lightning
					COUNT = scope:raijinto_damage
				}
			}
		}
		scope:secondary_recipient = {
			pod_death = {
				death_reason = death_execution
				killer = scope:actor
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
	}
}

POD_kai_elder_5_perk_interaction = {
	category = interaction_category_vampire
	icon = POD_kai_interaction
	desc = POD_kai_elder_5_perk_interaction_desc

	cooldown = { years = 5 }
	ai_frequency = 0

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			has_perk = kai_elder_5_perk
			has_kai_trigger = yes
		}
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:recipient = {
			is_at_war = yes
		}
		scope:actor = {
			POD_has_enough_blood_for_discipline = { AMOUNT = 5 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = { #If nerfing then change the modifier values instead since this is essentially an on war interaction that has a bit of a cost and minor use of being able to use it to sabotage a side in a war you are not apart of which is mostly for roleplaying (uncertain of how powerful/weak the supply duration effect will prove to be against ai)
			POD_discipline_cost_effect = { COUNT = 5 }
			POD_commit_heinous_act_effect = yes
			if = {
				limit = {
					faith = { has_doctrine_parameter = hurt_mortal_lose_piety }
				}
				add_piety = -200
			}
		}
		if = { #Crimson Storm
			limit = {
				scope:actor = { NOT = { has_perk = kai_elder_6_perk } }
			}
			scope:recipient = {
				add_character_modifier = {
					modifier = kai_elder_5_perk_modifier
					years = 1
				}
				every_vassal = {
					add_character_modifier = {
						modifier = kai_elder_5_perk_modifier
						years = 1
					}
				}
				every_courtier = {
					add_character_modifier = {
						modifier = kai_elder_5_perk_modifier
						years = 1
					}
				}
			}
		}
		else = { #Divine Wind
			scope:recipient = {
				add_character_modifier = {
					modifier = kai_elder_6_perk_modifier
					years = 1
				}
				every_vassal = {
					add_character_modifier = {
						modifier = kai_elder_6_perk_modifier
						years = 1
					}
				}
				every_courtier = {
					add_character_modifier = {
						modifier = kai_elder_6_perk_modifier
						years = 1
					}
				}
			}
		}
	}

	auto_accept = yes

	ai_will_do = {
		base = 0
		
		modifier = {
			add = 45
			scope:actor = {
				is_at_war_with = scope:recipient
			}
		}
		modifier = {
			factor = 2
			scope:actor = {
				has_any_bad_relationship_with_character_trigger = {
					CHARACTER = scope:recipient
				}
			}
		}
		modifier = {
			factor = 0
			scope:recipient = {
				is_ai = no
			}
		}
	}
}

