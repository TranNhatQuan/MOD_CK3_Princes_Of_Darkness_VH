POD_drain_the_life_interaction = {
	category = interaction_category_vampire
	#common_interaction = yes
	#interface_priority = 90
	icon = POD_obtenebration_interaction
	desc = POD_drain_the_life_interaction_desc

	ai_frequency = 0

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_obtenebration_trigger = yes }
		scope:actor = {
			has_perk = drain_the_life_perk
		}
		scope:recipient = { is_landed = yes }
		scope:recipient.highest_held_title_tier > tier_barony
		scope:actor = { NOT = { target_is_liege_or_above = scope:recipient } }
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_blood_for_discipline = { AMOUNT = 2 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		custom_tooltip = POD_drain_the_life_interaction.tt
		scope:actor = {
			trigger_event = POD_drain_the_life.001
		}
	}

	#ai_targets = {
	#	ai_recipients = primary_war_enemies
	#}

	auto_accept = yes

	cooldown = { years = 5 }

	ai_will_do = {
		base = 0
		
		modifier = {
			add = 45
			scope:actor = {
				is_at_war_with = scope:recipient
			}
			piety > 300
		}
		modifier = {
			add = 10
			scope:actor = {
				has_relation_rival = scope:recipient
			}
		}
	}
}


POD_nocturne_on_personal_interaction = {
	category = interaction_category_vampire
	#common_interaction = yes
	#interface_priority = 90
	icon = POD_obtenebration_interaction
	desc = POD_nocturne_on_personal_interaction_desc
	
	ai_frequency = 20

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_obtenebration_trigger = yes }
		scope:actor = {
			has_perk = nocturne_perk
		}
		scope:actor = { NOT = { has_character_modifier = nocturne_power_modifier } }
	}
	
	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_blood_for_discipline = { AMOUNT = 1 }
		}
		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		custom_tooltip = POD_nocturne_on_personal_interaction.tt
		scope:actor = {
			add_character_modifier = nocturne_power_modifier
			POD_hunger_rouse_check_effect = yes
			if = {
				limit = {
					has_perk = nocturne_perk
					NOT = { has_perk = the_darkness_within_perk }
					NOT = { has_trait = obtenebrationadvanced }
				}
				POD_small_risk_of_increasing_masquerade_exposure = yes
			}
			else_if = {
				limit = {
					has_perk = nocturne_perk
					OR = {
						has_perk = the_darkness_within_perk
						has_trait = obtenebrationadvanced
					}
					NOT = { has_perk = devour_all_hope_perk }
				}
				POD_moderate_risk_of_increasing_masquerade_exposure = yes
			}
			else_if = {
				limit = {
					has_perk = devour_all_hope_perk
				}
				POD_hunger_rouse_check_effect = yes
				POD_very_high_risk_of_increasing_masquerade_exposure = yes
			}
		}
	}
	
	ai_targets = {
		ai_recipients = self
	}
	
	ai_will_do = {
		base = 0
		
		modifier = {
			add = 45
			is_at_war = yes
			piety > 450
		}
		
		modifier = {
			add = 100
			is_commanding_army = yes
			piety > 250
		}
	}
}

POD_nocturne_off_personal_interaction = {
	category = interaction_category_vampire
	#common_interaction = yes
	#interface_priority = 90
	icon = POD_obtenebration_interaction
	desc = POD_nocturne_off_personal_interaction_desc
	
	ai_frequency = 20

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_character_modifier = nocturne_power_modifier }
	}
	
	is_valid_showing_failures_only = {
		# always allow turning it off
		#POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			custom_tooltip = POD_nocturne_off_personal_interaction.tt
			remove_character_modifier = nocturne_power_modifier
			POD_minimal_risk_of_increasing_masquerade_exposure = yes
		}
	}
	
	ai_targets = {
		ai_recipients = self
	}
	
	ai_will_do = {
		base = 0
		
		modifier = {
			add = 50
			is_at_war = no
		}
		
		modifier = {
			add = 70
			is_commanding_army = no
		}
		modifier = {
			add = 100
			piety < 150
		}
	}
}

POD_shadow_twin_interaction = {
	category = interaction_category_vampire
	#common_interaction = yes
	#interface_priority = 91
	icon = POD_obtenebration_interaction
	desc = POD_shadow_twin_interaction_desc

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_obtenebration_trigger = yes }
		scope:actor = { has_perk = shadow_twin_perk }
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_blood_for_discipline = { AMOUNT = 2 }
			POD_has_no_mindless_clone_of_type = { TYPE = flag:shadow_twin }
		}
		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		custom_tooltip = POD_shadow_twin_interaction.tt
		scope:actor = {
			trigger_event = POD_shadow_twin.001
		}
	}

	auto_accept = yes

	cooldown = { years = 20 }

	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
		
		piety > 800
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 360
	ai_will_do = {
		base = 5
		
		modifier = {
			add = 10
			is_at_war = yes
		}
	}
}

POD_feed_the_darkness_interaction = {
	category = interaction_category_vampire
	common_interaction = yes
	#interface_priority = 88
	desc = POD_feed_the_darkness_interaction_desc
	ai_frequency = 0
	
	is_shown = {
		scope:actor = { has_POD_vamp_blood_trigger = yes }
		scope:actor != scope:recipient
		scope:recipient = { is_adult = yes }
		scope:actor = { is_adult = yes }
		scope:actor = {
			has_character_modifier = ritual_feed_the_darkness_modifier
		}
		scope:recipient = { is_courtier_of = scope:actor }
		POD_hostile_interaction_is_shown = yes
	}
	
	is_valid_showing_failures_only = {
		scope:recipient = {
			POD_has_strong_mental_defenses_trigger = no
			POD_can_be_drained_as_prisoner_trigger = yes
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}
	
	send_option = {
		flag = sip
		is_shown = {
			scope:recipient = { drinkable_blood_left >= 1 }
		}
		localization = POD_drink_herd_sip_option
	}
	
	send_option = {
		flag = drink
		is_shown = {
			scope:recipient = { drinkable_blood_left >= 2 }
		}
		localization = POD_drink_herd_drink_option
	}
	
	send_option = {
		flag = alot
		is_shown = {
			scope:recipient = { drinkable_blood_left >= 3 }
		}
		localization = POD_drink_herd_alot_option
	}
	
	send_option = {
		flag = drain
		localization = POD_drink_herd_drain_option
	}
	
	on_accept = {
		#Sets the proper scopes so that the effects work
		scope:actor = { save_scope_as = vampire }
		scope:recipient = { save_scope_as = victim }
		scope:actor = { save_scope_as = herd_owner }
		scope:recipient = { save_scope_as = herdmember }
		
		hidden_effect = {
			scope:victim = {
				POD_set_resonance_effect = yes
			}
		}

		if = {
			limit = { scope:sip = yes }

			scope:actor = {
				POD_drink_from_victim_no_secondary_effect = {
					VICTIM = scope:recipient
					AMOUNT = 1
				}
			}
		}

		if = {
			limit = { scope:drink = yes }

			scope:actor = {
				POD_drink_from_victim_no_secondary_effect = {
					VICTIM = scope:recipient
					AMOUNT = 2
				}
			}
		}

		if = {
			limit = { scope:alot = yes }

			scope:actor = {
				POD_drink_from_victim_no_secondary_effect = {
					VICTIM = scope:recipient
					AMOUNT = 3
				}
			}
		}

		if = {
			limit = { scope:drain = yes }

			scope:actor = {
				if = { limit = { scope:recipient = { has_main_supernatural_race_trigger = yes } }
					duel = {
						skills = { prowess intrigue }
						target = scope:recipient
						50 = {
							desc = POD_shared_loca_success
							compare_modifier = {
								value = scope:duel_value
							}
							POD_drain_victim_effect = { VICTIM = scope:recipient }
							remove_character_modifier = ritual_feed_the_darkness_modifier
							POD_static_stress_gain = { COUNT = 100 }
						}
						50 = {
							desc = POD_shared_loca_failure
							compare_modifier = {
								value = scope:duel_value
								multiplier = high_negative_duel_skill_multiplier
							}
							scope:actor = {
								add_stress = 125
								add_piety = massive_piety_loss
							}
						}
					}
				}
				else = {
					POD_drain_victim_effect = { VICTIM = scope:recipient }
				}
			}
		}
	}
	auto_accept = yes
}

#POD_call_leviathan_to_war_interaction = {
#	category = interaction_category_vampire
#	icon = alliance
#	desc = POD_call_leviathan_to_war_interaction_desc
#
#	cost = { piety = 500 gold = 50 }
#
#	is_shown = {
#		scope:actor = scope:recipient
#		scope:actor = { has_character_flag = access_to_leviathan }
#	}
#
#	is_valid = {
#		scope:actor = {
#			is_at_war = yes
#		}
#	}
#
#	is_valid_showing_failures_only = {
#		scope:actor = {
#			trigger_if = {
#				limit = { has_variable = leviathan_summon }
#				custom_tooltip = {
#					text = POD_call_leviathan_to_war_interaction_tt
#					NOT = { any_character_war = { this = scope:actor.var:leviathan_summon } }
#				}
#			}
#		}
#		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
#	}
#
#	on_accept = {
#		scope:actor = {
#			random_character_war = { save_temporary_scope_as = war }
#			spawn_army = {
#				men_at_arms = {
#					type = the_leviathan
#					stacks = 1
#				}
#				location = capital_province
#				war = scope:war
#				inheritable = no
#				name = army_of_leviathans
#			}
#			set_variable = { name = leviathan_summon value = scope:war }
#		}
#	}
#
#	auto_accept = yes
#}
