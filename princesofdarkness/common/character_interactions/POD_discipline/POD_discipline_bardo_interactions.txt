
POD_paradox_perk_interaction = {
	category = interaction_category_vampire
	icon = POD_bardo_interaction
	desc = POD_paradox_perk_interaction_desc
	
	cooldown = { months = 1 }
	cost = { piety = major_piety_value }

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_bardo_trigger = yes }
		scope:actor = { has_perk = paradox_perk }
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_blood_for_discipline = { AMOUNT = 1 }
		}
		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}
	
	on_accept = {
		scope:actor = { POD_hunger_rouse_check_effect = yes	}
		scope:actor = {
			random_targeting_faction = {
				destroy_faction = yes
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 72
	ai_will_do = {
		base = 5
	}
}

POD_boon_of_anubis_perk_interaction = {
	category = interaction_category_vampire
	icon = POD_bardo_interaction
	desc = POD_boon_of_anubis_perk_interaction_desc

	cost = { piety = minor_piety_value }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_bardo_trigger = yes }
		scope:actor = { has_perk = boon_of_anubis_perk }
		POD_interaction_is_shown = yes
	}

	is_valid = {
		scope:recipient = {
			trigger_if = {
				limit = { NOT = { has_character_modifier = boon_of_anubis_perk_modifier } }
				NOR = {
					has_trait = vampire
					has_trait = fera
					has_trait = wraith
					has_trait = fae
					has_trait = demon
					has_trait = mummy
					has_trait = kueijin
					has_trait = spirit
				}
			}
		}
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_blood_for_discipline = { AMOUNT = 3 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			scope:actor = { POD_discipline_cost_effect = { COUNT = 3 } }
		}
		scope:recipient = {
			if = {
				limit = { has_character_modifier = boon_of_anubis_perk_modifier }
				remove_character_modifier = boon_of_anubis_perk_modifier
			}
			else = { add_character_modifier = boon_of_anubis_perk_modifier }
		}
	}

	auto_accept = yes

	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
		
		is_knight = no
		number_of_knights > 0
		primary_title.tier > tier_barony
	}
	ai_targets = {
		ai_recipients = peer_vassals
		ai_recipients = courtiers
		max = 10
	}
	ai_frequency = 72
	ai_will_do = {
		base = 0
		
		modifier = {
			add = 100
			scope:recipient = {
				is_knight_of = scope:actor
			}
		}
	}
}

POD_mummification_ritual_perk_interaction = {
	category = interaction_category_vampire
	icon = POD_bardo_interaction
	desc = POD_mummification_ritual_perk_interaction_desc
	
	cooldown = { years = 10 }
	cost = { piety = major_piety_value }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			OR = {
				AND = {
					has_bardo_trigger = yes
					has_perk = mummification_ritual_perk
				}
				uses_cairo_children_main_reward = yes
			}
		}
		POD_hostile_interaction_is_shown = yes
	}
	
	is_valid = {
		custom_description = {
			text = same_location
			scope:actor = { exists = location }
			scope:recipient = { exists = location }
			scope:actor = { location = scope:recipient.location }
		}
	}
	
	is_valid_showing_failures_only = {
		scope:actor = {
			trigger_if = {
				limit = { has_POD_vamp_blood_trigger = yes }
				POD_has_enough_blood_for_discipline = { AMOUNT = 5 }
			}
		}
		scope:recipient = {
			POD_is_not_incorporeal_spirit = yes
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}
	
	on_accept = {
		scope:actor = { POD_willpower_rouse_check_effect = yes }
		scope:actor = {
			if = {
				limit = { has_POD_vamp_blood_trigger = yes }
				POD_discipline_cost_effect = { COUNT = 5 }
			}
			else = { add_piety = -750 }
		}
		scope:actor = {
			duel = {
				skill = intrigue
				target = scope:recipient
				10 = {
					desc = POD_shared_loca_success
					compare_modifier = {
						value = scope:duel_value
						multiplier = high_positive_duel_skill_multiplier
					}
					scope:recipient = {
						POD_enter_torpor_effect = yes
						POD_depose_effect = yes
					}
					scope:actor = {
						send_interface_toast = {
							type = event_generic_neutral
							title = POD_shared_loca_success
							left_icon = scope:recipient
							right_icon = scope:actor
						}
					}
				}
				90 = {
					desc = POD_shared_loca_failure
					compare_modifier = {
						value = scope:duel_value
						multiplier = low_negative_duel_skill_multiplier
					}
					modifier = {
						factor = 0.5
						uses_cairo_children_alternate_reward = yes
					}
					scope:actor = {
						send_interface_toast = {
							type = event_generic_neutral
							title = POD_shared_loca_failure
							left_icon = scope:recipient
							right_icon = scope:actor

						}
					}
				}
			}
		}
		scope:recipient = {
			add_opinion = {
				target = scope:actor
				modifier = hurt_opinion
				opinion = -100
			}
			progress_towards_rival_effect = {
				CHARACTER = scope:actor
				REASON = rival_cast_spell
				OPINION = 0
			}
		}
	}
	
	auto_accept = yes
	
	#ai_potential = {
	#	# The basic requirements
	#	is_imprisoned = no
	#	is_landed = yes
	#}
	#ai_targets = {
	#	ai_recipients = primary_war_enemies
	#}
	ai_frequency = 0
	ai_will_do = {
		base = 0
		
		#modifier = {
		#	add = 45
		#	scope:actor = {
		#	    is_at_war_with = scope:recipient
		#	}
		#}
		#modifier = {
		#	factor = 2
		#	scope:actor = {
		#		has_any_bad_relationship_with_character_trigger = {
		#			CHARACTER = scope:recipient
		#		}
		#	}
		#}
		#modifier = {
		#	factor = 0
		#	scope:recipient = {
		#		is_ai = no
		#	}
		#}
	}
}