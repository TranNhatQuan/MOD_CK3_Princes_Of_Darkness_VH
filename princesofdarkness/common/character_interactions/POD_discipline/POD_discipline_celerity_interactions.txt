POD_blink_interaction = {
	category = interaction_category_vampire
	desc = POD_blink_interaction_desc
	icon = POD_celerity_interaction

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = {
			OR = {
				AND = {
					has_celerity_trigger = yes
					has_perk = blink_perk
				}
			}
		}
	}
	
	cooldown = { years = 5 }

	is_valid_showing_failures_only = {
		scope:actor = {
		   POD_has_enough_energy_for_risen_discipline = { AMOUNT = 1 }
		}
		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			add_character_modifier = {
				modifier = POD_blink_modifier
				years = 1
			}
			POD_risen_discipline_cost_effect = { COUNT = 1 }
			if = {
				limit = {
					NOT = { has_perk = lightning_strike_perk }
				}
				add_character_modifier = {
					modifier = POD_drained_modifier
					years = 5
				}
			}
			if = {
				limit = {
					NOT = { has_perk = split_second_perk }
				}
				add_stress = major_stress_gain
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 36
	ai_will_do = {
		base = 100
	}
}

POD_quickness_decision_interaction = {
	category = interaction_category_vampire
	icon = POD_celerity_interaction
	desc = POD_quickness_decision_interaction_desc

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_celerity_trigger = yes }
		scope:actor = { has_perk = quickness_perk }
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 5 }
		}
		# allow escaping prison
		#POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_willpower_rouse_check_effect = yes
			POD_risen_discipline_cost_effect = { COUNT = 5 }
			increase_wounds_no_death_effect = { REASON = bloodloss }
		}
		scope:recipient = {
			# Artifact
			remove_decision_cooldown = commission_artifact_decision
			remove_decision_cooldown = commission_epic_decision
			remove_decision_cooldown = commission_legend_artifact_decision
			# Trait
			remove_decision_cooldown = deceitful_progress_hostile_schemes_decision
			remove_decision_cooldown = diligent_development_focus_decision
			remove_decision_cooldown = impatient_rushing_all_schemes_decision
			# Religion
			remove_decision_cooldown = seek_aid_of_the_spirits_decision
			remove_decision_cooldown = meditate_in_seclusion_decision
			remove_decision_cooldown = divine_the_stars_decision
			# Prison
			remove_decision_cooldown = escape_from_prison_decision
			# Court
			remove_decision_cooldown = hold_court_decision
			remove_decision_cooldown = petition_liege_decision
			# Pet
			remove_decision_cooldown = pet_dog_decision
			remove_decision_cooldown = pet_cat_decision
			# Stess
			remove_decision_cooldown = stress_loss_drunkard_decision
			remove_decision_cooldown = stress_loss_hashishiyah_decision
			remove_decision_cooldown = stress_loss_rakish_decision
			remove_decision_cooldown = stress_loss_reclusive_decision
			remove_decision_cooldown = stress_loss_irritable_decision
			remove_decision_cooldown = stress_loss_flagellant_decision
			remove_decision_cooldown = stress_loss_profligate_decision
			remove_decision_cooldown = stress_loss_improvident_decision
			remove_decision_cooldown = stress_loss_contrite_decision
			remove_decision_cooldown = stress_loss_comfort_eater_decision
			remove_decision_cooldown = stress_loss_inappetetic_decision
			remove_decision_cooldown = stress_loss_journaller_decision
			remove_decision_cooldown = stress_loss_confider_decision
			remove_decision_cooldown = stress_loss_athletic_decision
			# Pod
			remove_decision_cooldown = host_elysium_decision
			remove_decision_cooldown = survey_capital_decision
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
		
		any_scheme = { exists = yes }
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 48
	ai_will_do = {
		base = 5
	}
}

POD_quickness_scheme_interaction = {
	category = interaction_category_vampire
	icon = POD_celerity_interaction
	desc = POD_quickness_scheme_interaction_desc
	
	cost = { piety = 500 }
	cooldown = { years = 1 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_celerity_trigger = yes }
		scope:actor = { has_perk = quickness_perk }
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 5 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_willpower_rouse_check_effect = yes
			POD_risen_discipline_cost_effect = { COUNT = 5 }
			increase_wounds_no_death_effect = { REASON = bloodloss }
		}
		scope:actor = {
			# Hostile
			remove_scheme_cooldown_against = { scheme = abduct target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = claim_throne target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = overthrow_regent target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = fabricate_hook target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = murder target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = steal_back_artifact target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = diablerize target = scope:recipient }
			# Personal
			remove_scheme_cooldown_against = { scheme = befriend target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = convert_to_witchcraft target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = courting target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = elope target = scope:recipient }
			remove_scheme_cooldown_against = { scheme = learn_language target = scope:recipient }
			#remove_scheme_cooldown_against = { scheme = seduce target = scope:recipient }
			#remove_scheme_cooldown_against = { scheme = sway target = scope:recipient }
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
		
		any_scheme = { exists = yes }
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 48
	ai_will_do = {
		base = 5
	}
}

POD_time_out_of_time_interaction = {
	category = interaction_category_vampire
	icon = POD_celerity_interaction
	desc = POD_time_out_of_time_interaction_desc
	
	cost = { piety = 500 }
	cooldown = { years = 1 }

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_celerity_trigger = yes }
		scope:actor = { has_perk = time_out_of_time_perk }
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 5 }
		}
		scope:recipient = {
			custom_description = {
				text = "impatient_rushing_all_schemes_decision_missing_scheme"
				any_scheme = {
					count > 0
				}
			}
		}
		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = { POD_willpower_rouse_check_effect = yes }
		scope:actor = {
			POD_risen_discipline_cost_effect = { COUNT = 5 }
		}
		scope:recipient = {
			every_scheme = {
				change_opportunities = 5
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
		
		any_scheme = { exists = yes }
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 48
	ai_will_do = {
		base = 5
	}
}

POD_between_the_ticks_interaction = {
	category = interaction_category_vampire
	icon = POD_celerity_interaction
	desc = POD_between_the_ticks_interaction_desc

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = { has_celerity_trigger = yes }
		scope:actor = { has_perk = between_the_ticks_perk }
	}
	
	cooldown = { years = 5 }

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 5 }
		}
		POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_willpower_rouse_check_effect = yes
			POD_risen_discipline_cost_effect = { COUNT = 5 }
			add_character_modifier = {
				modifier = POD_between_the_ticks_modifier
				months = 3
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 36
	ai_will_do = {
		base = 0
		
		modifier = {
			add = 100
			scope:actor = {
				is_at_war = yes
			}
		}
	}
}
