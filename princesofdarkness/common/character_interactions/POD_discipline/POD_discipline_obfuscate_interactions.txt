POD_mask_of_a_thousand_faces_interaction = {
	category = interaction_category_vampire
	desc = POD_mask_of_a_thousand_faces_interaction_desc
	icon = POD_obfuscate_interaction
	
	is_shown = {
		OR = {
			scope:actor != scope:recipient
			scope:actor = { has_variable = original_face }
		}
		scope:actor = {
			OR = {
				AND = {
					has_obfuscate_trigger = yes
					has_perk = mask_of_a_thousand_faces_perk
				}
				any_equipped_character_artifact = { has_variable = pale_jade_mask_variable }
			}
		}
		POD_hostile_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 1 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}
	
	send_option = {
		is_shown = {
			scope:actor = { has_variable = original_face }
		}
		is_valid = {
			scope:actor = scope:recipient
		}
		flag = restore_face
		localization = POD_vampshapeshift_interaction_restore_face
	}
	
	send_option = {
		is_shown = {
			scope:recipient = { has_variable = original_face }
			scope:actor != scope:recipient
		}
		flag = restore_face_recipient
		localization = POD_vampshapeshift_interaction_restore_face_recipient
	}
	
	send_option = {
		is_shown = {
			scope:actor != scope:recipient
		}
		flag = copy_face
		localization = POD_vampshapeshift_interaction_copy_face
	}
	
	send_option = {
		is_shown = {
			scope:actor != scope:recipient
		}
		is_valid = {
			scope:actor = { has_perk = mask_of_janus_perk }
		}
		flag = paste_face
		localization = POD_vampshapeshift_interaction_paste_face
	}

	on_accept = {
		scope:actor = {
			POD_risen_discipline_cost_effect = { COUNT = 1 }
		}
		if = {
			limit = {
				scope:copy_face = yes
			}
			scope:actor = {
				hidden_effect = {
					if = {
						limit = { NOT = { has_variable = original_face } }
						POD_create_dummyclone_effect = { CHARACTER = scope:actor }
						if = {
							limit = { exists = scope:dummyclone }
							scope:dummyclone = { copy_inheritable_appearance_from = scope:actor }
							set_variable = {
								name = original_face
								value = scope:dummyclone
							}
						}
						else = {
							send_interface_toast = {
								title = POD_kueijin_spy.duel.failure
								left_icon = scope:actor
								right_icon = scope:recipient
							}
						}
					}
				}
				copy_inheritable_appearance_from = scope:recipient
			}
		}
		if = {
			limit = {
				scope:paste_face = yes
			}
			scope:recipient = {
				copy_inheritable_appearance_from = scope:actor
				hidden_effect = {
					if = {
						limit = { NOT = { has_variable = original_face } }
						create_character = {
							age = scope:recipient.effective_age
							faith = scope:recipient.faith
							gender = scope:recipient
							random_traits = no
							dynasty = none
							employer = character:POD_wraith_01
							trait = wraith
							save_scope_as = new_created_character
						}
						set_variable = {
							name = original_face
							value = scope:new_created_character
						}
					}
				}
				add_opinion = {
					target = scope:actor
					modifier = hurt_opinion
					opinion = -10
				}
			}
		}
		if = {
			limit = {
				scope:restore_face = yes
			}
			scope:actor = {
				copy_inheritable_appearance_from = var:original_face
				remove_variable = original_face
			}
		}
		if = {
			limit = {
				scope:restore_face_recipient = yes
			}
			scope:recipient = {
				copy_inheritable_appearance_from = var:original_face
				remove_variable = original_face
			}
		}
	}
	
	auto_accept = yes
	
	ai_will_do = {
		base = 0
	}
}

POD_vanish_interaction = {
	category = interaction_category_vampire
	desc = POD_vanish_interaction_desc
	icon = POD_obfuscate_interaction

	is_shown = {
		scope:actor = scope:recipient
		scope:actor = {
			OR = {
				AND = {
					has_obfuscate_trigger = yes
					has_perk = vanish_perk
				}
			}
		}
	}
	
	cooldown = { years = 5 }

	cost = { prestige = 100 }

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 1 }
		}
		# allow for prisoners
		#POD_self_targeted_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_risen_discipline_cost_effect = { COUNT = 1 }
			custom_tooltip = POD_vanish_tt
			hidden_effect = {
				every_targeting_scheme = {
					end_scheme = yes
				}
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
		
		any_targeting_scheme = {
			hostile_scheme_trigger = yes
			is_scheme_exposed = yes
		}
	}
	ai_targets = {
		ai_recipients = self
	}
	ai_frequency = 72
	ai_will_do = {
		base = 100
	}
}

POD_silence_of_death_interaction = {
	category = interaction_category_vampire
	desc = POD_silence_of_death_interaction_desc
	icon = POD_obfuscate_interaction

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = {
			OR = {
				AND = {
					has_obfuscate_trigger = yes
					has_perk = silence_of_death_perk
				}
			}
		}
		POD_hostile_interaction_is_shown = yes
	}
	
	cooldown = { years = 5 }

	cost = { piety = 100 }

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 1 }
		}
		scope:recipient = { is_playable_character = yes }
		scope:recipient = { is_imprisoned = no }
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_risen_discipline_cost_effect = { COUNT = 1 }
		}
		scope:recipient = {
			add_character_modifier = {
				modifier = POD_silence_death_modifier
				years = 2
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		# The basic requirements
		is_imprisoned = no
		is_landed = yes
	}
	ai_targets = {
		ai_recipients = primary_war_enemies
	}
	ai_frequency = 72
	ai_will_do = {
		base = 0
		
		modifier = {
			add = 45
			scope:actor = {
				is_at_war_with = scope:recipient
			}
		}
		modifier = {
			factor = 2
			scope:actor = {
				has_any_bad_relationship_with_character_trigger = {
					CHARACTER = scope:recipient
				}
			}
		}
		modifier = {
			factor = 0
			scope:recipient = {
				is_ai = no
			}
		}
	}
}

POD_old_friend_interaction = {
	category = interaction_category_vampire
	icon = POD_obfuscate_interaction
	desc = POD_old_friend_interaction_desc
	
	cooldown = { years = 5 }
	
	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_obfuscate_trigger = yes }
		scope:actor = { has_perk = old_friend_perk }
		scope:actor = { can_set_relation_friend_trigger = { CHARACTER = scope:recipient } }
		POD_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 3 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}

	on_accept = {
		scope:actor = {
			POD_risen_discipline_cost_effect = { COUNT = 3 }
		}
		scope:actor = {
			duel = {
				skills = { diplomacy intrigue }
				target = scope:recipient
				50 = {
					desc = POD_shared_loca_success
					compare_modifier = {
						value = scope:duel_value
						multiplier = high_positive_duel_skill_multiplier
					}
					set_relation_friend = scope:recipient
					min = 1
				}
				50 = {
					desc = POD_shared_loca_failure
					compare_modifier = {
						value = scope:duel_value
						multiplier = low_negative_duel_skill_multiplier
					}
					scope:actor = {
						add_stress = medium_stress_impact_gain
						add_piety = medium_piety_loss
					}
					min = 1
				}
			}
		}
	}

	auto_accept = yes
	
	ai_will_do = {
		base = 0
	}
}

POD_create_name_interaction = {
	category = interaction_category_vampire
	icon = POD_obfuscate_interaction
	desc = POD_create_name_interaction_desc

	cost = { piety = 500 }

	is_shown = {
		scope:actor != scope:recipient
		scope:actor = { has_obfuscate_trigger = yes }
		scope:actor = { has_perk = create_name_perk }
		POD_hostile_mental_interaction_is_shown = yes
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			POD_has_enough_energy_for_risen_discipline = { AMOUNT = 5 }
		}
		POD_interaction_is_valid_showing_failures_only = yes
	}
	
	on_accept = {
		scope:actor = {
			POD_risen_discipline_cost_effect = { COUNT = 5 }
		}
		scope:recipient = {
			if = {
				limit = { has_hook = scope:actor }
				remove_hook = { target = scope:actor }
				add_opinion = {
					target = scope:actor
					modifier = lost_strong_hook_opinion
				}
			}
			if = {
				limit = { has_truce = scope:actor }
				cancel_truce_both_ways = scope:actor
			}
			if = {
				limit = { has_relation_friend = scope:actor }
				remove_relation_friend = scope:actor
			}
			if = {
				limit = { has_relation_lover = scope:actor }
				remove_relation_lover = scope:actor
			}
			if = {
				limit = { has_relation_rival = scope:actor }
				remove_relation_rival = scope:actor
			}
			if = {
				limit = { is_consort_of = scope:actor }
				divorce = scope:actor
			}
			scope:actor = {
				every_held_title = {
					limit = { scope:recipient = { has_claim_on = prev } }
					scope:recipient = { remove_claim = prev }
				}
				every_character_artifact = {
					limit = { scope:recipient = { has_artifact_claim = prev } }
					scope:recipient = { remove_personal_artifact_claim = prev }
				}
			}
			scope:recipient = {
				random_unspent_known_secret = {
					limit = { secret_owner = scope:actor }
					disable_exposure_by = scope:recipient
				}
			}
		}
	}
	
	auto_accept = yes
	
	ai_frequency = 0
	ai_will_do = {
		base = 0
	}
}