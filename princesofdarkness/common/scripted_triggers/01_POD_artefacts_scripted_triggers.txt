POD_artifact_is_unavailable_trigger = {
	has_variable = unavailable_artifact
}

# Whether the artifact is literally indestructible
POD_artifact_is_destructible_trigger = {
	always = yes
}

# Whether the artifact *should* be able to get damaged in events
POD_artifact_is_destructible_by_events_trigger = {
	POD_artifact_is_destructible_trigger = yes
	NOR = {
		has_variable = historical_unique_artifact
		artifact_type = animal
		AND = {
			has_variable = fetter_owner
			var:fetter_owner = artifact_owner
		}
	}
}

POD_artifact_can_be_copied_trigger = {
	NOT = { has_variable = summoned_artifact }
	NOT = { has_variable = temporary_vessel }
	NOT = { has_variable = wraith_jade }
	NOT = { artifact_type = animal }
}

cant_frenzy_trigger = {
	OR = {
		any_equipped_character_artifact = {
			OR = {
				has_variable = blade_of_enoch
				has_variable = hand_nikolas
			}	
		}
		has_trait_with_flag = immune_to_frenzy_flag
		has_character_modifier = ritual_purge_the_inner_demon_modifier
		has_character_flag = suppressed_beast_flag
		has_character_modifier = armor_of_diamond_serenity_modifier
		#has_character_flag = immune_to_frenzy # from old bloodsorcery
	}
}

mythical_duel_items_trigger = {
	any_equipped_character_artifact = {
		OR = {
			has_variable = sword_of_nul
			has_variable = ainkurn
			has_variable = blade_of_enoch
			has_variable = sword_of_troile
		}
	}	
}	

illustrious_duel_items_trigger = {
	any_equipped_character_artifact = {
		OR = {
			has_variable = kristrbani
			has_variable = flagrum_passion
			has_variable = ayr_sword
			has_variable = sword_arthur	
			has_variable = dagger_thorns
			has_variable = spear_destiny
			has_variable = spear_bathel											
		}
	}	
}
	
illustrious_armor_items_trigger = {
	any_equipped_character_artifact = {
		has_variable = ayr_armor
	}
}	

masterwork_duel_items_trigger = {
	any_equipped_character_artifact = {
		OR = {
			has_variable = slay_death
			has_variable = jurgen_broadsword
			has_variable = rowan_ring
			has_variable = pacifist_ji	
			has_variable = gauthier_sword
			has_variable = enchanted_scimitar
		}
	}	
}