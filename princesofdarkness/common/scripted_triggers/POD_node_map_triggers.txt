# Checks if any line exists containing the pair of given nodes
POD_line_exists_between_nodes = {
	$NODE1$ = {
		has_variable_list = lines
		any_in_list = {
			variable = lines
			OR = {
				var:start_node = $NODE2$
				var:end_node = $NODE2$
			}
		}
	}
}
