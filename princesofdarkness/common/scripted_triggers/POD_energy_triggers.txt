# The purpose of these triggers is to decouple supernatural abilities from any specific splat.
# For example, Gifts have a Gnosis cost.
# But since the triggers are splat-agnostic, any other resource can be used in its place.
# (e.g. Blood for Abominations)

# also see the priority list in common\scripted_effects\POD_energy_effects.txt


### Energy priority list

# priority 1
POD_splat_has_hunger_exclusively = {
	POD_splat_has_hunger_trigger = yes
}

# priority 2
POD_splat_has_angst_exclusively = {
	POD_splat_has_hunger_trigger = no
	POD_splat_has_angst_trigger  = yes
}

# priority 3
POD_splat_has_pathos_exclusively = {
	POD_splat_has_hunger_trigger = no
	POD_splat_has_angst_trigger  = no
	POD_splat_has_pathos_trigger = yes
}

# priority 4
POD_splat_has_resolve_exclusively = {
	POD_splat_has_hunger_trigger  = no
	POD_splat_has_angst_trigger   = no
	POD_splat_has_pathos_trigger  = no
	POD_splat_has_resolve_trigger = yes
}

# priority 5
POD_splat_has_gnosis_exclusively = {
	POD_splat_has_hunger_trigger  = no
	POD_splat_has_angst_trigger   = no
	POD_splat_has_pathos_trigger  = no
	POD_splat_has_resolve_trigger = no
	POD_splat_has_gnosis_trigger  = yes
}

# priority 6
POD_splat_has_glamour_exclusively = {
	POD_splat_has_hunger_trigger  = no
	POD_splat_has_angst_trigger   = no
	POD_splat_has_pathos_trigger  = no
	POD_splat_has_resolve_trigger = no
	POD_splat_has_gnosis_trigger  = no
	POD_splat_has_glamour_trigger = yes
}

# priority 7
POD_splat_has_dread_as_resource_exclusively = {
	POD_splat_has_hunger_trigger  = no
	POD_splat_has_angst_trigger   = no
	POD_splat_has_pathos_trigger  = no
	POD_splat_has_resolve_trigger = no
	POD_splat_has_gnosis_trigger  = no
	POD_splat_has_glamour_trigger = no
	POD_splat_has_dread_as_resource_trigger = yes
}

# priority 8
POD_splat_has_willpower_as_resource_exclusively = {
	POD_splat_has_hunger_trigger  = no
	POD_splat_has_angst_trigger   = no
	POD_splat_has_pathos_trigger  = no
	POD_splat_has_resolve_trigger = no
	POD_splat_has_gnosis_trigger  = no
	POD_splat_has_glamour_trigger = no
	POD_splat_has_dread_as_resource_trigger = no
}


### Triggers for energy type assignment

POD_splat_has_hunger_trigger = {
	OR = {
		has_POD_vamp_blood_trigger = yes
		has_trait = entomos
	}
}

POD_splat_has_angst_trigger = {
	has_trait = spectre
}

POD_splat_has_pathos_trigger = {
	has_trait = wraith
}

POD_splat_has_resolve_trigger = {
	POD_is_demon_trigger = yes
}

POD_splat_has_gnosis_trigger = {
	OR = {
		has_trait = fera
		has_trait = gnosis # ww abomination get this trait in embrace
	}
}

POD_splat_has_glamour_trigger = {
	OR = {
		has_trait = fae
		has_trait = changeling
		has_trait = kinain
	}
}

POD_splat_has_dread_as_resource_trigger = {
	has_character_modifier = yama_king_modifier
}


### Triggers for specific splats (wrappers)

# TODO: AMOUNT and COUNT are used inconsistently

POD_has_enough_splat_agnostic_energy_reserves = {
	POD_has_enough_energy_max_10 = { AMOUNT = $AMOUNT$ }
}

POD_has_enough_blood_for_discipline = {
	POD_has_enough_energy_max_5 = { AMOUNT = $AMOUNT$ }
}

POD_has_enough_energy_for_risen_discipline = {
	POD_has_enough_energy_max_5 = { AMOUNT = $AMOUNT$ }
}

POD_has_enough_resolve_for_power = {
	POD_has_enough_energy_max_10 = { AMOUNT = $COUNT$ }
}

POD_has_enough_energy_for_lore = {
	POD_has_enough_energy_max_10 = { AMOUNT = $COUNT$ }
}

POD_has_enough_glamour_for_fae_art = {
	POD_has_enough_energy_max_5 = { AMOUNT = $AMOUNT$ }
}

POD_has_enough_pathos_for_arcanos = {
	POD_has_enough_energy_max_5 = { AMOUNT = $AMOUNT$ }
}

POD_has_enough_gnosis_for_gift = {
	POD_has_enough_energy_max_5 = { AMOUNT = $COUNT$ }
}

# static stress for kuei-jin
POD_has_enough_chi_for_kj_art = {
	# bandaid fix to allow arhats to use 100 mental strain abilities without losing dharma
	# no longer necessary if KJ get a real resource again
	save_temporary_scope_value_as = {
		name = chi_amount
		value = {
			value = $AMOUNT$
			multiply = chi_cost_multiplier_from_dharma
			ceiling = yes
		}
	}
	POD_has_enough_energy_max_10 = { AMOUNT = scope:chi_amount }
}

# static stress for mummies
POD_has_enough_sekhem_for_hekau = {
	POD_has_enough_energy_max_10 = { AMOUNT = $AMOUNT$ }
}

# static stress for wu'tian
POD_has_enough_chi_for_tan = {
	POD_has_enough_energy_max_10 = { AMOUNT = $AMOUNT$ }
}

# static stress for shih
POD_has_enough_chi_for_qiao = {
	# bandaid fix to allow arhats to use 100 mental strain abilities without losing dharma
	# (dhampyr shih have dharma traits.... sometimes)
	# no longer necessary if KJ get a real resource again
	save_temporary_scope_value_as = {
		name = chi_amount
		value = {
			value = $AMOUNT$
			multiply = chi_cost_multiplier_from_dharma
			ceiling = yes
		}
	}
	POD_has_enough_energy_max_10 = { AMOUNT = scope:chi_amount }
}

# dread for yama kings
POD_has_enough_essence_for_panoply = {
	POD_has_enough_energy_max_10 = { AMOUNT = $AMOUNT$ }
}

# static stress for mortals
POD_has_enough_energy_for_sorcery = {
	POD_has_enough_energy_max_10 = { AMOUNT = $AMOUNT$ }
}

# TODO: move other energy triggers here


### Other energy triggers

# for is_valid_showing_failures_only (text only shows up if negated)
POD_is_not_full_on_energy = {
	custom_tooltip = {
		text = POD_is_full_on_energy
		splat_agnostic_energy_deficit > 0
	}
}

POD_is_not_full_on_energy_including_vessels = {
	custom_tooltip = {
		text = POD_is_full_on_energy
		splat_agnostic_energy_deficit_including_vessels > 0
	}
}


### Triggers for selecting the right energy type per splat
# (DO NOT USE THESE OUTSIDE THIS FILE, use the wrappers instead)

POD_has_enough_energy_max_5 = {
	save_temporary_scope_value_as = {
		name = amount_max_10
		value = {
			value = $AMOUNT$
			multiply = 2
		}
	}
	POD_has_enough_energy = {
		AMOUNT_MAX_5  = $AMOUNT$
		AMOUNT_MAX_10 = scope:amount_max_10
	}
}

POD_has_enough_energy_max_10 = {
	save_temporary_scope_value_as = {
		name = amount_max_5
		value = {
			value = $AMOUNT$
			multiply = 0.5
			ceiling = yes
		}
	}
	POD_has_enough_energy = {
		AMOUNT_MAX_5  = scope:amount_max_5
		AMOUNT_MAX_10 = $AMOUNT$
	}
}

POD_has_enough_energy = {
	trigger_if = {
		limit = { POD_splat_has_hunger_trigger = yes } # max 5
		POD_has_enough_energy_as_blood = { AMOUNT = $AMOUNT_MAX_5$ }
	}
	trigger_else_if = {
		limit = { POD_splat_has_angst_trigger = yes } # max 5
		POD_has_enough_energy_as_angst = { AMOUNT = $AMOUNT_MAX_5$ }
	}
	trigger_else_if = {
		limit = { POD_splat_has_pathos_trigger = yes } # max 5
		POD_has_enough_energy_as_pathos = { AMOUNT = $AMOUNT_MAX_5$ }
	}
	trigger_else_if = {
		limit = { POD_splat_has_resolve_trigger = yes } # max 10
		POD_has_enough_energy_as_resolve = { AMOUNT = $AMOUNT_MAX_10$ }
	}
	trigger_else_if = {
		limit = { POD_splat_has_gnosis_trigger = yes } # max 5
		POD_has_enough_energy_as_gnosis = { AMOUNT = $AMOUNT_MAX_5$ }
	}
	trigger_else_if = {
		limit = { POD_splat_has_glamour_trigger = yes } # max 5
		POD_has_enough_energy_as_glamour = { AMOUNT = $AMOUNT_MAX_5$ }
	}
	trigger_else_if = {
		limit = { POD_splat_has_dread_as_resource_trigger = yes } # scaled to max 10 for simplicity
		POD_has_enough_energy_as_dread = { AMOUNT = $AMOUNT_MAX_10$ }
	}
	trigger_else = {
		# static stress as fallback (scaled to max 10 for simplicity)
		POD_has_enough_energy_as_willpower = { AMOUNT = $AMOUNT_MAX_10$ }
	}
}


### Base triggers for checking a splat's energy reserves
# (DO NOT USE THESE OUTSIDE THIS FILE, use the wrappers instead)

# using a scale of 0-5
POD_has_enough_energy_as_blood = {
	trigger_if = {
		limit = { POD_has_infinite_blood_trigger = yes }
		always = yes
	}
	trigger_else = {
		custom_description = {
			text = POD_has_total_blood_reserves
			value = $AMOUNT$
			total_blood_reserves >= $AMOUNT$
		}
	}
}

# using a scale of 0-5
POD_has_enough_energy_as_angst = {
	custom_description = {
		text = POD_has_total_angst_reserves
		value = $AMOUNT$
		total_angst_reserves >= $AMOUNT$
	}
}

# using a scale of 0-5
POD_has_enough_energy_as_pathos = {
	custom_description = {
		text = POD_has_total_pathos_reserves
		value = $AMOUNT$
		total_pathos_reserves >= $AMOUNT$
	}
}

# using a scale of 0-10
POD_has_enough_energy_as_resolve = {
	custom_description = {
		text = POD_has_total_resolve_reserves
		value = $AMOUNT$
		total_resolve_reserves >= $AMOUNT$
	}
}

# using a scale of 0-5
POD_has_enough_energy_as_gnosis = {
	custom_description = {
		text = POD_has_total_gnosis_reserves
		value = $AMOUNT$
		total_gnosis_reserves >= $AMOUNT$
	}
}

# using a scale of 0-5
POD_has_enough_energy_as_glamour = {
	custom_description = {
		text = POD_has_total_glamour_reserves
		value = $AMOUNT$
		total_glamour_reserves >= $AMOUNT$
	}
}

# using a scale of 0-10, e.g. AMOUNT = 4 means 40 dread
POD_has_enough_energy_as_dread = {
	save_temporary_scope_value_as = {
		name = dread_overflow
		value = {
			value = $AMOUNT$
			multiply = 10 # each "energy point" equivalent to 10 dread
			subtract = total_stored_splat_energy
			min = 0
		}
	}
	dread >= scope:dread_overflow # TODO: check if this one's missing vanilla loc too
}

# using a scale of 0-10, e.g. AMOUNT = 4 means 40 static_stress
POD_has_enough_energy_as_willpower = {
	save_temporary_scope_value_as = {
		name = willpower_overflow
		value = {
			value = $AMOUNT$
			multiply = 10 # each "energy point" equivalent to 10 stress
			subtract = total_stored_splat_energy
			min = 0
		}
	}
	trigger_if = {
		limit = { scope:willpower_overflow > 0 }
		save_temporary_scope_value_as = {
			name = max_allowed_stress
			value = {
				value = stress_cap_for_willpower
				subtract = scope:willpower_overflow
			}
		}
		stress < scope:max_allowed_stress
	}
	trigger_else = {
		always = yes
	}
}