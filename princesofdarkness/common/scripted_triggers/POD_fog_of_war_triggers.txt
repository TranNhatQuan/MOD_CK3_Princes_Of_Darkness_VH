POD_is_default_fog_of_war_enabled = {
	always = no
	#has_game_rule = pod_fog_of_war_enabled # TODO: hide for now
}

POD_is_custom_fog_of_war_enabled = {
	has_game_rule = pod_fog_of_war_custom
}

POD_show_random_outcomes_as_percentage = {
	trigger_if = {
		limit = { POD_is_custom_fog_of_war_enabled = yes }
		has_game_rule = pod_random_outcomes_percentages
	}
	trigger_else = { POD_is_default_fog_of_war_enabled = no }
}

### unnecessary with the way PODRandomOutcome is set up
# POD_show_random_outcomes_as_descriptions = {
# 	trigger_if = {
# 		limit = { POD_is_custom_fog_of_war_enabled = yes }
# 		has_game_rule = pod_random_outcomes_immersive
# 	}
# 	trigger_else = { POD_is_default_fog_of_war_enabled = yes }
# }

POD_show_random_outcomes_as_uncertain = {
	trigger_if = {
		limit = { POD_is_custom_fog_of_war_enabled = yes }
		has_game_rule = pod_random_outcomes_uncertain
	}
	trigger_else = { always = no }
}