camarilla_created = {
	scope = character

	is_shown = { POD_reached_endgame_date_trigger = { YEAR = 1517 } }
	is_valid = {}
	effect = {}
}

camarilla_meeting_tutorial = {
	scope = character

	is_shown = {
	}
	is_valid = {
	}
	effect = {
		trigger_event = POD_camarilla.999
	}
}

caitiff_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			OR = {
				is_target_in_variable_list = { 
					name = member_clan
					target = doctrine:doctrine_vampirethinblood_justicar
				}
				is_target_in_variable_list = { 
					name = member_clan
					target = doctrine:doctrine_vampirethinblood_accepted
				}
			}
		}
	}
}

ventrue_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = ventrue }
		}
	}
}

toreador_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = toreador }
		}
	}
}
			
tremere_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = tremere }
		}
	}
}

brujah_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = brujah }
		}
	}
}

nosferatu_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = nosferatu }
		}
	}
}

gangrel_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = gangrel }
		}
	}
}

malkavian_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = malkavian }
		}
	}
}

lasombra_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = lasombra }
		}
	}
}

tzimisce_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = tzimisce }
		}
	}
}

ravnos_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = ravnos }
		}
	}
}

hecata_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = hecata }
		}
	}
}

setite_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = setite }
		}
	}
}

assamite_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = assamite }
		}
	}
}

salubri_is_member = {
	scope = faith
	is_valid = {
		AND = {
			has_variable_list = member_clan
			POD_is_member_clan_trigger = { CLAN = salubri }
		}
	}
}

red_list_blood_hunt_war = {
	scope = character
	saved_scopes = { hunted }

	is_shown = {
	}
	is_valid = {
		NOT = { is_at_war_with = scope:hunted }
		scope:hunted = { is_imprisoned = no }
		scope:hunted = { is_landed = yes }
	}
	effect = {
		start_war = {
			cb = POD_blood_hunt_war
			target = scope:hunted
		}
	}
}

red_list_blood_hunt_duel = {
	scope = character
	saved_scopes = { hunted }

	is_shown = {
	}
	is_valid = {
		scope:hunted = { is_landed = no }
	}
	effect = {
		custom_tooltip = fp1_jomsvikings.0041.a.tt
		configure_start_single_combat_effect = {
			SC_INITIATOR = root
			SC_ATTACKER = root
			SC_DEFENDER = scope:hunted
			FATALITY = always
			FIXED = no
			LOCALE = terrain_scope
			OUTPUT_EVENT = single_combat.1006
			INVALIDATION_EVENT = tribal.1022
		}
	}
}

red_list_add_bounty = {
	scope = character
	saved_scopes = { hunted }

	is_shown = {
	}
	is_valid = {
		gold >= medium_gold_value
	}
	effect = {
		custom_tooltip = POD_camarilla_add_bounty_interaction
		open_interaction_window = {
			interaction = POD_camarilla_add_bounty_interaction
			actor = root
			recipient = scope:hunted
		}
	}
}

justicar_lift_anathema = {
	scope = character
	saved_scopes = { justicar }

	is_shown = {
	}
	is_valid = {
		has_trait = anathema
		POD_is_red_list_trigger = no
	}
	effect = {
		custom_tooltip = POD_lift_anathema_interaction
		open_interaction_window = {
			interaction = POD_lift_anathema_interaction
			actor = root
			recipient = scope:justicar
		}
	}
}

inner_circle_visible = {
	scope = character

	is_shown = {
		camarilla_status >= 7 # Justicar and Inner Circle
	}
	is_valid = {
	}
	effect = {
	}
}