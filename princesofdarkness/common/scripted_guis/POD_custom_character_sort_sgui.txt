POD_customsort_toggle_pinned_characters = {
	scope = character
	effect = {
		# TODO: package all the sorting variables in an inheritable story_cycle
		POD_toggle_variable = { VAR = customsort_pinned_only }
		POD_sort_event_character_list = yes
	}
}