tremere_council_enable = {
	scope = character
	is_shown = {
		POD_is_tremere_main_faith_trigger = yes
		is_tremere_trigger = yes
	}
}

council_session = {
	scope = character 
	#saved_scopes = {
	#	player
	#}
	effect = {
		add_character_flag = {
			flag = council_session_done_flag
			years = 20
		}
		trigger_event = pod_tremere_council.12
	}
	is_valid = {
		NOT = {
			has_character_flag = council_session_done_flag
		}
		has_variable = influence_inner_council
	}
}