# Root is the target, need to supply the player as scope:actor
is_ventrue_clan_curse_applicable_gui = {
	scope = character
	is_shown = {
		is_alive = yes
		POD_is_not_apocalyptic_background_character = yes
		
		OR = {
			POD_can_be_herd_trigger = yes
			POD_is_vampire_trigger = yes
		}
		
		scope:actor ?= {
			is_ventrue_trigger = yes
		}
	}
}

# Root is the target, need to supply the player as scope:actor
fits_feeding_restrictions_gui = {
	scope = character
	saved_scopes = { actor }
	is_shown = {
		ventrue_weakness_compatible_trigger = {
			ACTOR = scope:actor
			RECIPIENT = root
		}
	}
}

scry_the_soul_sgui = {
	scope = character
	is_shown = {
		root ?= {
			OR = {
				has_perk = scry_the_soul_perk
			}
		}
	}
}