activity_invite_rule_coterie_members = {
	effect = {
		var:coterie ?= {
			every_in_list = {
				variable = members
				limit = {
					NOT = { this = root }
				}
				add_to_list = characters
			}
		}
	}
}