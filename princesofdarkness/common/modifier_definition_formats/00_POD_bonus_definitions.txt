# POD schemes

PODbloodbond_scheme_phase_duration_add = {
	prefix = MOD_SPEED_PREFIX
	suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_POSITIVE
	negative_suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_NEGATIVE
	no_difference_sign = yes
	color = bad
	decimals = 0
}

diablerize_scheme_phase_duration_add = {
	prefix = MOD_SPEED_PREFIX
	suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_POSITIVE
	negative_suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_NEGATIVE
	no_difference_sign = yes
	color = bad
	decimals = 0
}

PODentrance_scheme_phase_duration_add = {
	prefix = MOD_SPEED_PREFIX
	suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_POSITIVE
	negative_suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_NEGATIVE
	no_difference_sign = yes
	color = bad
	decimals = 0
}

PODsummon_scheme_phase_duration_add = {
	prefix = MOD_SPEED_PREFIX
	suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_POSITIVE
	negative_suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_NEGATIVE
	no_difference_sign = yes
	color = bad
	decimals = 0
}

# Enemy schemes

PODbloodbond_enemy_scheme_phase_duration_add = {
	prefix = MOD_SPEED_PREFIX
	suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_POSITIVE
	negative_suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_NEGATIVE
	no_difference_sign = yes
	decimals = 0
}

diablerize_enemy_scheme_phase_duration_add = {
	prefix = MOD_SPEED_PREFIX
	suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_POSITIVE
	negative_suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_NEGATIVE
	no_difference_sign = yes
	decimals = 0
}

# Schemes against scripted relations

scheme_phase_duration_against_coterie_member_add = {
	prefix = MOD_SPEED_PREFIX
	suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_POSITIVE
	negative_suffix = MOD_DAYS_PER_SCHEME_PHASE_SUFFIX_NEGATIVE
	no_difference_sign = yes
	color = bad
	decimals = 0
}

# Misc

max_PODsummon_schemes_add = {
	decimals = 0
}

mummy_xp_gain_mult = {
	decimals = 0
	percent = yes
}
mummy_xp_loss_mult = {
	decimals = 0
	color = bad
	percent = yes
}
integrity_xp_gain_mult = {
	decimals = 0
	percent = yes
}
integrity_xp_loss_mult = {
	decimals = 0
	color = bad
	percent = yes
}

trait_track_forging_xp_gain_mult = {
	decimals = 0
	percent = yes
}
trait_track_brewing_xp_gain_mult = {
	decimals = 0
	percent = yes
}
trait_track_enhancing_xp_gain_mult = {
	decimals = 0
	percent = yes
}