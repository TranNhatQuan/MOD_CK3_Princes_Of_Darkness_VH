
vbox = {
	name = "POD_event_window_widget_character_filters"
	layoutpolicy_horizontal = expanding
	
	vbox = {
		spacing = 12
		margin_bottom = 12
		layoutpolicy_horizontal = expanding
		
		hbox = {
			layoutpolicy_horizontal = expanding
			
			tooltip = "POD_customsort_pinned_tt"
			using = tooltip_above
			tooltip_offset = { 0 12 }
			
			expand = {}
			
			text_single = {
				margin_bottom = 5
				text = "POD_customsort_pinned_desc"
			}
			
			spacer = {
				size = { 6 0 }
			}
			
			button_checkbox = {
				size = { 24 24 }
				checked = "[GetPlayer.MakeScope.Var('customsort_pinned_only').IsSet]"
				onclick = "[PlayerGuiExecute('POD_customsort_toggle_pinned_characters')]"
			}
			
			expand = {}
			
		}
	}
}