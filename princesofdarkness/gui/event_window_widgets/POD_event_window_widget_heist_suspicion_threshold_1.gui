
### Reached threshold 1 -> current suspicion level is 2
vbox = {
	name = "POD_event_window_widget_heist_suspicion_threshold_1"
	layoutpolicy_horizontal = expanding
	#margin = { 24 0 }
	
	vbox = {
		margin_bottom = 12
		#margin_bottom = 24
		layoutpolicy_horizontal = expanding
		
		pod_suspicion_display = {
			blockoverride "fontsize" { fontsize = 22 }
			blockoverride "header_bg" {}
			blockoverride "background" {
				background = {
					using = pod_heist_suspicion_bg
					margin = { 10 20 }
					
					shaderfile = "gfx/FX/POD_shaders/POD_event_shaders.shader"
					block "effect" { effectname = "FogSuspicion2" }

					modify_texture = {
						texture = "gfx/interface/component_masks/mask_fade_horizontal_short.dds"
						blend_mode = alphamultiply
					}
					modify_texture = {
						texture = "gfx/interface/component_masks/mask_fade_horizontal_short.dds"
						mirror = horizontal
						blend_mode = alphamultiply
					}
				}
				
				# background = {
				# 	using = Background_Frame
				# 	using = pod_color_suspicion2
					
				# 	margin = { 10 20 }
				# }
				
				# background = {
				# 	using = Background_Frame
				# 	using = pod_color_suspicion2
				# 	alpha = 0.5
					
				# 	margin = { 4 14 }
				# }
			}
		}
	}
}