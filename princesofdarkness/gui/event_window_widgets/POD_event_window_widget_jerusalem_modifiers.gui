@invisible_color = "#color:{0.0,0.0,0.0,0.0}"

vbox = {
	name = "POD_event_window_widget_jerusalem_modifiers"
	pod_jerusalem_modifiers = {}
	spacer = { size = { 0 56 } }
}

types PODJerusalemWidgets {
	type pod_jerusalem_modifiers = text_single {
		text = "[GetScriptedGui('POD_show_jerusalem_influence_modifiers').BuildTooltip(GuiScope.SetRoot(GetPlayer.MakeScope).End)]"
		fontsize = 16
		align = center
		default_format = @invisible_color
	}
}