namespace = POD_travel_events

POD_travel_events.1 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.1.t
	desc = POD_travel_events.1.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:4323 #Alamut
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = POD_travel_events.1.a
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		add_stress = medium_stress_gain
		
		if = {
			limit = {
				has_trait = lifestyle_blademaster
				is_assamite_trigger = yes
			}
			add_trait_xp = {
				trait = lifestyle_blademaster
				value = 10
			}
		}
		
		else_if = {
			limit = {
				is_assamite_trigger = yes
			}			
			add_trait = lifestyle_blademaster
		}
		
		else = {
			add_prestige = 150
		}
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.2 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.2.t
	desc = POD_travel_events.2.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:515 #Ceoris
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_tremere_trigger = no }
			text = POD_travel_events.2.a
		}
		name = {
			trigger = { is_tremere_trigger = yes }
			text = POD_travel_events.2.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		add_prestige = 150
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}


POD_travel_events.3 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.3.t
	desc = POD_travel_events.3.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:4582 #Carthage
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_brujah_trigger = no }
			text = POD_travel_events.3.a
		}
		name = {
			trigger = { is_brujah_trigger = yes }
			text = POD_travel_events.3.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		add_learning_lifestyle_xp = 100
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.4 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.4.t
	desc = POD_travel_events.4.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:760 #Erciyes
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_cappadocian_trigger = no }
			text = POD_travel_events.3.a
		}
		name = {
			trigger = { is_cappadocian_trigger = yes }
			text = POD_travel_events.4.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		add_intrigue_lifestyle_xp = 100
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}


POD_travel_events.5 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.5.t
	desc = POD_travel_events.5.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:536 #Cathedral of Flesh
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_tzimisce_trigger = no }
			text = POD_travel_events.5.a
		}
		name = {
			trigger = { is_tzimisce_trigger = yes }
			text = POD_travel_events.5.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		add_stress = medium_stress_gain
		
		add_prestige = 250
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}


POD_travel_events.6 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.6.t
	desc = POD_travel_events.6.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:2638 #Castel del ombro
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_lasombra_trigger = no }
			text = POD_travel_events.6.a
		}
		name = {
			trigger = { is_lasombra_trigger = yes }
			text = POD_travel_events.6.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		add_learning_lifestyle_xp = 150
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.7 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.7.t
	desc = POD_travel_events.7.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:5965 #Pit of Malkav
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_malkavian_trigger = no }
			text = POD_travel_events.7.a
		}
		name = {
			trigger = { is_malkavian_trigger = yes }
			text = POD_travel_events.7.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				is_malkavian_trigger = yes
			}
		add_stress = major_stress_loss
		}
		
		else = {
			add_stress = major_stress_gain
			add_prestige = 150
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}


POD_travel_events.8 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.8.t
	desc = POD_travel_events.8.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:6089 #Fire Court
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_setite_trigger = no }
			text = POD_travel_events.8.a
		}
		name = {
			trigger = { is_setite_trigger = yes }
			text = POD_travel_events.8.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				is_setite_trigger = yes
			}
		add_piety = 250
		}
		
		else = {
			add_stress = major_stress_gain
			add_prestige = 150
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}


POD_travel_events.9 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.9.t
	desc = POD_travel_events.9.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:480 #Knossos
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_baali_trigger = no }
			text = POD_travel_events.9.a
		}
		name = {
			trigger = { is_baali_trigger = yes }
			text = POD_travel_events.9.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				is_baali_trigger = yes
			}
		add_piety = 250
		}
		
		else = {
			add_stress = major_stress_gain
			add_prestige = 150
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.10 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.10.t
	desc = POD_travel_events.10.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:4990 #Sernog
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_tzimisce_trigger = no }
			text = POD_travel_events.10.a
		}
		name = {
			trigger = { is_tzimisce_trigger = yes }
			text = POD_travel_events.10.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				is_tzimisce_trigger = yes
			}
		add_piety = 250
		}
		
		else = {
			add_stress = major_stress_gain
			add_prestige = 150
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.11 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.11.t
	desc = POD_travel_events.11.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:3931 #Hunedoara
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = POD_travel_events.11.a

	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		add_prestige = 150
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.12 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.12.t
	desc = POD_travel_events.12.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:3086 #Vienna
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_tremere_trigger = no }
			text = POD_travel_events.12.a
		}
		name = {
			trigger = { is_tremere_trigger = yes }
			text = POD_travel_events.12.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				is_tremere_trigger = yes
			}
		add_piety = 250
		}
		
		else = {
			add_stress = major_stress_gain
			add_prestige = 150
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}


scripted_effect POD_travel_events_13_werewolf_duel_effect = {
	$FIGHTER$ = {
		duel = {
			skill = prowess
			value = 35
			5 = {
				desc = POD_travel_danger_events.13.success
				compare_modifier = {
					value = scope:duel_value
					multiplier = 1
					min = -49
				}
				root = {
					send_interface_toast = {
						title = POD_travel_danger_events.13.success
						left_icon = $FIGHTER$
						$FIGHTER$ = { add_prestige = medium_prestige_gain }
					}
				}
				min = 1
			}
			5 = {
				desc = POD_travel_danger_events.13.wound
				trigger = {
					NOT = {
						has_trait_rank = {
							trait = wounded
							rank > 1
						}
					}
				}
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = -49
				}
				root = {
					send_interface_toast = {
						title = POD_travel_danger_events.13.wound
						left_icon = $FIGHTER$
						$FIGHTER$ = {
							increase_wounds_effect = { REASON = fight }
						}
					}
				}
				min = 3
			}
			1 = {
				desc = POD_travel_danger_events.13.failure
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
					min = -49
				}
				root = {
					send_interface_toast = {
						title = POD_travel_danger_events.13.failure
						left_icon = $FIGHTER$
						$FIGHTER$ = {
							pod_death_no_killer = {
								death_reason = death_werewolf
							}						
						}
					}
				}
				min = 2
			}
		}
	}
}

POD_travel_events.13 = { #Werewolf attack
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.13.t
	desc = POD_travel_events.13.desc
	theme = travel_danger
	left_portrait = {
		character = root
		animation = shock
	}
	right_portrait = {
		character = scope:beast_victim
		animation = fear
	}
	lower_left_portrait = {
		trigger = { exists = scope:travel_leader }
		character = scope:travel_leader
	}

	trigger = {
		is_ai = no
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes
		location = {
			exists = province_owner
			OR = {
				travel_danger_type = {
					travel_plan = root.current_travel_plan
					terrain = jungle
				}
				travel_danger_type = {
					travel_plan = root.current_travel_plan
					terrain = forest
				}
			}
		}
		NOT = { has_trait = fera }
	}
	cooldown = { months = 12 }

	weight_multiplier = {
		base = 1
	}

	immediate = {
		mp_delay_travel_plan = { DAYS = 90 }
		current_travel_plan = {
			random_entourage_character = {
				limit = {
					can_be_combatant_based_on_gender_trigger = { ARMY_OWNER = root }
				}
				alternative_limit = { always = yes }
				weight = {
					base = 1
					modifier = { add = prowess }
					modifier = {
						add = 10
						has_court_position = bodyguard_court_position
					}
					modifier = {
						add = 10
						is_knight_of = root
					}
				}
				save_scope_as = beast_victim
			}
			#Save travel leader
			if = {
				limit = {
					NOT = { travel_leader ?= scope:beast_victim }
				}
				travel_leader ?= { save_scope_as = travel_leader }
			}
			#Save destination scope 
			next_destination_province = { save_scope_as = destination }
		}
		traveler_danger_xp_effect = {
			MIN = 3
			MAX = 6
		}
	}

	option = { #Fight it
		name = POD_travel_events.13.a
		save_scope_as = fighter
		stress_impact = {
			craven = medium_stress_gain
		}
		POD_travel_events_13_werewolf_duel_effect = { FIGHTER = root }
		
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				has_trait = craven
			}
		}
		
	}

	option = { #Distract with someone else
		name = POD_travel_events.13.b
		trigger = { exists = scope:beast_victim }
		POD_travel_events_13_werewolf_duel_effect = { FIGHTER = scope:beast_victim }
		stress_impact = {
			compassionate = medium_stress_impact_gain
			brave = minor_stress_impact_gain
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				has_trait = compassionate
			}
		}
	}

	option = { #Run!
		name = PODcanon_chars.33.b
		add_prestige = minor_prestige_loss
		current_travel_plan = { 
			delay_travel_plan = { days = 10 }
		}
		stress_impact = {
			base = minor_stress_impact_gain
			craven = minor_stress_impact_gain
		}
		stress_impact = {
			brave = medium_stress_impact_gain
			craven = minor_stress_impact_loss
		}
		ai_chance = {
			base = 100
			modifier = {
				factor = 0
				has_trait = brave
			}
		}
	}

	after = {
		mp_resume_travel_plan = yes
	}
}

POD_travel_events.14 = { 
    type = character_event
	content_source = dlc_POD
    title = POD_travel_events.14.t
    desc = POD_travel_events.14.desc
    theme = travel_danger
	override_background = {
		reference = ep2_travel_bridge
	}
    left_portrait = {
		character = root
		triggered_animation = {
			trigger = {
				OR = {
					has_trait = brave
					has_trait = wrathful
					has_trait = arrogant
					has_trait = vengeful
				}
			}
  			animation = anger
  		}
		animation = shock
	}

	lower_left_portrait = scope:knight_scope
    cooldown = { years = 2 }
	
    trigger = {
		is_ai = no
		is_playable_character = yes
		is_supehunter_trigger = no
		location ?= { is_riverside_province = yes } #there's an actual river
		exists = location.province_owner
		current_travel_plan ?= {
			any_entourage_character = {
				NOT = { this = root }
				count > 1
			}
		}
    }
	
	weight_multiplier = {
		base = 1
		modifier = {
			factor = 5
			has_variable = bad_prophecy_var
		}
	}
	
    immediate = {
		location.province_owner = { save_scope_as = value_target }
		mp_delay_travel_plan = { DAYS = 90 }
		current_travel_plan = {
			ordered_entourage_character = {
				limit = { NOT = { this = root } }
				order_by = {
					value = prowess
					if = {
						limit = { is_knight_of = root }
						add = 50
					}
					if = {
						limit = {
							root = { exists = cp:councillor_marshal }
							this = root.cp:councillor_marshal
						}
						add = 50
					}
					if = {
						limit = { has_court_position = bodyguard_court_position }
						add = 50
					}
				}
				save_scope_as = knight_scope
			}
		}
		traveler_danger_xp_effect = {
			MIN = 3
			MAX = 7
		}
	}
	
    option = { 
        name = POD_travel_events.14.a
		
			duel = {
				skill = martial
				value = 15
				50 = {
					desc = POD_travel_danger_events.13.success
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
					}
					root = {
						send_interface_toast = {
							title = POD_travel_danger_events.13.success
							left_icon = root

							add_prestige = minor_prestige_gain
						}
					}
				}
				50 = {
					desc = POD_travel_danger_events.13.wound
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
					}
					root = {
						send_interface_toast = {
							title = POD_travel_danger_events.13.wound
							left_icon = root
							root = {
								increase_wounds_effect = { REASON = fight }
								add_prestige = minor_prestige_loss
							}
						}
					}
				}
			}
		
	}
	
	#Refuse
    option = {
        name = POD_travel_events.14.b
		scope:knight_scope = {
			duel = {
				skill = prowess
				value = 30
				50 = {
					desc = POD_travel_danger_events.13.success
					compare_modifier = {
						value = scope:duel_value
						multiplier = 3.5
					}
					root = {
						send_interface_toast = {
							title = POD_travel_danger_events.13.success
							left_icon = scope:knight_scope

							scope:knight_scope = {
								if = {
									limit = {
										NOT = { has_trait = aggressive_attacker }
										number_of_commander_traits < commander_trait_limit
									}
									random_list = {
										50 = { }
										20 = {
											add_trait = aggressive_attacker
										}
									}
								}
							}
						}
					}
				}
				50 = {
					desc = POD_travel_danger_events.13.wound
					compare_modifier = {
						value = scope:duel_value
						multiplier = -3.5
					}
					root = {
						send_interface_toast = {
							title = POD_travel_danger_events.13.wound
							left_icon = scope:knight_scope
							add_prestige = medium_prestige_loss
							scope:knight_scope = {
								increase_wounds_effect = { REASON = fight }
							}
						}
					}
				}
			}
		}
		stress_impact = {
			craven = medium_stress_impact_gain
		}
		
	}
	
	option = {
		name = POD_travel_events.14.c
		duel = {
			skill = intrigue #intimidation check
			value = 15
			50 = {
				desc = POD_travel_danger_events.14.success
				compare_modifier = {
					value = scope:duel_value
					multiplier = 3.5
				}
				send_interface_toast = {
					title = POD_travel_danger_events.14.success
					left_icon = root
					stress_impact = {
						base = miniscule_stress_impact_loss
						wrathful = miniscule_stress_impact_loss
					}
				}
			}
			50 = {
				desc = POD_travel_danger_events.13.failure
				compare_modifier = {
					value = scope:duel_value
					multiplier = -3.5
				}
				send_interface_toast = {
					title = POD_travel_danger_events.13.failure
					left_icon = scope:knight_scope
					increase_wounds_effect = { REASON = fight }
					scope:knight_scope = {
						increase_wounds_effect = { REASON = fight }
					}
				}
			}
		}
		stress_impact = {
			humble = medium_stress_impact_gain
			craven = medium_stress_impact_gain
			shy = medium_stress_impact_gain
			calm = medium_stress_impact_gain
		}
		
	}
	after = {
		mp_resume_travel_plan = yes
	}
}


POD_travel_events.15 = { 
    type = character_event
	content_source = dlc_POD
    title = POD_travel_events.15.t
    desc = POD_travel_events.15.desc
    theme = travel_danger

    left_portrait = {
		character = root
		animation = worry
	}

    cooldown = { years = 10 }
	
    trigger = {
		is_ai = no
		is_playable_character = yes
		location = { is_sea_province = no }
    }
	
    immediate = {
		location.province_owner = { save_scope_as = value_target }
		mp_delay_travel_plan = { DAYS = 90 }

		traveler_danger_xp_effect = {
			MIN = 3
			MAX = 7
		}
	}
	
    option = { 
        name = POD_travel_events.15.a
				
		add_stress = medium_stress_gain	
	}
	
    option = {
        name = POD_travel_events.15.b
		
		add_prestige = medium_prestige_loss
		
		root.current_travel_plan = {
		every_entourage_character = {
			limit = {
				NOT = { this = root }
			}
		add_stress = medium_stress_gain	
		}
		}
	}
	
	option = {
		name = POD_travel_events.15.c
		
		trigger = { is_malkavian_trigger = yes }
		
		add_stress = medium_stress_loss
	}
	after = {
		mp_resume_travel_plan = yes
	}
}


POD_travel_events.16 = { 
    type = character_event
	content_source = dlc_POD
    title = POD_travel_events.16.t
    desc = POD_travel_events.16.desc
    theme = travel_danger

    left_portrait = {
		character = root
		animation = worry
	}

    cooldown = { years = 10 }
	
    trigger = {
		is_ai = no
		is_playable_character = yes
		location = { is_sea_province = no }
    }
	
    immediate = {
		location.province_owner = { save_scope_as = value_target }
		mp_delay_travel_plan = { DAYS = 90 }

		traveler_danger_xp_effect = {
			MIN = 3
			MAX = 7
		}
	}
	
    option = { 
        name = POD_travel_events.16.a
				
		add_prestige = minor_prestige_gain
		
		current_travel_plan = {
			every_entourage_character = {
				custom = every_entourage_tt
				add_opinion = {
					target = root 
					modifier = frustrated_opinion
					opinion = -15
				}
			}
			delay_travel_plan = { days = 20 }
		}		
	}
	
    option = {
        name = POD_travel_events.16.b
		
		add_prestige = minor_prestige_loss
		
		current_travel_plan = {
			every_entourage_character = {
				custom = every_entourage_tt
				add_opinion = {
					target = root 
					modifier = pleased_opinion
					opinion = 10
				}
			}
			delay_travel_plan = { days = 20 }
		}
		
	}
	
	option = {
		name = POD_travel_events.16.c
		
		trigger = { has_trait = sadistic }
		
		add_intrigue_lifestyle_xp = 50
		
		current_travel_plan = {
			every_entourage_character = {
				custom = every_entourage_tt
				add_opinion = {
					target = root 
					modifier = frustrated_opinion
					opinion = -15
				}
			}
			delay_travel_plan = { days = 20 }
		}	
	}
	after = {
		mp_resume_travel_plan = yes
	}
}


POD_travel_events.17 = { 
    type = character_event
	content_source = dlc_POD
    title = POD_travel_events.17.t
    desc = POD_travel_events.17.desc
    theme = travel_danger

    left_portrait = {
		character = root
		animation = personality_rational
	}

    cooldown = { years = 10 }
	
    trigger = {
		is_ai = no
		is_playable_character = yes
		location = { is_sea_province = no }
    }
	
    immediate = {
		location.province_owner = { save_scope_as = value_target }
		mp_delay_travel_plan = { DAYS = 90 }

		traveler_danger_xp_effect = {
			MIN = 3
			MAX = 7
		}
	}
	
    option = { 
        name = POD_travel_events.17.a
						
		current_travel_plan = {
			delay_travel_plan = { days = 30 }
		}		
	}
	
    option = {
        name = POD_travel_events.17.b
				
		remove_short_term_gold = 75
		add_prestige = minor_prestige_loss
		
	}
	
    option = {
        name = POD_travel_events.17.c
		
		add_martial_lifestyle_xp = 100
		
		current_travel_plan = {
			delay_travel_plan = { days = 90 }
		}
		
	}	

	after = {
		mp_resume_travel_plan = yes
	}
}

#Vamp specific, not dangerous
POD_travel_events.18 = { 
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.18.t
	desc = POD_travel_events.18.desc
	theme = travel
	left_portrait = {
		character = root 
	   	animation = stress
	}
	cooldown = { years = 10 }
	trigger = {
		is_ai = no
		is_available_travelling = yes
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes
		is_vampire_trigger = yes
	}

	immediate = {
		mp_delay_travel_plan = { DAYS = 90 }
	}

	option = {
		name = POD_travel_events.18.a
		current_travel_plan = {
			delay_travel_plan = {
				weeks = 3
				add = yes
			}
		}
		add_stress = medium_stress_loss

		stress_impact = {
			lazy = medium_stress_impact_loss
			diligent = medium_stress_impact_gain
		}
	}
	# No, we *must* keep going!
	option = {
		name = POD_travel_events.18.b
		stress_impact = {
			lazy = medium_stress_impact_gain
		}
	}
	after = {
		mp_resume_travel_plan = yes
	}
}

#Mummy specific, not dangerous
POD_travel_events.19 = { 
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.19.t
	desc = POD_travel_events.19.desc
	theme = travel
	left_portrait = {
		character = root 
	   	animation = personality_bold
	}
	cooldown = { years = 10 }
	trigger = {
		is_ai = no
		is_available_travelling = yes
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes
		is_mummy_trigger = yes
		NOT = { faith = faith:cabiri }
		NOT = { faith = faith:ten_mandates_of_heaven }
	}

	immediate = {
		mp_delay_travel_plan = { DAYS = 90 }
	}

	option = {
		name = POD_travel_events.19.a
		current_travel_plan = {
			delay_travel_plan = {
				weeks = 4
				add = yes
			}
		}
		add_stress = medium_stress_loss
		remove_short_term_gold = 100

		add_prestige = major_prestige_gain

		stress_impact = {
			arrogant = medium_stress_impact_loss
			humble = medium_stress_impact_gain
		}
	}
	# No, we *must* keep going!
	option = {
		name = POD_travel_events.19.b

	}
	after = {
		mp_resume_travel_plan = yes
	}
}

#Hunters specific, not dangerous
POD_travel_events.20 = { 
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.20.t
	desc = POD_travel_events.20.desc
	theme = travel
	left_portrait = {
		character = root 
	   	animation = worry
	}
	cooldown = { years = 10 }
	trigger = {
		is_ai = no
		is_available_travelling = yes
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes
		is_supehunter_trigger = yes
	}

	immediate = {
		mp_delay_travel_plan = { DAYS = 90 }
	}

	option = {
		name = POD_travel_events.20.a
		current_travel_plan = {
			delay_travel_plan = {
				weeks = 3
				add = yes
			}
		}
		add_stress = minor_stress_gain

		add_piety = 200

		stress_impact = {
			just = medium_stress_impact_loss
			arbitrary = medium_stress_impact_gain
		}
	}
	# No, we *must* keep going!
	option = {
		name = POD_travel_events.20.b

	}
	after = {
		mp_resume_travel_plan = yes
	}
}

#Werewolf specific, not dangerous
POD_travel_events.21 = { 
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.21.t
	desc = POD_travel_events.21.desc
	theme = travel
	left_portrait = {
		character = root 
	   	animation = personality_rational
	}
	cooldown = { years = 10 }
	trigger = {
		is_ai = no
		is_available_travelling = yes
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes
		is_werewolf_trigger = yes
		NOT = { religion = religion:blackspiraldancer_religion }
	}

	immediate = {
		mp_delay_travel_plan = { DAYS = 90 }
	}

	option = {
		name = POD_travel_events.21.a
		current_travel_plan = {
			delay_travel_plan = {
				weeks = 3
				add = yes
			}
		}
		add_stress = minor_stress_loss

		add_piety = 50
		add_prestige = 50

	}
	# No, we *must* keep going!
	option = {
		name = POD_travel_events.21.b

	}
	after = {
		mp_resume_travel_plan = yes
	}
}

#Demons specific, not dangerous
POD_travel_events.22 = { 
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.22.t
	desc = POD_travel_events.22.desc
	theme = travel
	left_portrait = {
		character = root 
	   	animation = schadenfreude
	}
	cooldown = { years = 10 }
	trigger = {
		is_ai = no
		is_available_travelling = yes
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes
		is_demon_trigger = yes
	}

	immediate = {
		mp_delay_travel_plan = { DAYS = 90 }
	}

	option = {
		name = POD_travel_events.22.a
		current_travel_plan = {
			delay_travel_plan = {
				weeks = 5
				add = yes
			}
		}
		add_stress = major_stress_loss

		add_piety = 50

	}
	# No, we *must* keep going!
	option = {
		name = POD_travel_events.21.b

	}
	after = {
		mp_resume_travel_plan = yes
	}
}

POD_travel_events.23 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.23.t
	desc = POD_travel_events.23.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		is_ai = no
		location = province:6077 #Ombos
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { has_religion = religion:roadofset_religion }
			text = POD_travel_events.23.a
		}
		name = {
			trigger = { NOT = { has_religion = religion:roadofset_religion } }
			text = POD_travel_events.23.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				has_religion = religion:roadofset_religion
			}
			add_piety = 250
		}
		
		else_if = {
			limit = {
				can_learn_serpentis_trigger = yes
				pod_convert_trigger = { CHARACTER = this FAITH = faith:viaset }
			}
			add_trait = serpentis
			add_piety = -200
			set_character_faith = faith:viaset
		}
		
		else = {
			add_prestige = 50
		}
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.24 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.24.t
	desc = POD_travel_events.24.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:1618 #Sherwood
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_anarch_faith_trigger = yes }
			text = POD_travel_events.24.a
		}
		name = {
			trigger = { is_anarch_faith_trigger = no }
			text = POD_travel_events.24.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				is_anarch_faith_trigger = yes
			}
			if = { 
				limit = {
					NOT = { has_trait = tourney_participant }
				}
			add_trait = tourney_participant
			}
			add_trait_xp = {
                trait = tourney_participant
				track = bow
                value = 20
            }		
		}
		
		else = {
			add_prestige = 100
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.25 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.25.t
	desc = POD_travel_events.25.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:2280 #Salianna's court
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = POD_travel_events.25.a

		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = { 
			limit = {
				has_royal_court = yes
			}
		add_character_modifier = {
			modifier = world_shattering_party_aftermath_modifier
			years = 5
		}
		}
		
		else = {
			add_prestige = 150
		}
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.26 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.26.t
	desc = POD_travel_events.26.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:154 #Telyavel
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { has_religion = religion:roadofbeast_religion }
			text = POD_travel_events.26.a
		}
		name = {
			trigger = { NOT = { has_religion = religion:roadofbeast_religion } }
			text = POD_travel_events.26.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				has_religion = religion:roadofbeast_religion
			}
			random = {
				chance = 10
			if = { 
				limit = {
					NOT = { has_trait = lifestyle_mystic }
				}
			add_trait = lifestyle_mystic
			}	
			}
			add_piety = 150
		}
		
		else = {
			add_prestige = 100
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}

POD_travel_events.27 = {
	type = character_event
	content_source = dlc_POD
	title = POD_travel_events.27.t
	desc = POD_travel_events.27.desc

	theme = travel
	left_portrait = {
		character = root
		animation = thinking
	}

	trigger = {
		location = province:2506 #Giovanni mausoleum
		current_travel_plan.travel_plan_owner = root
		is_playable_character = yes
		is_location_valid_for_travel_event_on_land = yes #Should not be needed...
		NOT = { location.county.holder = root } #If I own it then it does not make sense to visit it.
		NOT = {
			any_memory = { #Did I come here before?
				has_memory_type = memory_big_city_visited
				has_variable = city_location
				var:city_location = root.location
			}
		}
		
	}

	immediate = {
		play_music_cue = mx_cue_travel_arrival
		location = {
			save_scope_as = city_province
		}
	}

	option = { 
		name = { 
			trigger = { is_giovanni_trigger = yes }
			text = POD_travel_events.27.a
		}
		name = {
			trigger = { is_giovanni_trigger = no }
			text = POD_travel_events.27.b
		}
	
		current_travel_plan = {
			delay_travel_plan = {
				days = 7
			}
		}
		
		if = {
			limit = {
				is_giovanni_trigger = yes
			}
			remove_short_term_gold = 100
			add_prestige = 250
			add_piety = 250
		}
		
		else = {
			add_prestige = 100
		}
		
	}

	option = { 
		name = POD_travel_events.1.b
	
	}
	
	after = {
		create_character_memory = {
			type = memory_big_city_visited
		}
		scope:new_memory = {
			save_scope_as = city_memory
		}
		scope:city_memory = {
			set_variable = {
				name = city_location
				value = scope:city_province
			}
		}
	}
}