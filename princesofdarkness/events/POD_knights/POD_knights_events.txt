namespace = pod_knights

scripted_trigger is_valid_root_for_POD_knight_events = {
	is_ai = no
	is_vampire_trigger = yes
	is_playable_character = yes
	is_available = yes
}

scripted_trigger is_valid_target_for_POD_knight_events = {
	is_ruler = no
	is_vampire_trigger = yes
	NOT = { is_child_of = root }
	POD_is_clone_trigger = no
}

# Some events depending on your knight traits

pod_knights.1 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.1.t
	desc = pod_knights.1.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = dismissal
	}
	right_portrait = {
		character = scope:knight
		animation = scheme
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = arrogant
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = arrogant
			}
			save_scope_as = knight
		}
	}

	option = { 
		name = pod_knights.1.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}
	
	option = { 
		name = pod_knights.1.b
		
		POD_masquerade_exposure_increase_effect = yes
	}
}

pod_knights.2 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.2.t
	desc = pod_knights.2.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:knight
		animation = schadenfreude
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = deceitful
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = deceitful
			}
			save_scope_as = knight
		}
	}

	option = { 
		name = pod_knights.2.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}

	option = { 
		name = pod_knights.2.b
	
		add_character_modifier = {
			modifier = malignant_hearsay_modifier
			years = 20
		}	
	}
}

pod_knights.3 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.3.t
	desc = pod_knights.3.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = stress
	}
	right_portrait = {
		character = scope:knight
		animation = rage
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = wrathful
			prowess > 40
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = wrathful
				prowess > 40
			}
			save_scope_as = knight
		}
		if = {
			limit = {
				any_vassal_or_below = {
					is_available = yes
					prowess <= 40
				}
			}
			random_vassal = {
				limit = {
					is_available = yes
					prowess <= 40
				}
				save_scope_as = target
			}
		}
		else = {
			hidden_effect_new_object = {
				create_character = {
					location = root.capital_province
					template = POD_mortal_hunter_template
					save_scope_as = mortal
				}
				scope:mortal = {
					add_trait = "mortal"
					set_to_lowborn = yes
					add_character_flag = {
						flag = blocked_from_leaving
						years = 2
					}
					add_trait = POD_knows_too_much
					save_scope_as = target
				}
				add_courtier = scope:mortal
			}
		}			
	}

	option = { 
		name = pod_knights.3.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}

	option = { 
		name = pod_knights.3.b
	
		unknown_murder_effect = {
			VICTIM = scope:target
			MURDERER = scope:knight
			REASON = death_execution
		}
	}
}

pod_knights.4 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.4.t
	desc = pod_knights.4.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = dismissal
	}
	right_portrait = {
		character = scope:knight
		animation = fear
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = craven
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = craven
			}
			save_scope_as = knight
		}
	}

	option = { 
		name = pod_knights.4.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}

	option = { 
		name = pod_knights.4.b
	
		add_prestige = major_prestige_loss
	}
}

pod_knights.5 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.5.t
	desc = pod_knights.5.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = dismissal
	}
	right_portrait = {
		character = scope:knight
		animation = personality_greedy
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = greedy
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = greedy
			}
			save_scope_as = knight
		}
	}

	option = { 
		name = pod_knights.5.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}

	option = { 
		name = pod_knights.5.b
	
		remove_long_term_gold = major_gold_value
	}
}

pod_knights.6 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.6.t
	desc = pod_knights.6.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = fear
	}
	right_portrait = {
		character = scope:knight
		animation = paranoia
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = paranoid
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = paranoid
			}
			save_scope_as = knight
		}
	}

	option = { 
		name = pod_knights.6.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}

	option = { 
		name = pod_knights.6.b
	
		add_character_modifier = {
			modifier = lashing_out_at_victims_modifier
			years = 10
		}
	}
}

pod_knights.7 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.7.t
	desc = pod_knights.7.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = dismissal
	}
	right_portrait = {
		character = scope:knight
		animation = personality_cynical
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = cynical
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = cynical
			}
			save_scope_as = knight
		}
	}

	option = { 
		name = pod_knights.7.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}

	option = { 
		name = pod_knights.7.b
	
		add_piety = -250
	}
}

pod_knights.8 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.8.t
	desc = pod_knights.8.desc
	theme = masquerade
	left_portrait = {
		character = root
		animation = dismissal
	}
	right_portrait = {
		character = scope:knight
		animation = personality_vengeful
	}	
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	trigger = {
		is_valid_root_for_POD_knight_events = yes
		any_knight = {
			is_valid_target_for_POD_knight_events = yes
			has_trait = vengeful
		}
	}

	immediate = {
		random_knight = {
			limit = {
				is_valid_target_for_POD_knight_events = yes
				has_trait = vengeful
			}
			save_scope_as = knight
		}
	}

	option = { 
		name = pod_knights.8.a
		
		scope:knight = { 
			select_and_move_to_pool_effect = yes
		}
	}

	option = { 
		name = pod_knights.8.b
	
		add_character_modifier = {
			modifier = encouraged_city_burn_modifier
			years = 10
		}
	}
}

# Knight loyalty

pod_knights.99 = {
	hidden = yes
	immediate = {
		every_knight = {
			limit = {
				NOT = {
					prev = {
						is_target_in_variable_list = {
							name = potential_disloyal_knights
							target = prev
						}
					}
				}
			}
			# prevent event spam from checking knights that were employed just now
			add_to_list = knights_to_check
		}

		every_in_list = {
			variable = potential_disloyal_knights
			limit = {
				is_knight_of = prev
				POD_knight_should_leave_due_to_loyalty_trigger = { EMPLOYER = prev }
			}
			prev = {
				send_interface_message = {
					type = event_generic_bad
					title = pod_knights.100.message
					left_icon = prev
					prev = {
						custom_description = {
							text = "knight_became_wanderer"
							subject = this
							move_to_pool = yes
							if = {
								limit = { 
									has_relation_worshipped_demon = root 
									NOT = { has_relation_demonic_master = root }
									NOT = { has_relation_possessesing_wraith = root }
								}
								remove_relation_worshipped_demon = root
							}
							if = {
								limit = { 
									has_relation_worshipped_reborn = root 
									NOT = { house ?= root.house }
								}
								remove_relation_worshipped_reborn = root
							}
							POD_clear_discipline_battle_effect = yes
						}
						add_opinion = {
							target = prev
							modifier = knight_left_due_to_loyalty_opinion
							opinion = -25
						}
					}
				}
			}
		}

		clear_variable_list = potential_disloyal_knights

		every_in_list = {
			list = knights_to_check
			limit = {
				POD_knight_should_leave_due_to_loyalty_trigger = { EMPLOYER = prev }
			}
			prev = {
				add_to_variable_list = {
					name = potential_disloyal_knights
					target = prev
				}
			}
		}

		if = {
			limit = {
				has_variable_list = potential_disloyal_knights
				variable_list_size = {
					name = potential_disloyal_knights
					value >= 1
				}
			}
			trigger_event = pod_knights.100
		}
	}
}

pod_knights.100 = {
	type = character_event
	content_source = dlc_POD
	title = pod_knights.100.t
	desc = pod_knights.100.desc
	theme = masquerade
	
	left_portrait = {
		character = root
		animation = stress
	}
	
	right_portrait = {
		character = scope:portrait1
		trigger = {
			exists = scope:portrait1
			NOT = { exists = scope:portrait2 }
		}
	}
	
	lower_left_portrait = {
		character = scope:portrait1
		trigger = {
			exists = scope:portrait1
			exists = scope:portrait2
		}
	}
	
	lower_center_portrait = {
		character = scope:portrait2
		trigger = { exists = scope:portrait2 }
	}
	
	lower_right_portrait = {
		character = scope:portrait3
		trigger = { exists = scope:portrait3 }
	}
	
	widget = {
		gui = "POD_shader_event_fog_alley_night"
		container = "background_shader_vfx_container"
	}

	immediate = {
		ordered_in_list = {
			variable = potential_disloyal_knights
			position = 0
			save_scope_as = portrait1
		}
		if = {
			limit = {
				variable_list_size = {
					name = potential_disloyal_knights
					value > 1
				}
			}
			ordered_in_list = {
				variable = potential_disloyal_knights
				position = 1
				save_scope_as = portrait2
			}
			if = {
				limit = {
					variable_list_size = {
						name = potential_disloyal_knights
						value > 2
					}
				}
				ordered_in_list = {
					variable = potential_disloyal_knights
					position = 2
					save_scope_as = portrait3
				}
			}
		}
	}

	option = { 
		name = pod_knights.100.a
		
		custom_tooltip = pod_knights.100.tt
		every_in_list = {
			variable = potential_disloyal_knights
			custom_description = {
				text = "character_getfirstnamenicknamed"
				subject = this
			}
		}
		custom_tooltip = pod_knights.100.tt2
	}

	option = { 
		name = pod_knights.100.b

		every_in_list = {
			variable = potential_disloyal_knights
			set_knight_status = forbid
		}
	}
}