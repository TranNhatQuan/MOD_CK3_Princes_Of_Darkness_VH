# Events for saving from a murder attempt

namespace = POD_murder_save

#Save against sun : owner

POD_murder_save.0001 = {
	type = character_event
	title = POD_murder_save.0001.t
	desc = POD_murder_save.0001.desc
	orphan = yes
	theme = murder_scheme
	left_portrait = {
		character = scope:target
		animation = shock
	}
	widget = {
		gui = "event_window_widget_scheme"
		container = "custom_widgets_container"
	}
	
	trigger = {
		#scope:murder_method = flag:vampire_sun
		scope:target = {
			OR = {
				has_trait = fortitudeadvanced
				has_trait = celerityadvanced
				has_trait = temporisadvanced
				has_perk = darkstatue_perk
			}
		}
	}

	weight_multiplier = {
		base = 1
		modifier = {
			add = 1
			scope:target = {
				has_trait = fortitudeadvanced
				has_trait = celerityadvanced
				has_trait = temporisadvanced
				has_perk = darkstatue_perk
			}	
		}		
		modifier = {
			add = -1
			scope:target ?= { has_trait = setite }	
		}
	}

	immediate = {
		save_scope_value_as = {
			name = murder_saved
			value = flag:vampire_sun_saved
		}
		murder_failure_effect = yes # Fires the response for the target
	}

	option = {
		name = POD_murder_save.0001.a
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			scope:scheme = {
				end_scheme = yes
			}
		ai_chance = {
			base = 100
		}
	}

	option = {
		name = POD_murder_save.0001.b
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			restart_murder_scheme_effect = yes
		ai_chance = {
			base = 0
		}
	}
	after = {
		scope:target = { trigger_event = murder_save.1001 }
	}
}


#Save against sun : target

POD_murder_save.1001 = {
	type = character_event
	title = POD_murder_save.1001.t
	desc = POD_murder_save.1001.desc
	
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:owner_revealed
		animation = shock
	}
	theme = intrigue

	trigger = {
		exists = scope:murder_saved
		scope:murder_saved = flag:vampire_sun_saved
	}

	immediate = {
		if = {
			limit = {
				exists = scope:scheme_discovered
			}
			scope:owner = {
				save_scope_as = owner_revealed
			}
		}
	}

	option = { #Who could do such a thing?
		name = POD_murder_save.1001.a
		trigger = {
			NOT = {
				exists = scope:scheme_discovered
			}
		}
		custom_tooltip = murder_save.failure_unknown_owner_tt
	}

	option = { #Vengeance!
		name = POD_murder_save.1001.b
		trigger = {
			exists = scope:scheme_discovered
		}
		custom_tooltip = murder_save.failure_known_owner_tt
	}

	after = {
		add_character_modifier = {
			modifier = watchful_modifier
			days = watchful_modifier_duration
		}
	}
}


#Save against sun : hunters

POD_murder_save.0002 = {
	type = character_event
	title = POD_murder_save.0002.t
	desc = POD_murder_save.0002.desc
	orphan = yes
	theme = murder_scheme
	left_portrait = {
		character = scope:target
		animation = shock
	}
	widget = {
		gui = "event_window_widget_scheme"
		container = "custom_widgets_container"
	}
	
	trigger = {
		#scope:murder_method = flag:vampire_hunters
		scope:target = {
			prowess >= 35
		}
	}

	weight_multiplier = {
		base = 1
		modifier = {
			add = 1
			scope:target = {
				has_trait = potenceadvanced
				}	
			}		
		}

	immediate = {
		save_scope_value_as = {
			name = murder_saved
			value = flag:vampire_hunters_saved
		}
		murder_failure_effect = yes # Fires the response for the target
	}

	option = {
		name = POD_murder_save.0002.a
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			scope:scheme = {
				end_scheme = yes
			}
		ai_chance = {
			base = 100
		}
	}

	option = {
		name = POD_murder_save.0002.b
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			restart_murder_scheme_effect = yes
		ai_chance = {
			base = 0
		}
	}
	after = {
		scope:target = { trigger_event = murder_save.1002 }
	}
}


#Save against hunters : target

POD_murder_save.1002 = {
	type = character_event
	title = POD_murder_save.1002.t
	desc = POD_murder_save.1002.desc
	
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:owner_revealed
		animation = shock
	}
	theme = intrigue

	trigger = {
		exists = scope:murder_saved
		scope:murder_saved = flag:vampire_hunters_saved
	}

	immediate = {
		if = {
			limit = {
				exists = scope:scheme_discovered
			}
			scope:owner = {
				save_scope_as = owner_revealed
			}
		}
	}

	option = { #Who could do such a thing?
		name = POD_murder_save.1002.a
		trigger = {
			NOT = {
				exists = scope:scheme_discovered
			}
		}
		custom_tooltip = murder_save.failure_unknown_owner_tt
	}

	option = { #Vengeance!
		name = POD_murder_save.1002.b
		trigger = {
			exists = scope:scheme_discovered
		}
		custom_tooltip = murder_save.failure_known_owner_tt
	}

	after = {
		add_character_modifier = {
			modifier = watchful_modifier
			days = watchful_modifier_duration
		}
	}
}

#Save against murder : werewolves

POD_murder_save.0003 = {
	type = character_event
	title = POD_murder_save.0003.t
	desc = POD_murder_save.0003.desc
	orphan = yes
	theme = murder_scheme
	left_portrait = {
		character = scope:target
		animation = shock
	}
	widget = {
		gui = "event_window_widget_scheme"
		container = "custom_widgets_container"
	}
	
	trigger = {
		#scope:murder_method = flag:vampire_werewolves
		scope:target = {
			OR = {
				has_trait = animalismadvanced
				has_trait = proteanadvanced
				}
		}
	}

	weight_multiplier = {
		base = 1
		modifier = {
			add = 1
			scope:target = {
				has_trait = animalismadvanced
				has_trait = proteanadvanced
				}	
			}		
		}

	immediate = {
		save_scope_value_as = {
			name = murder_saved
			value = flag:vampire_werewolves_saved
		}
		murder_failure_effect = yes # Fires the response for the target
	}

	option = {
		name = POD_murder_save.0003.a
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			scope:scheme = {
				end_scheme = yes
			}
		ai_chance = {
			base = 100
		}
	}

	option = {
		name = POD_murder_save.0003.b
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			restart_murder_scheme_effect = yes
		ai_chance = {
			base = 0
		}
	}
	after = {
		scope:target = { trigger_event = murder_save.1003 }
	}	
}


#Save against werewolves : target

POD_murder_save.1003 = {
	type = character_event
	title = POD_murder_save.1003.t
	desc = POD_murder_save.1003.desc
	
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:owner_revealed
		animation = shock
	}
	theme = intrigue

	trigger = {
		exists = scope:murder_saved
		scope:murder_saved = flag:vampire_werewolves_saved
	}

	immediate = {
		if = {
			limit = {
				exists = scope:scheme_discovered
			}
			scope:owner = {
				save_scope_as = owner_revealed
			}
		}
	}

	option = { #Who could do such a thing?
		name = POD_murder_save.1003.a
		trigger = {
			NOT = {
				exists = scope:scheme_discovered
			}
		}
		custom_tooltip = murder_save.failure_unknown_owner_tt
	}

	option = { #Vengeance!
		name = POD_murder_save.1003.b
		trigger = {
			exists = scope:scheme_discovered
		}
		custom_tooltip = murder_save.failure_known_owner_tt
	}

	after = {
		add_character_modifier = {
			modifier = watchful_modifier
			days = watchful_modifier_duration
		}
	}
}

#Save against fire : owner

POD_murder_save.0004 = {
	type = character_event
	title = POD_murder_save.0004.t
	desc = POD_murder_save.0004.desc
	orphan = yes
	theme = murder_scheme
	left_portrait = {
		character = scope:target
		animation = shock
	}
	widget = {
		gui = "event_window_widget_scheme"
		container = "custom_widgets_container"
	}
	
	trigger = {
		#scope:murder_method = flag:vampire_fire
		scope:target = {
			OR = {
				has_trait = celerityadvanced
				has_trait = flightadvanced
				has_trait = fortitudeadvanced
				has_trait = temporisadvanced
				POD_immune_to_fire_trigger = yes
			}
		}
	}

	weight_multiplier = {
		base = 1
		modifier = {
			add = 1
			scope:target = {
				has_trait = celerityadvanced
				has_trait = flightadvanced
				has_trait = fortitudeadvanced
				has_trait = temporisadvanced
				POD_immune_to_fire_trigger = yes
			}
		}
	}

	immediate = {
		save_scope_value_as = {
			name = murder_saved
			value = flag:vampire_fire_saved
		}
		murder_failure_effect = yes # Fires the response for the target
	}

	option = {
		name = POD_murder_save.0004.a
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			scope:scheme = {
				end_scheme = yes
			}
		ai_chance = {
			base = 100
		}
	}

	option = {
		name = POD_murder_save.0004.b
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			restart_murder_scheme_effect = yes
		ai_chance = {
			base = 0
		}
	}
	after = {
		scope:target = { trigger_event = murder_save.1004 }
	}
}


#Save against fire : target

POD_murder_save.1004 = {
	type = character_event
	title = POD_murder_save.1004.t
	desc = POD_murder_save.1004.desc
	
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:owner_revealed
		animation = shock
	}
	theme = intrigue

	trigger = {
		exists = scope:murder_saved
		scope:murder_saved = flag:vampire_fire_saved
	}

	immediate = {
		if = {
			limit = {
				exists = scope:scheme_discovered
			}
			scope:owner = {
				save_scope_as = owner_revealed
			}
		}
	}

	option = { #Who could do such a thing?
		name = POD_murder_save.1004.a
		trigger = {
			NOT = {
				exists = scope:scheme_discovered
			}
		}
		custom_tooltip = murder_save.failure_unknown_owner_tt
	}

	option = { #Vengeance!
		name = POD_murder_save.1004.b
		trigger = {
			exists = scope:scheme_discovered
		}
		custom_tooltip = murder_save.failure_known_owner_tt
	}

	after = {
		add_character_modifier = {
			modifier = watchful_modifier
			days = watchful_modifier_duration
		}
	}
}


#Save against betrayal : owner

POD_murder_save.0005 = {
	type = character_event
	title = POD_murder_save.0005.t
	desc = POD_murder_save.0005.desc
	orphan = yes
	theme = murder_scheme
	left_portrait = {
		character = scope:target
		animation = shock
	}
	widget = {
		gui = "event_window_widget_scheme"
		container = "custom_widgets_container"
	}
	
	trigger = {
		#scope:murder_method = flag:vampire_betrayal
		scope:target = {
				has_trait = auspexadvanced
		}
	}

	weight_multiplier = {
		base = 1
		modifier = {
			add = 1
			scope:target = {
				has_trait = fortitudeadvanced
				}	
			}		
		}

	immediate = {
		save_scope_value_as = {
			name = murder_saved
			value = flag:vampire_betrayal_saved
		}
		murder_failure_effect = yes # Fires the response for the target
	}

	option = {
		name = POD_murder_save.0005.a
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			scope:scheme = {
				end_scheme = yes
			}
		ai_chance = {
			base = 100
		}
	}

	option = {
		name = POD_murder_save.0005.b
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			restart_murder_scheme_effect = yes
		ai_chance = {
			base = 0
		}
	}
	after = {
		scope:target = { trigger_event = murder_save.1005 }
	}
}


#Save against betrayal : target

POD_murder_save.1005 = {
	type = character_event
	title = POD_murder_save.1005.t
	desc = POD_murder_save.1005.desc
	
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:owner_revealed
		animation = shock
	}
	theme = intrigue

	trigger = {
		exists = scope:murder_saved
		scope:murder_saved = flag:vampire_betrayal_saved
	}

	immediate = {
		if = {
			limit = {
				exists = scope:scheme_discovered
			}
			scope:owner = {
				save_scope_as = owner_revealed
			}
		}
	}

	option = { #Who could do such a thing?
		name = POD_murder_save.1005.a
		trigger = {
			NOT = {
				exists = scope:scheme_discovered
			}
		}
		custom_tooltip = murder_save.failure_unknown_owner_tt
	}

	option = { #Vengeance!
		name = POD_murder_save.1005.b
		trigger = {
			exists = scope:scheme_discovered
		}
		custom_tooltip = murder_save.failure_known_owner_tt
	}

	after = {
		add_character_modifier = {
			modifier = watchful_modifier
			days = watchful_modifier_duration
		}
	}
}

#Save against assasin : owner

POD_murder_save.0006 = {
	type = character_event
	title = POD_murder_save.0006.t
	desc = POD_murder_save.0006.desc
	orphan = yes
	theme = murder_scheme
	left_portrait = {
		character = scope:target
		animation = shock
	}
	widget = {
		gui = "event_window_widget_scheme"
		container = "custom_widgets_container"
	}
	
	trigger = {
		#scope:murder_method = flag:hired_assassin
		scope:target = {
			prowess >= 35
		}
	}

	weight_multiplier = {
		base = 1
		modifier = {
			add = 1
			scope:target = {
				has_trait = potenceadvanced
				}	
			}		
		}

	immediate = {
		save_scope_value_as = {
			name = murder_saved
			value = flag:vampire_assassin_saved
		}
		murder_failure_effect = yes # Fires the response for the target
	}

	option = {
		name = POD_murder_save.0006.a
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			scope:scheme = {
				end_scheme = yes
			}
		ai_chance = {
			base = 100
		}
	}

	option = {
		name = POD_murder_save.0006.b
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			restart_murder_scheme_effect = yes
		ai_chance = {
			base = 0
		}
	}
	after = {
		scope:target = { trigger_event = murder_save.1006 }
	}
}


#Save against assassin : target

POD_murder_save.1006 = {
	type = character_event
	title = POD_murder_save.1006.t
	desc = POD_murder_save.1006.desc
	
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:owner_revealed
		animation = shock
	}
	theme = intrigue

	trigger = {
		exists = scope:murder_saved
		scope:murder_saved = flag:vampire_assassin_saved
	}

	immediate = {
		if = {
			limit = {
				exists = scope:scheme_discovered
			}
			scope:owner = {
				save_scope_as = owner_revealed
			}
		}
	}

	option = { #Who could do such a thing?
		name = POD_murder_save.1006.a
		trigger = {
			NOT = {
				exists = scope:scheme_discovered
			}
		}
		custom_tooltip = murder_save.failure_unknown_owner_tt
	}

	option = { #Vengeance!
		name = POD_murder_save.1006.b
		trigger = {
			exists = scope:scheme_discovered
		}
		custom_tooltip = murder_save.failure_known_owner_tt
	}

	after = {
		add_character_modifier = {
			modifier = watchful_modifier
			days = watchful_modifier_duration
		}
	}
}

#Save against mob : owner

POD_murder_save.0007 = {
	type = character_event
	title = POD_murder_save.0007.t
	desc = POD_murder_save.0007.desc
	orphan = yes
	theme = murder_scheme
	left_portrait = {
		character = scope:target
		animation = shock
	}
	widget = {
		gui = "event_window_widget_scheme"
		container = "custom_widgets_container"
	}
	
	trigger = {
		#scope:murder_method = flag:riled_up_mob
		scope:target = {
			OR = {
				has_trait = presenceadvanced
				has_trait = dominateadvanced
				}
		}
	}

	weight_multiplier = {
		base = 1
		modifier = {
			add = 1
			scope:target = {
				has_trait = presenceadvanced
				has_trait = dominateadvanced
				}	
			}		
		}

	immediate = {
		save_scope_value_as = {
			name = murder_saved
			value = flag:vampire_mob_saved
		}
		murder_failure_effect = yes # Fires the response for the target
	}

	option = {
		name = POD_murder_save.0007.a
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			scope:scheme = {
				end_scheme = yes
			}
		ai_chance = {
			base = 100
		}
	}

	option = {
		name = POD_murder_save.0007.b
			if = {
				limit = { exists = scope:scheme_discovered }
				add_dread = medium_dread_gain
			}
			restart_murder_scheme_effect = yes
		ai_chance = {
			base = 0
		}
	}
	after = {
		scope:target = { trigger_event = murder_save.1007 }
	}	
}


#Save against mob : target

POD_murder_save.1007 = {
	type = character_event
	title = POD_murder_save.1007.t
	desc = POD_murder_save.1007.desc
	
	left_portrait = {
		character = root
		animation = anger
	}
	right_portrait = {
		character = scope:owner_revealed
		animation = shock
	}
	theme = intrigue

	trigger = {
		exists = scope:murder_saved
		scope:murder_saved = flag:vampire_mob_saved
	}

	immediate = {
		if = {
			limit = {
				exists = scope:scheme_discovered
			}
			scope:owner = {
				save_scope_as = owner_revealed
			}
		}
	}

	option = { #Who could do such a thing?
		name = POD_murder_save.1007.a
		trigger = {
			NOT = {
				exists = scope:scheme_discovered
			}
		}
		custom_tooltip = murder_save.failure_unknown_owner_tt
	}

	option = { #Vengeance!
		name = POD_murder_save.1007.b
		trigger = {
			exists = scope:scheme_discovered
		}
		custom_tooltip = murder_save.failure_known_owner_tt
	}

	after = {
		add_character_modifier = {
			modifier = watchful_modifier
			days = watchful_modifier_duration
		}
	}
}
