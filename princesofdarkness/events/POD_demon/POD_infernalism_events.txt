namespace = POD_infernalism_events

# Demonic Pact Creator
# needs scope:recipient (the demon) on the TopScope
POD_infernalism_events.1 = {
	type = character_event
	content_source = dlc_POD
	title = POD_infernalism_events.1.t
	desc = POD_infernalism_events.1.desc
	theme = demon

	override_background = { reference = shader_event_ritual }

	immediate = {
		POD_pact_gui_init_all_stipulations = yes
		# prevent glitches from having multiple pact windows open
		add_character_flag = POD_busy_drafting_pact
	}
	
	widgets = {
		widget = {
			gui = "POD_event_window_widget_demonic_pact_preview"
			container = "custom_widgets_container"
		}
		widget = {
			gui = "POD_event_window_widget_demonic_pact"
			container = "foreground_shader_vfx_container"
		}
		
		widget = {
			gui = "POD_shader_event_ritual"
			container = "background_shader_vfx_container"
		}
	}

	option = {
		name = POD_infernalism_events.1.a
	}

	after = {
		trigger_event = {
			id = POD_infernalism_events.2
			days = 1
		}
		remove_character_flag = POD_busy_drafting_pact
	}
}

POD_infernalism_events.2 = {
	hidden = yes

	immediate = {
		remove_variable = selected_frequency
		remove_variable = selected_obligation
		remove_variable = selected_consequence
		POD_pact_gui_clear_stipulations = yes
	}
}