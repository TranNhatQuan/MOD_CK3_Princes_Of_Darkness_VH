namespace = POD_temporis

POD_temporis.100 = {
	type = character_event
	content_source = dlc_POD
	title = POD_temporis.100.t
	desc = POD_temporis.100.desc
	theme = temporis

	left_portrait = {
	    character = scope:recipient
		animation = fear
	}
	
	widget = {
		gui = "POD_event_window_widget_edit_age"
		container = "custom_widgets_container"
	}
	
	immediate = {
		save_scope_value_as = {
			name  = original_age
			value = scope:recipient.age
		}
	}
	
	option = {
		name = POD_temporis.100.a
	}
}

POD_temporis.101 = {
	hidden = yes
	
	immediate = {
		if = {
			limit = { scope:actor = scope:sc_victor }
			scope:actor = { trigger_event = POD_temporis.100 }
		}
	}
}

POD_temporis.300 = {
	hidden = yes
	
	trigger = { has_perk = summon_history_perk }
	
	immediate = {
		create_summoned_historical_artifact_effect = yes
		hidden_effect_new_object = { 
			scope:newly_created_artifact = {
				set_max_durability = 10
				set_artifact_name = artifact_summoned_historical_unique_artifact_name
				set_artifact_rarity = common
				set_variable = summoned_artifact
			}
		}
		trigger_event = {
			id = POD_temporis.300
			years = 3
		}
	}
}