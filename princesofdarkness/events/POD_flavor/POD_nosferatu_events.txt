namespace = podnosferatu

# intro
podnosferatu.1 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.1.t
	desc = podnosferatu.1.desc
	theme = nosferatu_coa 

	right_portrait = {
		character = root
		animation = worry
	}
	
	trigger = { 
	}

	option = {
		name = podnosferatu.1.a		

		add_internal_flag = dangerous	
		
		trigger_event = {
			id = podnosferatu.2
			days = 15
		}
		
		custom_tooltip = nosferatu_intro_tt
		
	}
	
}

#Setup of the system
podnosferatu.2 = {
	hidden = yes
	
	trigger = {
	is_ai = no
	is_nosferatu_trigger = yes
	NOT = {
		has_global_variable = variable_POD_absi_dead
		}		
	}
		
	immediate = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.3 years = { 15 45 } } } #Abraxes
			20 = { trigger_event = { id = podnosferatu.4 years = { 15 45 } } } #Azazel
			20 = { trigger_event = { id = podnosferatu.5 years = { 15 45 } } } #Echidna
			20 = { trigger_event = { id = podnosferatu.6 years = { 15 45 } } } #Goyomart
			20 = { trigger_event = { id = podnosferatu.7 years = { 15 45 } } } #Gorgo
			20 = { trigger_event = { id = podnosferatu.8 years = { 15 45 } } } #Illuyankas
			20 = { trigger_event = { id = podnosferatu.9 years = { 15 45 } } } #Nuckalavee
			20 = { }
		}
	}
}

#Paranoia? Abraxes
podnosferatu.3 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.3.t
	desc = podnosferatu.3.desc
	theme = nosferatu_coa 
	right_portrait = {
		character = root
		animation = worry
	}
	
	option = {
		name = podnosferatu.3.a
		
		add_character_flag = nictuku_well_prepared_flag
		
		custom_tooltip = nictuku_well_prepared_tt
		
		capital_county = {
			add_county_modifier = {
				modifier = nictuku_well_prepared_modifier
				years = 5
			}
		} 		
	}
	
	option = {
		name = podnosferatu.3.b

		add_character_flag = nictuku_prepared_flag
		
		custom_tooltip = nictuku_prepared_tt
		
		capital_county = {
			add_county_modifier = {
				modifier = nictuku_prepared_modifier
				years = 5
			}
		} 
	}

	option = {
		name = podnosferatu.3.c

		add_prestige = 100
		
	}
	
	after = {
		hidden_effect = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.30 years = { 5 10 } } }
			80 = { 
				if = {
					limit = {
						has_character_flag = nictuku_well_prepared_flag
					}
					remove_character_flag = nictuku_well_prepared_flag
				}
				else_if = {
					limit = {
						has_character_flag = nictuku_prepared_flag
					}
					remove_character_flag = nictuku_prepared_flag
				}
				else = {}
			}
		}
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#not a fake news
podnosferatu.30 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.30.t
	desc = podnosferatu.30.desc
	theme = nosferatu_coa
	left_portrait = {
		character = root
		animation = fear
	}	
	
	option = {
		name = podnosferatu.30.a
	
		duel = {
			skill = stewardship
			value = 20
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 0.5
					}
				desc = podnosferatu.30.effect.success.desc
				
				
			send_interface_toast = {
				title = podnosferatu.30.effect.success.desc
				left_icon = root
				
				add_prestige = 200
				}				
				
			modifier = {
				has_character_flag = nictuku_well_prepared_flag
				add = 10
			}
			modifier = {
				has_character_flag = nictuku_prepared_flag
				add = 5
			}
			}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -0.5
					}

					desc = podnosferatu.30.critical.failure.effect.desc
					
					death = {
						death_reason = death_murder
					}
				}
			}		
		
	}

	option = {
		name = POD_survey_events.110.option.d
	
		add_character_modifier = {
			modifier = broken_authority_modifier
			years = 5
		}
	}
	
	after = {
		hidden_effect = {
		if = {
			limit = {
				has_character_flag = nictuku_well_prepared_flag
			}
			remove_character_flag = nictuku_well_prepared_flag
		}
		else_if = {
			limit = {
				has_character_flag = nictuku_prepared_flag
			}
			remove_character_flag = nictuku_prepared_flag
		}
		else = {}
		
		trigger_event = { id = podnosferatu.2 years = 5 }
	}
	}
}

#Paranoia? Azazel
podnosferatu.4 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.4.t
	desc = podnosferatu.4.desc
	theme = nosferatu_coa 
	right_portrait = {
		character = root
		animation = worry
	}
	
	option = {
		name = podnosferatu.4.a
		
		add_character_flag = nictuku_well_prepared_flag
		
		custom_tooltip = nictuku_well_prepared_tt
		
		POD_masquerade_exposure_increase_effect = yes
 		POD_masquerade_exposure_increase_effect = yes

	}
	
	option = {
		name = podnosferatu.3.b

		add_character_flag = nictuku_prepared_flag
		
		custom_tooltip = nictuku_prepared_tt
		
		POD_masquerade_exposure_increase_effect = yes
 
	}

	option = {
		name = podnosferatu.3.c

		add_prestige = 100
		
	}
	
	after = {
		hidden_effect = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.40 years = { 5 10 } } }
			80 = { 
				if = {
					limit = {
						has_character_flag = nictuku_well_prepared_flag
					}
					remove_character_flag = nictuku_well_prepared_flag
				}
				else_if = {
					limit = {
						has_character_flag = nictuku_prepared_flag
					}
					remove_character_flag = nictuku_prepared_flag
				}
				else = {}
			}
		}
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#not a fake news
podnosferatu.40 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.40.t
	desc = podnosferatu.40.desc
	theme = nosferatu_coa
	left_portrait = {
		character = root
		animation = fear
	}	
	
	option = {
		name = podnosferatu.40.a
	
		duel = {
			skill = diplomacy
			value = 20
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 0.5
					}
				desc = podnosferatu.30.effect.success.desc
				
				
			send_interface_toast = {
				title = podnosferatu.30.effect.success.desc
				left_icon = root
				
				add_prestige = 200
				}				
				
			modifier = {
				has_character_flag = nictuku_well_prepared_flag
				add = 10
			}
			modifier = {
				has_character_flag = nictuku_prepared_flag
				add = 5
			}
			}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -0.5
					}

					desc = podnosferatu.40.critical.failure.effect.desc
					
					death = {
						death_reason = death_murder
					}
				}
			}		
		
	}

	option = {
		name = POD_survey_events.110.option.d
	
		add_character_modifier = {
			modifier = broken_authority_modifier
			years = 5
		}
	}
	
	after = {
		hidden_effect = {
		if = {
			limit = {
				has_character_flag = nictuku_well_prepared_flag
			}
			remove_character_flag = nictuku_well_prepared_flag
		}
		else_if = {
			limit = {
				has_character_flag = nictuku_prepared_flag
			}
			remove_character_flag = nictuku_prepared_flag
		}
		else = {}
		
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#Paranoia? Echidna
podnosferatu.5 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.5.t
	desc = podnosferatu.5.desc
	theme = nosferatu_coa 
	right_portrait = {
		character = root
		animation = worry
	}
	
	option = {
		name = podnosferatu.5.a
		
		add_character_flag = nictuku_well_prepared_flag
		
		custom_tooltip = nictuku_well_prepared_tt
		
		capital_county = {
			change_development_level = -4
		}		
	}
	
	option = {
		name = podnosferatu.3.b

		add_character_flag = nictuku_prepared_flag
		
		custom_tooltip = nictuku_prepared_tt
		
		capital_county = {
			change_development_level = -2
		} 
	}

	option = {
		name = podnosferatu.3.c

		add_prestige = 100
		
	}
	
	after = {
		hidden_effect = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.50 years = { 5 10 } } }
			80 = { 
				if = {
					limit = {
						has_character_flag = nictuku_well_prepared_flag
					}
					remove_character_flag = nictuku_well_prepared_flag
				}
				else_if = {
					limit = {
						has_character_flag = nictuku_prepared_flag
					}
					remove_character_flag = nictuku_prepared_flag
				}
				else = {}
			}
		}
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#not a fake news
podnosferatu.50 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.50.t
	desc = podnosferatu.50.desc
	theme = nosferatu_coa
	left_portrait = {
		character = root
		animation = fear
	}	
	
	option = {
		name = podnosferatu.50.a
	
		duel = {
			skill = learning
			value = 20
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 0.5
					}
				desc = podnosferatu.30.effect.success.desc
				
				
			send_interface_toast = {
				title = podnosferatu.30.effect.success.desc
				left_icon = root
				
				add_prestige = 200
				}				
				
			modifier = {
				has_character_flag = nictuku_well_prepared_flag
				add = 10
			}
			modifier = {
				has_character_flag = nictuku_prepared_flag
				add = 5
			}
			}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -0.5
					}

					desc = podnosferatu.50.critical.failure.effect.desc
					
					death = {
						death_reason = death_murder
					}
				}
			}		
		
	}

	option = {
		name = POD_survey_events.110.option.d
	
		add_character_modifier = {
			modifier = broken_authority_modifier
			years = 5
		}
	}
	
	after = {
		hidden_effect = {
		if = {
			limit = {
				has_character_flag = nictuku_well_prepared_flag
			}
			remove_character_flag = nictuku_well_prepared_flag
		}
		else_if = {
			limit = {
				has_character_flag = nictuku_prepared_flag
			}
			remove_character_flag = nictuku_prepared_flag
		}
		else = {}
		
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}


#Paranoia? Gayomart
podnosferatu.6 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.6.t
	desc = podnosferatu.6.desc
	theme = nosferatu_coa 
	right_portrait = {
		character = root
		animation = worry
	}
	
	option = {
		name = podnosferatu.6.a
		
		add_character_flag = nictuku_well_prepared_flag
		
		custom_tooltip = nictuku_well_prepared_tt
		
		remove_short_term_gold = 300		
	}
	
	option = {
		name = podnosferatu.3.b

		add_character_flag = nictuku_prepared_flag
		
		custom_tooltip = nictuku_prepared_tt
		
		remove_short_term_gold = 150		
	}

	option = {
		name = podnosferatu.3.c

		add_prestige = 100
		
	}
	
	after = {
		hidden_effect = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.60 years = { 5 10 } } }
			80 = { 
				if = {
					limit = {
						has_character_flag = nictuku_well_prepared_flag
					}
					remove_character_flag = nictuku_well_prepared_flag
				}
				else_if = {
					limit = {
						has_character_flag = nictuku_prepared_flag
					}
					remove_character_flag = nictuku_prepared_flag
				}
				else = {}
			}
		}
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#not a fake news
podnosferatu.60 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.60.t
	desc = podnosferatu.60.desc
	theme = nosferatu_coa
	left_portrait = {
		character = root
		animation = fear
	}	
	
	option = {
		name = podnosferatu.60.a
	
		duel = {
			skill = intrigue
			value = 20
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 0.5
					}
				desc = podnosferatu.30.effect.success.desc
				
				
			send_interface_toast = {
				title = podnosferatu.30.effect.success.desc
				left_icon = root
				
				add_prestige = 200
				}				
				
			modifier = {
				has_character_flag = nictuku_well_prepared_flag
				add = 10
			}
			modifier = {
				has_character_flag = nictuku_prepared_flag
				add = 5
			}
			}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -0.5
					}

					desc = podnosferatu.60.critical.failure.effect.desc
					
					death = {
						death_reason = death_murder
					}
				}
			}		
		
	}

	option = {
		name = POD_survey_events.110.option.d
	
		add_character_modifier = {
			modifier = broken_authority_modifier
			years = 5
		}
	}
	
	after = {
		hidden_effect = {
		if = {
			limit = {
				has_character_flag = nictuku_well_prepared_flag
			}
			remove_character_flag = nictuku_well_prepared_flag
		}
		else_if = {
			limit = {
				has_character_flag = nictuku_prepared_flag
			}
			remove_character_flag = nictuku_prepared_flag
		}
		else = {}
		
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#Paranoia? Gorgo
podnosferatu.7 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.7.t
	desc = podnosferatu.7.desc
	theme = nosferatu_coa 
	right_portrait = {
		character = root
		animation = worry
	}
	
	option = {
		name = podnosferatu.7.a
		
		add_character_flag = nictuku_well_prepared_flag
		
		custom_tooltip = nictuku_well_prepared_tt
		
		add_character_modifier = {
			modifier = nictuku_knight_large_modifier
			years = 5
		}
	}
	
	option = {
		name = podnosferatu.3.b

		add_character_flag = nictuku_prepared_flag
		
		custom_tooltip = nictuku_prepared_tt
		
		add_character_modifier = {
			modifier = nictuku_knight_modifier
			years = 5
		}	
	}

	option = {
		name = podnosferatu.3.c

		add_prestige = 100
		
	}
	
	after = {
		hidden_effect = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.70 years = { 5 10 } } }
			80 = { 
				if = {
					limit = {
						has_character_flag = nictuku_well_prepared_flag
					}
					remove_character_flag = nictuku_well_prepared_flag
				}
				else_if = {
					limit = {
						has_character_flag = nictuku_prepared_flag
					}
					remove_character_flag = nictuku_prepared_flag
				}
				else = {}
			}
		}
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#not a fake news
podnosferatu.70 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.70.t
	desc = podnosferatu.70.desc
	theme = nosferatu_coa
	left_portrait = {
		character = root
		animation = fear
	}	
	
	option = {
		name = podnosferatu.70.a
	
		duel = {
			skill = martial
			value = 20
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 0.5
					}
				desc = podnosferatu.30.effect.success.desc
				
				
			send_interface_toast = {
				title = podnosferatu.30.effect.success.desc
				left_icon = root
				
				add_prestige = 200
				}				
				
			modifier = {
				has_character_flag = nictuku_well_prepared_flag
				add = 10
			}
			modifier = {
				has_character_flag = nictuku_prepared_flag
				add = 5
			}
			}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -0.5
					}

					desc = podnosferatu.70.critical.failure.effect.desc
					
					death = {
						death_reason = death_murder
					}
				}
			}		
		
	}

	option = {
		name = POD_survey_events.110.option.d
	
		add_character_modifier = {
			modifier = broken_authority_modifier
			years = 5
		}
	}
	
	after = {
		hidden_effect = {
		if = {
			limit = {
				has_character_flag = nictuku_well_prepared_flag
			}
			remove_character_flag = nictuku_well_prepared_flag
		}
		else_if = {
			limit = {
				has_character_flag = nictuku_prepared_flag
			}
			remove_character_flag = nictuku_prepared_flag
		}
		else = {}
		
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#Paranoia? Illuyankas 
podnosferatu.8 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.8.t
	desc = podnosferatu.8.desc
	theme = nosferatu_coa 
	right_portrait = {
		character = root
		animation = worry
	}
	
	option = {
		name = podnosferatu.8.a
		
		add_character_flag = nictuku_well_prepared_flag
		
		custom_tooltip = nictuku_well_prepared_tt
		
		add_character_modifier = {
			modifier = nictuku_knight_large_modifier
			years = 5
		}
	}
	
	option = {
		name = podnosferatu.3.b

		add_character_flag = nictuku_prepared_flag
		
		custom_tooltip = nictuku_prepared_tt
		
		add_character_modifier = {
			modifier = nictuku_knight_modifier
			years = 5
		}	
	}

	option = {
		name = podnosferatu.3.c

		add_prestige = 100
		
	}
	
	after = {
		hidden_effect = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.80 years = { 5 10 } } }
			80 = { 
				if = {
					limit = {
						has_character_flag = nictuku_well_prepared_flag
					}
					remove_character_flag = nictuku_well_prepared_flag
				}
				else_if = {
					limit = {
						has_character_flag = nictuku_prepared_flag
					}
					remove_character_flag = nictuku_prepared_flag
				}
				else = {}
			}
		}
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#not a fake news
podnosferatu.80 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.80.t
	desc = podnosferatu.80.desc
	theme = nosferatu_coa
	left_portrait = {
		character = root
		animation = fear
	}	
	
	option = {
		name = podnosferatu.80.a
	
		duel = {
			skill = prowess
			value = 60
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 0.5
					}
				desc = podnosferatu.30.effect.success.desc
				
				
			send_interface_toast = {
				title = podnosferatu.30.effect.success.desc
				left_icon = root
				
				add_prestige = 200
				}				
				
			modifier = {
				has_character_flag = nictuku_well_prepared_flag
				add = 20
			}
			modifier = {
				has_character_flag = nictuku_prepared_flag
				add = 10
			}
			}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -0.5
					}

					desc = podnosferatu.80.critical.failure.effect.desc
					
					death = {
						death_reason = death_murder
					}
				}
			}		
		
	}

	option = {
		name = POD_survey_events.110.option.d
	
		add_character_modifier = {
			modifier = broken_authority_modifier
			years = 5
		}
	}
	
	after = {
		hidden_effect = {
		if = {
			limit = {
				has_character_flag = nictuku_well_prepared_flag
			}
			remove_character_flag = nictuku_well_prepared_flag
		}
		else_if = {
			limit = {
				has_character_flag = nictuku_prepared_flag
			}
			remove_character_flag = nictuku_prepared_flag
		}
		else = {}
		
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#Paranoia? Nuckalavee  
podnosferatu.9 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.9.t
	desc = podnosferatu.9.desc
	theme = nosferatu_coa 
	right_portrait = {
		character = root
		animation = worry
	}
	
	option = {
		name = podnosferatu.9.a
		
		add_character_flag = nictuku_well_prepared_flag
		
		custom_tooltip = nictuku_well_prepared_tt
		
		add_character_modifier = {
			modifier = nictuku_knight_large_modifier
			years = 5
		}
	}
	
	option = {
		name = podnosferatu.3.b

		add_character_flag = nictuku_prepared_flag
		
		custom_tooltip = nictuku_prepared_tt
		
		add_character_modifier = {
			modifier = nictuku_knight_modifier
			years = 5
		}	
	}

	option = {
		name = podnosferatu.3.c

		add_prestige = 100
		
	}
	
	after = {
		hidden_effect = {
		random_list = {
			20 = { trigger_event = { id = podnosferatu.90 years = { 5 10 } } }
			80 = { 
				if = {
					limit = {
						has_character_flag = nictuku_well_prepared_flag
					}
					remove_character_flag = nictuku_well_prepared_flag
				}
				else_if = {
					limit = {
						has_character_flag = nictuku_prepared_flag
					}
					remove_character_flag = nictuku_prepared_flag
				}
				else = {}
			}
		}
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}

#not a fake news
podnosferatu.90 = {
	type = character_event
	content_source = dlc_POD
	title = podnosferatu.90.t
	desc = podnosferatu.90.desc
	theme = nosferatu_coa
	left_portrait = {
		character = root
		animation = fear
	}	
	
	option = {
		name = podnosferatu.80.a
	
		duel = {
			skill = prowess
			value = 60
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = 0.5
					}
				desc = podnosferatu.30.effect.success.desc
				
				
			send_interface_toast = {
				title = podnosferatu.30.effect.success.desc
				left_icon = root
				
				add_prestige = 200
				}				
				
			modifier = {
				has_character_flag = nictuku_well_prepared_flag
				add = 20
			}
			modifier = {
				has_character_flag = nictuku_prepared_flag
				add = 10
			}
			}
				10 = {
					compare_modifier = {
						value = scope:duel_value
						multiplier = -0.5
					}

					desc = podnosferatu.80.critical.failure.effect.desc
					
					death = {
						death_reason = death_murder
					}
				}
			}		
		
	}

	option = {
		name = POD_survey_events.110.option.d
	
		add_character_modifier = {
			modifier = broken_authority_modifier
			years = 5
		}
	}
	
	after = {
		hidden_effect = {
		if = {
			limit = {
				has_character_flag = nictuku_well_prepared_flag
			}
			remove_character_flag = nictuku_well_prepared_flag
		}
		else_if = {
			limit = {
				has_character_flag = nictuku_prepared_flag
			}
			remove_character_flag = nictuku_prepared_flag
		}
		else = {}
		
		trigger_event = { id = podnosferatu.2 years = 11 }
	}
	}
}