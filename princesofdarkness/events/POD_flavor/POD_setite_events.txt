namespace = podsetite

# intro
podsetite.1 = {
	type = character_event
	content_source = dlc_POD
	title = podsetite.1.t
	desc = podsetite.1.desc
	theme = setite_coa 

	right_portrait = {
		character = root
		animation = personality_zealous
	}
	
	trigger = { 
	}

	option = {
		name = podsetite.1.a		

		add_internal_flag = dangerous	
		
		custom_tooltip = setite_intro_tt
		
	}
	
}

#Corrupt a religion
podsetite.2 = {
	type = character_event
	content_source = dlc_POD
	title = podsetite.2.t
	desc = podsetite.2.desc
	theme = setite_coa 
	right_portrait = {
		character = root
		animation = personality_zealous
	}
	
	option = {
		name = podsetite.1.a
		
		add_prestige = 500

		every_religion_global = {
			limit = {
				is_in_family = rf_kindred 
			}
		random_faith = {
			limit = {
		NOT = { has_doctrine = special_doctrine_set_pillars_kindred }
		NOT = { has_doctrine = special_doctrine_set_corrupted_kindred }
		any_holy_site = {
			count >= 3
			county = {
				is_holy_site_controlled_by = root
			}
		}
		}
		add_doctrine = special_doctrine_set_corrupted_kindred
		if = {
			limit = {
				has_doctrine = doctrine_setite_evil
			}
			remove_doctrine = doctrine_setite_evil
		}
		}
		}		
	}
	
}
